---
phase: 05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zsh/.zshrc
  - README.md
  - zsh/.zshrc.local.example
autonomous: true
requirements:
  - SHEL-02
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "explicit (D-01): typing the first character auto-shows the completion list below the prompt with no keypress"
    - "explicit (D-02): an empty prompt shows no list — nothing pops up until the first character is typed"
    - "explicit (D-03): ghost autosuggestion text and the dropdown list render simultaneously, each with its own accept key"
    - "stock-default (D-04): auto-show fires in every typing context — command name, argument, path, mid-word, after sudo (judged live)"
    - "explicit (D-05): first Enter on a highlighted item selects it into the buffer for editing; only a second Enter executes"
    - "explicit (D-06-Esc): Esc keeps 100% stock vi behavior (insert-to-normal) and is never rebound"
    - "backstop (D-06-CtrlC): Ctrl+C keeps stock SIGINT semantics; the buffer-preserving menu dismiss is Ctrl+G (send-break)"
    - "explicit (D-07): Tab cycles forward and Shift-Tab cycles back inside the menu; arrow keys also navigate"
    - "explicit (D-08): Right-arrow in insert mode accepts ghost text when a suggestion is shown, else moves the cursor"
    - "explicit (D-09/D-10): fzf never breaks autocomplete; expendable fzf extras are stripped, Ctrl+R history is intact"
    - "explicit (D-12): all three plugins stay loaded with Ctrl+R owned by fzf and Tab owned by autocomplete"
    - "explicit (D-14): shell spine regresses clean — auto-show, Ctrl+R, ghost, Tab menu-select, error-free reload"
    - "explicit (D-15): no vi motion key (hjkl, w/b, gg/G) is rebound"
    - "explicit (D-16/D-17): full untruncated list by default with a visible (MORE) hint when the line cutoff engages"
    - "explicit (D-20): the auto-show list never contains history; history stays behind Ctrl+R"
    - "explicit (D-21): candidates match by prefix only — no fuzzy-anywhere matching"
    - "assumption (D-16-number): list-lines 200 feels lag-free; fallback ladder 200 to 60 to 16 if the live verdict reports lag"
    - "assumption (D-22): upstream stock result ordering is acceptable; user judges feel live"
    - "assumption (D-20-scope): Up-arrow opening the history menu on an explicit keypress does not violate the no-history rule"
  artifacts:
    - zsh/.zshrc (reordered plugin/fzf regions, new ownership block, two dead-code deletions)
    - README.md (behavior-text touch-up for auto-show, split ownership, prefix-only, dismiss keys)
    - zsh/.zshrc.local.example (commented list-lines override example)
  key_links:
    - "fzf ladder evaluates BEFORE the autocomplete engine load, and the ownership block evaluates AFTER both (last-writer-wins)"
    - "prefix-only compsys overrides sit AFTER the engine load or the engine silently overwrites them"
    - "Right-arrow ghost-accept binds AFTER all plugin loads so the captured original widget is vi-forward-char"
---

<objective>
Restore real-time type-ahead completion: typing the first character auto-shows the
marlonrichert/zsh-autocomplete list with no keypress (per D-01), while keeping ghost text,
Ctrl+R fzf history, Tab menu-select, and stock vi behavior intact (per D-03, D-05, D-07, D-08,
D-12, D-15, SHEL-02 no-regression). The whole fix lives in zsh/.zshrc evaluation order plus
atomic doc touch-ups (per D-18); no installer change, no new plugin, zero new files (per D-13, D-19).

Purpose: Close the deferred Phase-3 debt (STATE.md Deferred Items: typing 2-3 chars plus pause
shows no list; Tab menu-select plus ghost unverified) so daily Zsh feels finished.
Output: Modified zsh/.zshrc, README.md, zsh/.zshrc.local.example — proven by syntax plus
structural checks now and the user's live terminal verdict at close (per D-13).
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp/05-CONTEXT.md
@.planning/phases/05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp/05-RESEARCH.md
@.planning/phases/05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp/05-PATTERNS.md
@zsh/.zshrc
@zsh/.zshrc.local.example

Forbidden symbols (never add these anywhere — verified nonexistent or explicitly rejected
in 05-RESEARCH.md): engine styles widget-style, fzf-completion, find-directories; the
history-context style default-context; any stty isig manipulation; any Enter-accept binding
(.accept-line) in the menuselect keymap. The <acceptance_criteria> gates below prove absence.
</context>

<tasks>

<task type="tracer">
  <name>Tracer: fzf ladder before engine plus dead-code deletion, one coherent file end to end</name>
  <files>zsh/.zshrc</files>
  <read_first>zsh/.zshrc lines 283-362 (plugin block, fzf ladder, finalize block); 05-CONTEXT.md Phase Boundary plus D-11 and D-12; 05-RESEARCH.md Prime-suspect ranking rows 1-3 plus Anti-Patterns; 05-PATTERNS.md fzf ladder pattern plus Dead-code deletion pattern plus Frozen ordering constraints</read_first>
  <action>Reorder the single edit site per D-11 and D-12 without touching the frozen anchors (p10k instant prompt stays at file top, the .local sourcing line stays where it is, the array PATH self-assignment stays the last PATH line). Relocate the entire fzf degradation ladder body (version probe with sort -V branch, both legacy key-bindings rungs, Debian doc-examples rung, unconditional Ctrl+R bind plus widget-missing warn) from after the engine to directly between the fzf-history-search load and the autocomplete engine load, keeping its warn-and-continue idiom (every probe ends in silent fallthrough or true, no probe may return nonzero). Simplify the engine atload ice to a single main-map Tab binding entering menu-select, removing the second menuselect backtab line which duplicates what the engine binds itself. Delete the dead staged-modifiers ice at line 288 (stages modifiers for a completion-definitions repo that never loads — confirmed absent from the Zinit plugins dir, so delete, never convert into a real load) together with its one-line comment. Delete the whole commented finalize section at lines 346-359 (engine owns its deferred compinit at first precmd; nothing turbo-loads) while keeping the unset ZI_REPO line. Keep all three locked plugins loaded and keep the existing autosuggest Ctrl+Space and Ctrl+Underscore binds untouched. Every moved or deleted hunk keeps or gains a Why comment citing its D-ID.</action>
  <verify>
    <automated>zsh -n zsh/.zshrc</automated>
    <automated>grep -v '^[[:space:]]*#' zsh/.zshrc | grep -c 'marlonrichert/zsh-autocomplete' | grep -q '^1$'</automated>
    <automated>test "$(grep -n 'FZF DEGRADATION LADDER' zsh/.zshrc | cut -d: -f1)" -lt "$(grep -n '^zi light marlonrichert' zsh/.zshrc | cut -d: -f1)"</automated>
    <automated>grep -q 'zsh-autosuggestions' zsh/.zshrc</automated>
    <automated>grep -q 'fast-syntax-highlighting' zsh/.zshrc</automated>
    <automated>grep -q 'fzf-history-search' zsh/.zshrc</automated>
    <automated>grep -q 'HOME/.zshrc.local' zsh/.zshrc</automated>
    <automated>grep -v '^[[:space:]]*#' zsh/.zshrc | grep 'bindkey' | grep -c '\^C' | grep -q '^0$'</automated>
    <automated>grep -v '^[[:space:]]*#' zsh/.zshrc | grep 'menuselect' | grep -c '\^\[' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>zsh -n exits 0. The ladder header line number is smaller than the engine load line number, proving ladder-before-engine order. The menuselect backtab override is gone from the file: grep -v comment-filter piped to grep -c for reverse-menu-complete prints 0. The dead line-288 ice is gone: comment-filtered grep -c for the extra completion-definitions repo token prints 0. The finalize block is gone: comment-filtered grep -c for each of its two compinit-helper tokens prints 0. p10k instant prompt block is still within the first 20 lines and the array PATH self-assignment is still the last PATH-relevant line. All three kept plugin loads plus the history-search load are still present, the HOME .zshrc.local sourcing line is still present, the comment-filtered bindkey-scoped caret-C count prints 0, and the menuselect-scoped Esc-bracket count prints 0.</acceptance_criteria>
  <done>Tracer slice committed: ladder evaluates before the engine, engine stays the last plugin load, both dead blocks deleted, file parses clean.</done>
</task>

<task type="auto">
  <name>Ownership block: prefix-only styles plus ghost-accept plus expendable strips</name>
  <files>zsh/.zshrc</files>
  <read_first>zsh/.zshrc plugin and ladder regions as left by the tracer task; 05-RESEARCH.md Code Examples ownership sketch plus Legacy-rung strip sketch plus D-08/D-09/D-10/D-21 rows; 05-PATTERNS.md Ownership re-assertion pattern plus bindkey idiom catalog plus zstyle placement rule; 05-CONTEXT.md D-06, D-07, D-08, D-09, D-10, D-16, D-21</read_first>
  <action>Append one ownership block AFTER the engine load (it must be the last writer in ZLE, per D-09 and D-12) with a Phase-5 header comment citing the Tab-versus-Ctrl+R split. In it, re-assert the main-map Tab binding entering menu-select and the Ctrl+R fzf-history-widget binding with the existing missing-widget yellow warn moved here (per D-07 and D-12). Add the two compsys prefix-only overrides (completer limited to expand plus complete plus ignored; case-insensitive prefix matcher) which MUST sit after the engine load since identical patterns resolve last-set-wins (per D-21; document the intended loss of typo-correction in the Why comment). Add the documentary first-char trigger style with value 1 (per D-01) and the display-line cutoff style with value 200 (per D-16). Bind Right-arrow to autosuggest-accept in the viins keymap for both the terminfo application-cursor sequences so ghost accept falls back to plain cursor movement when no suggestion is shown (per D-08; binds go here, after all plugins, so the captured original is vi-forward-char). Bind Ctrl+G to send-break in the menuselect keymap as the buffer-preserving dismiss, with a comment recording that Ctrl+C keeps stock SIGINT semantics which aborts the whole line and therefore can never be a pure list-dismiss (per D-06 closest-achievable; never rebind Ctrl+C and never touch Esc). In the relocated ladder, strip expendables per rung without touching Ctrl+R: on each legacy rung add a loop over the emacs, viins, and vicmd keymaps removing the Ctrl+T and Meta-C bindings; on the native rung prefix the source with empty Ctrl-T and Alt-C command assignments plus an empty completion-trigger assignment (per D-10; the trigger-disable is assumption A1 — keep it, the live verdict confirms). Keep arrow and Shift-Tab behavior stock and add zero vi-motion rebinds (per D-07 and D-15).</action>
  <verify>
    <automated>zsh -n zsh/.zshrc</automated>
    <automated>grep -n "zstyle ':completion:\*' completer _expand _complete _ignored" zsh/.zshrc</automated>
    <automated>grep -n "zstyle ':autocomplete:\*' list-lines 200" zsh/.zshrc</automated>
    <automated>test "$(grep -n 'Ownership (Phase 5)' zsh/.zshrc | cut -d: -f1)" -gt "$(grep -n '^zi light marlonrichert' zsh/.zshrc | cut -d: -f1)"</automated>
    <automated>grep -q 'zsh-autosuggestions' zsh/.zshrc && grep -q 'fast-syntax-highlighting' zsh/.zshrc && grep -q 'fzf-history-search' zsh/.zshrc</automated>
    <automated>grep -q 'HOME/.zshrc.local' zsh/.zshrc</automated>
    <automated>grep -v '^[[:space:]]*#' zsh/.zshrc | grep 'bindkey' | grep -c '\^C' | grep -q '^0$'</automated>
    <automated>grep -v '^[[:space:]]*#' zsh/.zshrc | grep 'menuselect' | grep -c '\^\[' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>The ownership header line number is greater than the engine load line number, and both compsys override line numbers are greater than the engine load line number, proving after-engine placement. Comment-filtered grep -c prints 0 for the history-context style token, 0 for each nonexistent engine-style token named in the plan context, 0 for the stty token, and 0 for the menu Enter-accept widget token. bindkey dump after sourcing shows caret-I bound to menu-select, caret-R bound to the fzf history widget, both viins Right-arrow sequences bound to autosuggest-accept, and caret-G in menuselect bound to send-break. No vicmd motion binding differs from stock: diff of bindkey -M vicmd output before and after shows zero motion changes. The comment-filtered bindkey-scoped caret-C count still prints 0, the menuselect-scoped Esc-bracket count still prints 0, all three kept plugin loads plus the history-search load are still present, and the HOME .zshrc.local sourcing line is still present.</acceptance_criteria>
  <done>Ownership block committed: Tab and Ctrl+R split re-asserted last, prefix-only plus first-char plus 200-line cutoff active, Right-arrow ghost accept and Ctrl+G dismiss bound, expendables stripped, Ctrl+R intact.</done>
</task>

<task type="auto">
  <name>Atomic docs plus ad-hoc proof suite and assumption flags</name>
  <files>README.md, zsh/.zshrc.local.example, zsh/.zshrc</files>
  <read_first>README.md Machine-local overrides section near line 140 plus Manual Installation Zsh Plugin Manager near line 122; zsh/.zshrc.local.example full file; zsh/.zshrc ownership-block region as left by the ownership task (header comment through the dismiss bind); 05-CONTEXT.md Specific Ideas verbatim quotes plus D-04 plus D-13 and D-14 plus D-16; AGENTS.md (confirm no interactive-completion behavior text exists there — generated file, do not hand-edit)</read_first>
  <action>Ship the Phase-1 atomic-docs rule in the same commit as the behavior change (per D-18): extend the README shell area with five facts — list auto-shows on the first typed character and stays quiet on an empty prompt, Tab belongs to autocomplete while Ctrl+R belongs to fzf history, matching is prefix-only with typo-correction intentionally off, the full list shows with an automatic visible MORE marker past the line cutoff, and Ctrl+G dismisses the menu while Ctrl+C keeps stock whole-line abort plus Esc keeps stock vi behavior. Append a commented override example for the display-line cutoff to the local-example template following its existing Example-comment voice, noting the 200 default and the 60 and 16 fallbacks plus that the real file stays gitignored. Record the four live-judgment flags as comments in the zshrc ownership block region: the Up-arrow history-menu scope question, the final cutoff number, the stock result ordering, and the completion-trigger disable on the native rung. Run the ad-hoc diagnostic suite read-only (widget registration listing filtered for autocomplete, autosuggest, and fzf; caret-I, caret-R, and viins Right-arrow binding dumps; backtab availability print; both zstyle listings; engine log tail when present) and the D-14 spine checklist in a live terminal (one-char auto-show, empty-prompt silence, ghost alongside list, Tab enter plus Tab and Shift-Tab and arrow cycling, first-Enter edits without executing, Esc enters normal mode, Ctrl+R opens fzf history, clean reload with no errors other than the intended fzf-missing warn, plus one D-04 per-context auto-show probe each for command name, argument, path, mid-word, and after sudo, each judged as stock-default live verdict). Propose zero new files: fail the task if git status shows any new file beyond the three modified paths.</action>
  <verify>
    <automated>zsh -n zsh/.zshrc</automated>
    <automated>! git status --porcelain | grep -v '^ M \(zsh/.zshrc\|README.md\|zsh/.zshrc.local.example\)$' | grep -q .</automated>
    <automated>grep -c 'list-lines' zsh/.zshrc.local.example | grep -q '^[12]$'</automated>
  </verify>
  <acceptance_criteria>README contains the five documented facts worded observably (auto-show, split ownership, prefix-only, MORE marker, dismiss keys). The local-example file contains a commented cutoff override mentioning 200 plus the fallbacks, and no other functional line is added to it. git status shows at most the three modified paths and zero new files. The diagnostic suite output is pasted into the plan SUMMARY with each line explained, and the spine checklist records pass or fail per item including one live-verdict line per D-04 context (command name, argument, path, mid-word, after sudo), with the four live-judgment flags marked for the user's verdict.</acceptance_criteria>
  <done>Docs and proof committed atomically with the behavior change; assumption flags and diagnostic evidence recorded for the live verdict that closes the phase per D-13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo file to HOME shell | zsh/.zshrc evaluates in the user's interactive shell with account privileges |
| HOME-only local sourcing | ~/.zshrc.local is sourced if present; must never reach outside HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | ~/.zshrc.local sourcing | low | mitigate | Source only the fixed HOME path with an existence guard; document that only machine-local content belongs there; real file stays gitignored |
| T-05-02 | Denial of service | Terminal job control (Ctrl+C) | low | mitigate | Reject any terminal-driver change; Ctrl+C keeps stock SIGINT so foreground-job cancel always works; menu dismiss uses Ctrl+G send-break confined to the menuselect keymap |
| T-05-03 | Information disclosure | Shell config surface | low | accept | No secrets, tokens, or machine paths added; prefix-only matching reduces match surface; rationale: config-only phase with no credential handling |
| T-05-04 | Elevation of privilege | Installer and keyd path | low | accept | setup.sh is untouched and no privileged write is added; rationale: out of scope and explicitly unneeded per research D-18 finding |
| T-05-05 | Spoofing | Ghost suggestion rendering | low | accept | Ghost text only displays completions for the typed prefix and executes nothing without an explicit accept plus Enter; rationale: display-only surface |

Package legitimacy gate: no package-manager installs and no new plugins in this phase, so no audit table is required and no blocking human checkpoint is inserted.
</threat_model>

<verification>
Automated (executor-run): zsh -n passes; ladder-before-engine and ownership-after-engine
line-order proofs pass; comment-filtered absence gates pass for dead code, nonexistent engine
styles, the history-context style, terminal-driver changes, and menu Enter-accept bindings;
presence gates pass for the four real styles, the six ownership bindings, and the per-rung
expendable strips; git fileset gate shows only the three modified paths.
Human (user live terminal, closes the phase per D-13): type one character and confirm the list
auto-shows; confirm empty-prompt silence, ghost-plus-list coexistence, Tab enter and cycling,
first-Enter edits without executing, Esc enters normal mode, Ctrl+R opens fzf history, one
auto-show probe per D-04 context (command name, argument, path, mid-word, after sudo), and a
clean reload with no unexpected warnings. Open live-judgment flags: Up-arrow history-menu
scope, final cutoff number (200 pick, 60/16 fallbacks), stock result ordering feel, native-rung
trigger disable. SHEL-02 no-regression: Ctrl+R history plus PATH dedup plus local overrides
still behave as Phase 3 left them.
</verification>

<success_criteria>
Typing the first character auto-shows the completion list with no keypress while the empty
prompt stays quiet; auto-show fires across command, argument, path, mid-word, and sudo
contexts; ghost text and list coexist; Enter selects-then-edits; Esc is stock vi;
Tab and Shift-Tab cycle; Right-arrow accepts ghost; fzf expendables are stripped and Ctrl+R
history works; vi motions are untouched; the full list shows with a visible MORE marker past
the cutoff; history never appears in auto-show; matching is prefix-only; docs ship in the
same commit; zero new files exist; the user gives the live verdict.
</success_criteria>

<output>
Create `.planning/phases/05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New symbols (all inside existing files — D-13 forbids new files):

- zsh/.zshrc ownership block: `zstyle ':completion:*' completer _expand _complete _ignored`
  (D-21, after engine load), `zstyle ':completion:*' matcher-list 'm:{[:lower:]-}={[:upper:]_}'`
  (D-21, after engine load), `zstyle ':autocomplete:*' min-input 1` (D-01 documentary),
  `zstyle ':autocomplete:*' list-lines 200` (D-16, user judges; fallbacks 60, 16).
- zsh/.zshrc bindings: `bindkey '^I' menu-select` re-asserted last (D-07/D-12),
  `bindkey '^R' fzf-history-widget` re-asserted last plus moved missing-widget warn (D-12),
  `bindkey -M viins '^[[C' autosuggest-accept` plus `bindkey -M viins '^[OC' autosuggest-accept`
  (D-08), `bindkey -M menuselect '^G' send-break` (D-06 closest-achievable).
- zsh/.zshrc ladder additions: `FZF_CTRL_T_COMMAND=` and `FZF_ALT_C_COMMAND=` prefixes plus
  `FZF_COMPLETION_TRIGGER=''` on the native rung (D-10; trigger is assumption A1), and a
  `bindkey -r` loop over emacs/viins/vicmd removing Ctrl+T and Meta-C on each legacy rung
  (D-10; Ctrl+R deliberately kept).
- zsh/.zshrc deletions: dead staged-modifiers ice at line 288 plus its comment (never loads);
  redundant menuselect backtab line inside the engine atload (engine binds it itself);
  commented finalize section at lines 346-359 (engine owns compinit).
- README.md: five-fact behavior text (auto-show, split ownership, prefix-only, MORE marker,
  dismiss keys). zsh/.zshrc.local.example: commented cutoff override example only.
- No setup.sh change (fzf plus zsh already in common on all families), no new plugin (D-19
  swap bar not met), no harness or probe files (D-13).

## Source audit

GOAL (ROADMAP Phase 5: fix type-ahead so no Tab press is needed): covered by the tracer plus
ownership tasks. REQ (SHEL-02 carry-forward, Ctrl+R half live-passed in Phase 3): covered by
frontmatter requirements plus the D-14 spine regression. RESEARCH feasibility table: every
ACHIEVABLE row maps to a concrete edit above; D-06-CtrlC maps to the documented
closest-achievable alternative (never the impossible pure-dismiss); nonexistent knobs are
listed as forbidden and gated absent. CONTEXT D-01..D-23: D-01 tracer min-input plus ladder
order; D-02 stock default plus spine check; D-03 stock default plus spine check; D-04 stock
default plus live-verify note; D-05 stock default plus anti-recipe guard; D-06 split Esc
untouched plus Ctrl+G alternative; D-07 kept Tab override plus stock cycling; D-08 Right-arrow
bind; D-09 fzf loses by default; D-10 expendables stripped, Ctrl+R kept; D-11 ladder reworked;
D-12 three plugins kept plus split ownership; D-13 ad-hoc proof only; D-14 spine checklist;
D-15 zero motion rebinds; D-16 list-lines 200 plus ladder; D-17 stock MORE marker; D-18
zshrc-plus-docs scope with atomic-docs task; D-19 engine kept; D-20 history excluded by never
setting the history-context style; D-21 prefix-only overrides; D-22 stock ordering kept;
D-23 researcher method already satisfied by 05-RESEARCH.md. Agent's-discretion items
(D-09 resolution, D-22 ordering, D-16 number) ship as flagged live-judgment assumptions, not
locked configs. Deferred Ideas: none. No gaps — no phase split needed.
