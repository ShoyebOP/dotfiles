---
phase: 05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - zsh/.zshrc
autonomous: true
requirements:
  - SHEL-02
estimate:
  tokens: 10000
  raw_tokens: 5000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Right-arrow in viins accepts ghost text when a suggestion is shown and advances the cursor one char when no suggestion is shown (D-08, from WR-01)"
    - "Reload is error-free and silent headless: menuselect bind never errors without complist, no WARN prints non-interactively, Ctrl+R never installs a dead key on fzf-less hosts (D-14, from WR-02/WR-03/WR-04)"
    - "Spine no-regression: ladder-before-engine order, prefix-only styles, Tab ownership, three plugins intact, zsh -n clean (SHEL-02 carry-forward)"
  artifacts:
    - zsh/.zshrc
  key_links:
    - "wrapper registered with zle -N before both viins binds point at it"
    - "zmodload menuselect guard evaluated before the menuselect bind"
    - "Ctrl+R bind lives inside the widget-exists guard, else stock history-incremental-search-backward is left intact"
---

<objective>
Close the two VERIFICATION.md gaps in the single ownership block added by commit 81dccb7, with zero new scope: restore the D-08 Right-arrow fallback via a real wrapper widget, and harden the D-14 reload path (menuselect guard, interactive-gated WARN, widget-guarded Ctrl+R). One contiguous edit, one commit.

Purpose: Right-arrow is currently a dead key with no ghost visible (stock-vi regression), and clean reload prints spurious errors/warnings headless or on fzf-less hosts — both contradict the D-08/D-14 truths this phase promised.
Output: Modified zsh/.zshrc only, proven headless plus the user live re-verdict per D-13.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp/05-CONTEXT.md
@.planning/phases/05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp/05-RESEARCH.md
@.planning/phases/05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp/05-PATTERNS.md
@.planning/phases/05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp/05-VERIFICATION.md
@.planning/phases/05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp/05-REVIEW.md
@zsh/.zshrc
</context>

<tasks>

<task type="tracer">
  <name>Gap closure: Right-arrow fallback wrapper plus reload robustness guards</name>
  <files>zsh/.zshrc</files>
  <read_first>zsh/.zshrc lines 300-415 (ladder, engine load, full ownership block — the only edit site); 05-VERIFICATION.md Gaps Summary plus gap missing bullets; 05-REVIEW.md WR-01 through WR-04 fixes plus WR-05/WR-06 and IN-01 advisory; 05-01-PLAN.md threat model T-05-01 through T-05-05</read_first>
  <action>Make exactly four mandatory hunks confined to 361-400; optional hardening may touch ladder/native rung, per D-08 and D-14 with D-IDs cited in Why comments. First, replace the D-08 Why comment at lines 389-392 (which falsely claims the bare widget falls back) with a corrected comment stating the bare widget no-ops with zero args per installed src/bind.zsh and that the wrapper below provides the real fallback, then define a function named autosuggest-accept-or-forward that tests the POSTDISPLAY parameter for non-empty and invokes the autosuggest accept widget when set versus the vi forward-char widget when empty, register it with the ZLE new-widget builtin before any use, and repoint both viins Right-arrow binds at lines 393-394 (the two application-cursor sequences) from the bare accept widget to the wrapper name. Second, replace line 400 with a module-load line for the complist module using the ignore flag plus stderr suppression immediately followed by the menuselect Ctrl+G send-break bind with stderr suppression and an always-true fallback, so a shell without the menuselect keymap prints nothing and returns zero per WR-03. Third, restructure lines 369-374 so the Ctrl+R bind executes only inside a widget-exists conditional that lists ZLE widgets with errors suppressed and greps for the fzf history widget name, leaving stock history search untouched in the else path per WR-02, and gate the yellow WARN print on the interactive shell option so headless and scripted sources stay silent per WR-04 — the WARN text itself stays byte-identical, only its condition changes. Fourth, keep the Tab re-assertion, the four zstyle lines, the ladder, the engine load, the .local sourcing line, and the LIVE-JUDGMENT FLAGS comment untouched so last-writer-wins order is preserved. OPTIONAL hardening only if it adds zero new verification burden and stays in the same commit, otherwise skip and note the skip in the SUMMARY: extend Tab and Ctrl+R ownership with explicit per-keymap binds over the emacs, viins, and vicmd maps per WR-06, and add belt-and-braces unbinds for the expendable keys on the native rung plus extract the duplicated legacy strip loops into one shared helper per IN-01/WR-05. Out of scope and forbidden: any new plugin, installer or setup.sh change, keyd change, harness or probe file, README or local-example edit, menuselect Enter binding, terminal-driver change, vi-motion rebind, or history-context style.</action>
  <verify>
    <automated>zsh -n zsh/.zshrc</automated>
    <automated>grep -v '^[[:space:]]*#' zsh/.zshrc | grep -c 'autosuggest-accept-or-forward' | grep -q '^[34]$'</automated>
    <automated>grep -v '^[[:space:]]*#' zsh/.zshrc | grep -c 'zle -N autosuggest-accept-or-forward' | grep -q '^1$'</automated>
    <automated>grep -v '^[[:space:]]*#' zsh/.zshrc | grep 'bindkey -M viins' | grep -c 'autosuggest-accept-or-forward' | grep -q '^2$'</automated>
    <automated>grep -v '^[[:space:]]*#' zsh/.zshrc | grep 'bindkey -M viins' | grep '\^' | grep -c 'autosuggest-accept$' | grep -q '^0$'</automated>
    <automated>grep -q 'zmodload -i zsh/complist 2>/dev/null' zsh/.zshrc</automated>
    <automated>grep -q "bindkey -M menuselect '\^G' send-break 2>/dev/null || true" zsh/.zshrc</automated>
    <automated>zsh -f -c "bindkey -M menuselect '^G' send-break 2>/dev/null || true; echo GUARD-OK" | grep -q 'GUARD-OK'</automated>
    <automated>grep -q '\[\[ -o interactive \]\]' zsh/.zshrc</automated>
    <automated>zsh -c 'source ./zsh/.zshrc' 2>&1 | grep -v '^[[:space:]]*#' | grep -c 'WARN' | grep -q '^0$'</automated>
    <automated>test "$(grep -n 'zle -N autosuggest-accept-or-forward' zsh/.zshrc | cut -d: -f1)" -lt "$(grep -n "bindkey -M viins '\^\[\[C' autosuggest-accept-or-forward" zsh/.zshrc | cut -d: -f1)"</automated>
    <automated>test "$(grep -n 'FZF DEGRADATION LADDER' zsh/.zshrc | cut -d: -f1)" -lt "$(grep -n '^zi light marlonrichert' zsh/.zshrc | cut -d: -f1)"</automated>
    <automated>test "$(grep -n 'Ownership (Phase 5)' zsh/.zshrc | cut -d: -f1)" -gt "$(grep -n '^zi light marlonrichert' zsh/.zshrc | cut -d: -f1)"</automated>
    <automated>grep -q 'POSTDISPLAY' zsh/.zshrc</automated>
    <automated>grep -q 'zle autosuggest-accept' zsh/.zshrc</automated>
    <automated>grep -q 'zle vi-forward-char' zsh/.zshrc</automated>
    <automated>test "$(grep -n 'zle -N autosuggest-accept-or-forward' zsh/.zshrc | cut -d: -f1)" -lt "$(grep -n "bindkey -M viins '\^\[OC' autosuggest-accept-or-forward" zsh/.zshrc | cut -d: -f1)"</automated>
    <automated>test "$(grep -n 'zmodload -i zsh/complist' zsh/.zshrc | cut -d: -f1)" -lt "$(grep -n 'bindkey -M menuselect' zsh/.zshrc | cut -d: -f1)"</automated>
    <automated>grep -q 'zle -l 2>/dev/null | grep -q fzf-history-widget' zsh/.zshrc</automated>
    <automated>grep -v '^[[:space:]]*#' zsh/.zshrc | grep -c "bindkey.*\^R.*fzf-history-widget" | grep -q '^1$'</automated>
    <automated>test "$(grep -n 'zle -l.*fzf-history-widget' zsh/.zshrc | head -1 | cut -d: -f1)" -lt "$(grep -n "bindkey.*\^R.*fzf-history-widget" zsh/.zshrc | cut -d: -f1)"</automated>
  </verify>
  <acceptance_criteria>The file parses with exit zero. The comment-filtered count for the wrapper name prints 3 or 4 (definition plus registration plus two binds). The wrapper branches on POSTDISPLAY, invoking autosuggest-accept when set and vi-forward-char when empty. The registration line number is smaller than both viins Right-arrow bind line numbers. The comment-filtered count for viins binds ending in the bare accept widget prints 0. The literal module-load line for complist is present and its line number is smaller than the menuselect Ctrl+G line number, and the Ctrl+G line carries stderr suppression with an always-true fallback. The guarded menuselect snippet run under a bare shell prints the guard marker with exit zero and no keymap error. The ownership region contains the interactive-option gate token. A non-interactive source of the file prints a WARN count of zero. The widget-exists probe line for fzf-history-widget is present and its line number is smaller than the Ctrl+R bind line number, the comment-filtered count for Ctrl+R fzf-history-widget binds prints 1, and the else path contains no bind so stock search is preserved. The ladder header line number is smaller than the engine load line number and the ownership header line number is greater than the engine load line number. No file other than the single rc path is modified.</acceptance_criteria>
  <done>Single commit on the ownership block: Right-arrow fallback is real, reload is guarded and silent headless, Ctrl+R never installs a dead key, spine order intact, headless proofs recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo file to HOME shell | zsh/.zshrc evaluates in the user's interactive shell with account privileges |
| HOME-only local sourcing | ~/.zshrc.local is sourced if present; must never reach outside HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | ~/.zshrc.local sourcing | low | mitigate | Untouched by this plan; fixed HOME path with existence guard stays as 05-01 left it |
| T-05-02 | Denial of service | Terminal job control (Ctrl+C) | low | mitigate | No terminal-driver change, no Ctrl+C bind, no menuselect Esc bind; dismiss stays Ctrl+G confined to menuselect, now with a load guard so it cannot error at startup |
| T-05-03 | Information disclosure | Shell config surface | low | accept | No secrets, tokens, or machine paths added; wrapper branches only on the in-memory suggestion variable; rationale: config-only fix with no credential handling |
| T-05-04 | Elevation of privilege | Installer and keyd path | low | accept | setup.sh and keyd untouched, single rc file in fileset; rationale: out of scope per D-18 finding |
| T-05-05 | Spoofing | Ghost suggestion rendering | low | accept | Ghost still requires explicit accept plus Enter (two-step, nothing auto-executes); wrapper only routes an explicit Right-arrow keypress to accept-or-move; rationale: display-only surface |

Package legitimacy gate: no package-manager installs and no new plugins in this plan, so no audit table is required and no blocking human checkpoint is inserted.
</threat_model>

<verification>
Automated headless (executor-run, all must pass): syntax parse clean; wrapper definition plus registration plus two repointed binds present with bare-widget viins binds at zero; module-load guard precedes the suppressed menuselect bind and the isolated guarded snippet prints its marker with exit zero under a bare shell; interactive gate token present and a non-interactive source prints zero WARN markers; registration-before-binds, guard-before-bind, ladder-before-engine, and ownership-after-engine line-order proofs all hold; fileset is exactly the single rc path.
Human live re-verdict (user terminal, closes the gaps per D-13): with a ghost visible, Right-arrow accepts the suggestion; with the suggestion cleared (retype a non-matching prefix or clear the line), Right-arrow advances the cursor one char with no error; source reload prints no errors; Ctrl+R opens fzf history on an fzf host and falls back to stock history search with a single yellow WARN on an fzf-less host; Tab menu-select, ghost text, and auto-show feel unchanged. Out of scope for this plan: the 7 behavior_unverified items stay with the user verdict, WR-05 native-rung assumption A1 stays a live flag on the Arch host (fzf 0.44.1 here cannot execute the native rung), IN-02/IN-03/IN-04 folded only if trivial.
</verification>

<success_criteria>
Right-arrow accepts ghost when shown and moves the cursor when clear; reload is error-free with no headless WARN and no menuselect keymap error; Ctrl+R binds only when its widget exists and otherwise leaves stock search; ladder-before-engine and ownership-after-engine order still hold; syntax parses clean; exactly one file modified; the user confirms both live halves.
</success_criteria>

<output>
Create `.planning/phases/05-fix-marlonrichert-zsh-autocomplete-real-time-type-ahead-comp/05-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New symbols (all inside zsh/.zshrc — D-13 forbids new files):

- Wrapper widget `autosuggest-accept-or-forward` (D-08, from WR-01): branches on the suggestion display parameter, accepts when non-empty, moves via the forward-char widget when empty; registered before use; both viins Right-arrow sequences repointed to it; the stale fallback claim in the Why comment corrected.
- Reload guards (D-14, from WR-02/WR-03/WR-04): module-load line for the complist module plus suppressed menuselect bind; interactive-gated WARN probe with byte-identical text; widget-exists conditional owning the Ctrl+R bind with stock-search fallback.
- Optional (WR-06/IN-01/WR-05, only if cheap): explicit per-keymap Tab/Ctrl+R binds, shared expendable-strip helper, native-rung belt-and-braces unbinds — skipped without penalty if omitted, skip noted in SUMMARY.

## Source audit

| Gap source | Missing bullet | Task | How closed |
|------------|----------------|------|------------|
| VERIFICATION gap 1 (D-08 FAILED, WR-01) | Fallback wrapper widget with both viins binds pointed at it; live proof cursor advances with suggestion cleared | tracer | Wrapper defined, registered before binds, both sequences repointed, bare-widget binds at zero, Why comment corrected; headless order plus count proofs, live both-halves re-verdict |
| VERIFICATION gap 2a (D-14 PARTIAL, WR-03) | Module load plus suppressed menuselect bind at line 400 | tracer | Guard line precedes bind, isolated bare-shell snippet proves no keymap error |
| VERIFICATION gap 2b (D-14 PARTIAL, WR-04) | Gate WARN probe on interactive shell option | tracer | Gate token in ownership region, non-interactive source prints zero WARN |
| VERIFICATION gap 2c (D-14 PARTIAL, WR-02) | Bind Ctrl+R only inside widget-exists guard, else leave stock search | tracer | Conditional owns the bind, else path warns (interactive only) without binding, stock preserved |
| VERIFICATION suggested hardening (WR-06/IN-01/WR-05) | Explicit keymap loops, shared strip helper, native-rung unbinds | tracer (optional) | Folded only if cheap, marked optional, skip recorded — never blocks acceptance |
| Out of scope (not planned) | 7 behavior_unverified items; WR-05 A1 untestable on fzf 0.44.1; IN-02/IN-03/IN-04 style; new plugins, installer, harness files, setup.sh, keyd | — | Explicitly excluded per D-13; live verdict stays with the user |

GOAL (ROADMAP Phase 5: first-char auto-show with spine intact): no-regression held by order proofs plus untouched styles. REQ (SHEL-02 carry-forward): frontmatter carries SHEL-02, Ctrl+R half hardened not regressed. RESEARCH: wrapper follows the REVIEW sketch which supersedes the bare-bind sketch; per-rung mechanisms unchanged. CONTEXT D-01..D-23: D-08 and D-14 closed exactly as locked, all other D-items untouched, no deferred idea introduced. No split needed — both gaps live in one contiguous block, one plan, one commit.
