---
phase: 03-polished-shell-theme-local-overrides
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [setup.sh]
autonomous: false
requirements: [SHEL-04, EDIT-04]
gap_closure: true

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "User presses Ctrl+R and gets fzf history search, or a visible warning telling them to install fzf — never a silent dead key (SHEL-02 per D-03)"
    - "User types and the async completion list auto-shows below the prompt with no keypress; Tab only enters menu-select; ghost autosuggestion text still renders (SHEL-02 per D-04/D-05)"
    - "User reloads the shell repeatedly and PATH has zero duplicates while z, zi, zoxide, completions, and the p10k prompt still work (SHEL-03 per D-09)"
    - "User drops personal tweaks in HOME ~/.zshrc.local and the deployed nvim local.lua and they take effect without dirtying git; a fresh clone works with both files absent (SHEL-04, EDIT-04 per D-10/D-11)"
    - "User runs setup.sh --dry-run and sees the local-file preview with zero writes; a live run creates empty HOME files without truncating existing ones (SHEL-04, EDIT-04 per D-12)"
    - "No per-app appearance file is modified and no theme token, installer theme function, or mismatch warning is added anywhere (THEM-01 closed as intended-drift per D-13)"
  artifacts:
    - path: "setup.sh"
      provides: "DRY_RUN preview path invokes the already-safe local-file bootstrap so the documented dry-run command shows the preview with zero writes"
      contains: "ensure_local_files"
  key_links:
    - from: "main DRY_RUN preview branch (preview_selection plus chsh preview plus return 0)"
      to: "ensure_local_files HOME-derived local-file bootstrap"
      via: "A call in the preview path before return 0 reuses the function that already prints its Would-run marker and returns before any write, so the full documented dry-run command surfaces the preview"
      pattern: "ensure_local_files"
---

<objective>
**As a** daily Zsh user, **I want to** preview every installer write including machine-local bootstrap with a single dry-run, **so that** daily shell feels finished and safe.

Purpose: Close the single verified code gap from 03-VERIFICATION.md (T5 preview reachability, SHEL-04 plus EDIT-04 per D-12) with the preferred one-line reuse fix, then re-prove the full documented dry-run command plus live idempotence and no-truncate behavior with zero regression on the other five truths. No new features, no theme work, no scope expansion per MVP_MODE.

Output: One edited file (setup.sh preview path) committed atomically; the two interactive feel items (Ctrl+R UI, auto-show typing) recorded as an explicit human check, not code.

ROADMAP deltas stay noted, not fixed: (1) D-01 keep-BOTH plugins with split ownership supersedes one-plugin policy; (2) D-10 HOME-only local path supersedes dual-file roadmap text; (3) D-13 theme dropped as intended drift with zero code. This plan changes none of them. RESEARCH.md Q1/Q2 are substantively resolved by 03-01 execution plus VERIFICATION.md, so no RESEARCH.md edit is made here.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polished-shell-theme-local-overrides/03-CONTEXT.md
@.planning/phases/03-polished-shell-theme-local-overrides/03-RESEARCH.md
@.planning/phases/03-polished-shell-theme-local-overrides/03-PATTERNS.md
@.planning/phases/03-polished-shell-theme-local-overrides/03-VERIFICATION.md
@.planning/phases/03-polished-shell-theme-local-overrides/03-01-SUMMARY.md
@setup.sh
</context>

<tasks>

<task type="tracer">
  <name>Tracer: wire local-file bootstrap into the dry-run preview path</name>
  <files>setup.sh</files>
  <read_first>setup.sh lines 776-793 plus 1981-2023; .planning/phases/03-polished-shell-theme-local-overrides/03-VERIFICATION.md gaps plus missing plus behavior_unverified_items; .planning/phases/03-polished-shell-theme-local-overrides/03-CONTEXT.md D-10 plus D-12 plus D-13</read_first>
  <action>Per D-12, make the documented preview command surface the local-file preview by reusing the existing safe routine. In main, inside the DRY_RUN preview branch that today runs preview_selection around line 1982 then the chsh preview block around lines 1983-1997 then two echo lines plus return 0 around lines 1998-2000, insert one call to the local-file bootstrap routine defined around lines 779-793, positioned after the chsh preview block closes and before the DRY RUN complete echo plus return, with a one-line why-comment stating the preview path must surface the same marker the live path creates. Prefer the bare routine-name call with no arguments so it inherits the surrounding DRY_RUN true value and exercises its own early preview branch that prints its Would-run marker naming both HOME destinations and returns success before any filesystem mutation. Change nothing else in the branch: leave dependency preview, install preview, selection preview, chsh preview, and the final return untouched; leave the live-path call after post_verify around line 2019 untouched; keep file-global strict mode, local-variable quoting, HOME-derived paths with no repo-dir reference, and create-only-when-absent semantics exactly as they stand. Touch no shell-init file, no editor file, no gitignore, no README, and no per-app appearance file; add no central appearance token, no installer appearance routine, and no mismatch notice per D-13.</action>
  <reversibility rating="reversible">One added call line plus one comment line in the preview branch; reverts with one checkout and cannot migrate data or alter contracts.</reversibility>
  <verify>
    <automated>bash -n setup.sh && echo setup-syntax-ok</automated>
    <automated>P=$(grep -n 'preview_selection' setup.sh | tail -1 | cut -d: -f1); D=$(grep -n 'DRY RUN complete' setup.sh | head -1 | cut -d: -f1); E=$(grep -n 'ensure_local_files' setup.sh | awk -F: -v p="$P" -v d="$D" '$1>p && $1<d{print $1}'); test -n "$E" && echo preview-wired</automated>
    <automated>bash setup.sh --mode server --shell zsh --dry-run < /dev/null 2>&1 | grep -E 'zshrc\.local|local\.lua' | grep -q 'DRY RUN' && echo full-preview-ok</automated>
    <automated>rm -rf /tmp/gap02-home && mkdir -p /tmp/gap02-home && echo keepme > /tmp/gap02-home/.zshrc.local && HOME=/tmp/gap02-home bash -c 'source ./setup.sh; DRY_RUN=true ensure_local_files' | grep -q 'DRY RUN' && test ! -e /tmp/gap02-home/.config/nvim/lua/local.lua && grep -q keepme /tmp/gap02-home/.zshrc.local && echo sourced-preview-clean</automated>
    <automated>rm -rf /tmp/gap02-live && mkdir -p /tmp/gap02-live && echo keepme > /tmp/gap02-live/.zshrc.local && HOME=/tmp/gap02-live bash -c 'source ./setup.sh; DRY_RUN=false ensure_local_files' && test -f /tmp/gap02-live/.config/nvim/lua/local.lua && grep -q keepme /tmp/gap02-live/.zshrc.local && HOME=/tmp/gap02-live bash -c 'source ./setup.sh; DRY_RUN=false ensure_local_files' && grep -q keepme /tmp/gap02-live/.zshrc.local && echo live-idempotent-no-truncate</automated>
    <automated>git status --short | awk '{print $2}' | grep -Ev '^(setup.sh|.planning/phases/03-polished-shell-theme-local-overrides/03-02-PLAN.md|.planning/phases/03-polished-shell-theme-local-overrides/03-02-SUMMARY.md|.planning/ROADMAP.md)$' | grep -q .; test "$?" -ne 0 && echo change-set-clean</automated>
    <automated>grep -v '^#' setup.sh | grep -v 'intended drift' | grep -v 'intended-drift' | grep -Ec 'apply_theme|mismatch.*warn|warn.*mismatch' | grep -q '^0$' && echo no-theme-code</automated>
  </verify>
  <done>Full documented dry-run prints the local-file Would-run marker with zero writes, live bootstrap stays create-only-when-absent and idempotent, and the change set holds only the installer plus plan files with zero appearance-code.</done>
  <acceptance_criteria>bash -n exits 0 with syntax marker; strict inside-branch probe passes with an ensure_local_files call existing strictly between the selection preview line and the completion marker line; full server dry-run output contains the local marker lines with preview tag; sourced preview run creates nothing and preserves seeded content; stub-HOME live run creates the deployed editor file while seeded shell-local content survives a second run; change-set listing beyond the installer plus plan files is empty; appearance-routine absence probe returns zero.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human check: interactive shell feel plus dry-run preview read</name>
  <read_first>.planning/phases/03-polished-shell-theme-local-overrides/03-VERIFICATION.md behavior_unverified_items; .planning/phases/03-polished-shell-theme-local-overrides/03-01-SUMMARY.md Next Phase Readiness</read_first>
  <action>Confirm in one live interactive Zsh that the two behavior-unverified items from VERIFICATION.md need no code change: the fuzzy history UI on the history-search key and the async auto-show list while typing with ghost text intact. Also eyeball the fixed full dry-run output from the prior task and confirm the local-file Would-run lines read clearly. Record pass or fail verbatim; on fail, describe the observed symptom without requesting new scope.</action>
  <what-built>Tracer fix wiring the local-file bootstrap into the dry-run preview path, plus the carried Phase 3 spine (split-ownership history keys, convergent PATH guard, HOME-only local guards, gitignored locals with templates).</what-built>
  <how-to-verify>
    Open one fresh interactive Zsh on this machine and type 2-3 characters of a known command then pause without pressing Tab: a completion list appears automatically below the prompt, Tab moves into menu-select, faint ghost suggestion text still renders, and the prompt renders with no errors.
    In the same shell press the history-search key (Ctrl+R) and type a fragment of a previous command: a fuzzy history UI with matching entries appears, or on a machine without the finder a yellow warning naming the install action appears — never a silent dead key.
    In a terminal run the full preview command with server mode and zsh shell in dry-run and confirm the output shows the local-file Would-run lines alongside the existing selection and shell-change previews, with no files created.
  </how-to-verify>
  <resume-signal>Type approved if all three observations hold, or describe the failing observation with the exact key pressed and visible output</resume-signal>
  <verify>
    <automated>echo human-check-required</automated>
    <human-check>Human confirms auto-show list plus Tab menu-select plus ghost text, history-search UI or install warning, and dry-run preview readability in one live session</human-check>
  </verify>
  <done>Human confirms both interactive feel items and the preview readability, or files a concrete symptom for follow-up with no new code invented.</done>
  <acceptance_criteria>Checkpoint records an explicit approved token or a verbatim symptom description covering the typing pause observation, the history-search key observation, and the full preview readability observation.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer-preview→human-reader | New preview line discloses intended HOME write targets before any mutation; reader trusts it matches the live write |
| installer→HOME-filesystem | Reused bootstrap writes two files under HOME only on the live path; preview path must write nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Information disclosure | setup.sh preview line naming HOME destinations | low | accept | Preview names only fixed HOME-relative destinations already documented in README; no user content or secret is echoed |
| T-03-02-02 | Tampering | setup.sh preview-path call placement | medium | mitigate | Call sits strictly before the preview return and after the shell-change preview, inherits DRY_RUN true so the callee returns before any write; live-path call after post_verify untouched; create-only-when-absent plus HOME-dir guards unchanged; verified by sourced-preview-clean plus live-idempotent-no-truncate probes |
| T-03-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No registry package is installed by this plan; no install command is added, so no legitimacy gate applies |
</threat_model>

<verification>
Automated: syntax check, preview-ordering line probe, full documented dry-run marker grep, sourced-preview zero-write plus seed-preservation probe, stub-HOME live create plus idempotent no-truncate probe, plan-scoped change-set gate, and appearance-code absence probe. Human: one live interactive shell confirms auto-show typing feel, history-search UI or install warning, and preview readability per the two VERIFICATION.md behavior items. Regression: the six carried truths are re-checked — shell and editor guards untouched, so prior grep and headless proofs still hold; any failure outside setup.sh aborts as out-of-scope.
</verification>

<success_criteria>
T5 preview reachability closes: the exact documented dry-run command surfaces the local-file Would-run lines with zero writes while live runs stay idempotent and never truncate. The other five truths hold unregressed. Zero appearance code, HOME-only paths, strict-mode quoting, and noted-not-fixed roadmap deltas all hold. Human check for the two interactive feel items is recorded.
</success_criteria>

## Artifacts this phase produces

Symbols and files the review-convergence grounding pass must treat as intended additions, not drift: the single added call to the local-file bootstrap routine inside the main DRY_RUN preview branch before its return (plus one why-comment line); the already-existing Would-run marker line inside that routine naming both HOME destinations; the unchanged live-path call after post_verify; the unchanged HOME-derived destination paths and create-only-when-absent guards. No new files, no template changes, no shell-init or editor-init edits, no appearance-file touches.

## Source coverage audit (GOAL plus REQ plus RESEARCH plus CONTEXT)

GOAL Daily Zsh feels finished: preview reachability maps to the tracer call; history search plus PATH stability plus local take-effect map to no-code regression carry (human check plus unchanged guards); theme consistency maps to explicit zero-code preservation. REQ SHEL-04 plus EDIT-04 map to the tracer task (the gap requirements, listed in frontmatter); SHEL-02 plus SHEL-03 plus THEM-01 map to 03-01-PLAN.md (already executed, preserved via hunk-disjoint within setup.sh (preview branch vs function plus live call)) and are re-confirmed via carried truths, not re-implemented. RESEARCH DRY_RUN-safe bootstrap pattern plus preview-before-write idiom maps to the reuse-call variant the verifier named preferred. CONTEXT D-12 maps to the tracer fix; D-10 HOME-only maps to unchanged paths; D-13 zero-theme maps to the absence probe; D-01 split ownership and D-02 ladder map to untouched wiring re-confirmed by the human check. Deferred ideas (branch-wide history plus secret purge, keyd build path, backup plus CI) map to no task by design.

<output>
Create `.planning/phases/03-polished-shell-theme-local-overrides/03-02-SUMMARY.md` when done
</output>
