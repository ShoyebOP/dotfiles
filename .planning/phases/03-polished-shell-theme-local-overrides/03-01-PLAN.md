---
phase: 03-polished-shell-theme-local-overrides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [zsh/.zshrc, nvim/.config/nvim/init.lua, .gitignore, setup.sh, zsh/.zshrc.local.example, nvim/.config/nvim/lua/local.lua.example, README.md]
autonomous: true
requirements: [SHEL-02, SHEL-03, SHEL-04, THEM-01, EDIT-04]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "User presses Ctrl+R and gets fzf history search, or a visible warning telling them to install fzf — never a silent dead key (SHEL-02 per D-03)"
    - "User types and the async completion list auto-shows below the prompt with no keypress; Tab only enters menu-select; ghost autosuggestion text still renders (SHEL-02 per D-04/D-05)"
    - "User reloads the shell repeatedly and PATH has zero duplicates while z, zi, zoxide, completions, and the p10k prompt still work (SHEL-03 per D-09)"
    - "User drops personal tweaks in HOME ~/.zshrc.local and the deployed nvim local.lua and they take effect without dirtying git; a fresh clone works with both files absent (SHEL-04, EDIT-04 per D-10/D-11)"
    - "User runs setup.sh --dry-run and sees the local-file preview with zero writes; a live run creates empty HOME files without truncating existing ones (SHEL-04, EDIT-04 per D-12)"
    - "No per-app appearance file is modified and no theme token, installer theme function, or mismatch warning is added anywhere (THEM-01 closed as intended-drift per D-13)"
  artifacts:
    - path: "zsh/.zshrc"
      provides: "PATH dedup guard, repaired plugin block, version-branched fzf init, unconditional Ctrl+R bind with warning, HOME-only local tail guard, portable bun source"
      contains: "typeset -U path"
    - path: "nvim/.config/nvim/init.lua"
      provides: "Machine-local override load as the last startup step, silent when absent, warning when broken"
      contains: "pcall(require"
    - path: ".gitignore"
      provides: "Machine-local gitignore section covering local overrides and shell history"
      contains: "*.local"
    - path: "setup.sh"
      provides: "DRY_RUN-safe post-stow bootstrap creating empty HOME local files only when absent"
      contains: "ensure_local_files"
    - path: "zsh/.zshrc.local.example"
      provides: "Documented shell template showing PATH prepend, prompt tweak, and alias examples"
      contains: ".zshrc.local"
    - path: "nvim/.config/nvim/lua/local.lua.example"
      provides: "Documented editor template showing option tweak and keymap examples"
      contains: "local.lua"
    - path: "README.md"
      provides: "Machine-local overrides user docs with HOME-only paths and copy-from-template commands"
      contains: "Machine-local"
  key_links:
    - from: "zsh/.zshrc plugin load order (fzf-history-search own light, autocomplete last)"
      to: "bindkey Ctrl+R target widget"
      via: "The widget exists only if its plugin actually loads; the bare ice line without a light command loads nothing, so the unconditional bind needs the repaired load above it"
      pattern: "zi light joshskidmore/zsh-fzf-history-search"
    - from: "typeset -U path position near top"
      to: "All later PATH exports (top five, polaris append, bun, fpath)"
      via: "The unique attribute is forward-looking; anything mutated above the guard keeps dupes, so placement above the first export is what makes repeat sourcing converge"
      pattern: "typeset -U path"
    - from: "HOME local guard position (after tool inits, before p10k apply)"
      to: "User prompt and alias overrides rendering"
      via: "Sourcing before zoxide init gets clobbered by init output; sourcing after prompt apply never renders, so the tail slot is load-bearing"
      pattern: ".zshrc.local"
    - from: "setup.sh ensure_local_files HOME-derived paths"
      to: "Deployed ~/.zshrc.local and ~/.config/nvim/lua/local.lua"
      via: "Paths derive from HOME at runtime and are never written under the repo dir, which is what keeps the working tree clean on every machine"
      pattern: "ensure_local_files"
  prohibitions:
    - "Must NOT author a theme token, installer theme function, mismatch warning, or any per-app appearance normalization — theme spread is intended drift per D-13"
    - "Must NOT source a repo-side local file or commit a real local file or shell history into git — HOME-only sourcing with gitignored paths per D-10/D-12"
    - "Must NOT let any fzf probe or init failure abort the shell or disturb autocomplete widgets — degrade to warn-and-continue per D-02"
---

<objective>
**As a** daily Zsh user, **I want to** press Ctrl+R and get history search, reload without PATH dupes, and keep machine tweaks in gitignored HOME files, **so that** daily shell feels finished.

Purpose: Deliver the reshaped Phase 3 scope — autocomplete-first history wiring with a best-effort fzf ladder (D-01..D-05), top-of-file PATH dedup over every mutation site (D-06..D-09), HOME-only machine-local overrides bootstrapped by the installer (D-10..D-12) — with explicitly zero theme work (D-13). Single coarse plan with three tasks: a shell-spine tracer, an editor plus git-hygiene expansion, and an installer plus docs expansion.

Output: Edited zsh/.zshrc, nvim init.lua, .gitignore, setup.sh, README.md plus two new documented template files, committed atomically per the Phase 1 D-04 atomic-docs rule.

ROADMAP deltas recorded so the checker does not flag them as scope misses: (1) criterion 1 one-history-plugin policy is superseded by D-01 keep-BOTH with split ownership — Ctrl+R belongs to fzf-history-search, Tab belongs to autocomplete; (2) criterion 3 dual local files is superseded by D-10 HOME-only — only HOME ~/.zshrc.local is sourced, no repo-side file; (3) criterion 5 theme token plus installer warn is dropped by D-13 — THEM-01 closes as intended-drift with zero code. AGENTS.md is intentionally untouched: it is marker-generated and already carries the machine-local constraint, so the atomic-docs pair for this phase is README.md plus in-code rationale comments.

Scope-gate notes: API coverage detector returned false for this shell plus editor plus installer scope, so no coverage block applies. No ORM schema files exist in scope, so no schema-push handling applies. Assumption-delta scan detected true on lexical pluralization plus optional plus chosen signals from existing prose adjectives (second-machine wording and similar); decision no-change with rationale that the signals are adjectives in carried-forward prose with no competing architectural noun, so no invariant test is warranted. Specless probe fallback applies — SPEC is absent, so the eight unresolved edge probes are surfaced as flagged assumptions in their own section below, never silently dropped.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polished-shell-theme-local-overrides/03-CONTEXT.md
@.planning/phases/03-polished-shell-theme-local-overrides/03-RESEARCH.md
@.planning/phases/03-polished-shell-theme-local-overrides/03-PATTERNS.md
@zsh/.zshrc
@nvim/.config/nvim/init.lua
@.gitignore
@setup.sh
</context>

<tasks>
<!-- planner-discipline-allow: zsh/.zsh_history -->
<!-- planner-discipline-allow: zsh/.zshrc.local.example -->
<!-- planner-discipline-allow: nvim/.config/nvim/lua/local.lua.example -->

<task type="tracer">
  <name>Tracer: end-to-end polished shell spine in zshrc</name>
  <files>zsh/.zshrc</files>
  <read_first>zsh/.zshrc; .planning/phases/03-polished-shell-theme-local-overrides/03-CONTEXT.md; .planning/phases/03-polished-shell-theme-local-overrides/03-PATTERNS.md</read_first>
  <action>Per D-01 through D-10, build the complete shell spine inside zsh/.zshrc in one coherent edit, diagnosing before fixing. First diagnose live: run zle -l filtered for the fzf history widget name, print bindkey for Ctrl+I, and list the zinit plugins directory for a fzf-history entry to confirm whether the widget is missing because of the bare ice line with no light command. Then apply the fix: give joshskidmore/zsh-fzf-history-search its own zi light line following the working autosuggestions pattern, keep marlonrichert/zsh-autocomplete as the final plugin load with its existing Ctrl+I menu-select atload block byte-identical, and leave zsh-autosuggestions plus fast-syntax-highlighting lines untouched above so all three coexist per D-05; leave the commented zicompinit finalization block commented unless the diagnosis proves compinit never runs, in which case record the evidence in the commit message. Next add the fzf degrade ladder per D-02 immediately after the plugin block: probe command -v fzf, take the first whitespace field of fzf --version output, compare against 0.48 with sort -V so dotted versions order numerically, take the process-substitution integration branch on the new side and the /usr/share/fzf/key-bindings.zsh file-guarded branch on the legacy side, and fall through silently when fzf is absent; no probe failure may return nonzero at top level. Then add the unconditional bindkey of Ctrl+R to the fzf history widget per D-03 followed by a zle-list guard that prints a yellow WARN telling the user to install fzf when the widget is absent, and never rebind Ctrl+I anywhere in the fzf branch. Then insert typeset -U path with a why-comment on its own lines immediately above the first local-bins export per D-06, covering all five top exports plus the polaris/bin append plus bun and fpath additions per D-07 with keep-first semantics per D-08 and no other PATH line rewritten; as a portability rider rewrite the absolute home-pathed bun source at the file tail to the HOME-variable form with a non-empty-file guard. Finally insert the HOME-only local guard per D-10 with a why-comment immediately above the p10k source line near the tail, after every tool init and before prompt apply, quoting the HOME path and sourcing nothing under the repo; leave zsh/.zprofile untouched. Add a brief why-comment at every order-sensitive insertion per the atomic-docs rule.</action>
  <reversibility rating="reversible">Single-file shell-init edits revert with one checkout; no migration or contract change per D-06 and D-10 notes.</reversibility>
  <verify>
    <automated>zsh -n zsh/.zshrc && echo syntax-ok</automated>
    <automated>awk '/typeset -U path/{u=NR} /export PATH=/{if(!f){f=NR}} END{exit !(u && f && u<f)}' zsh/.zshrc && echo guard-on-top</automated>
    <automated>grep -q 'zi light joshskidmore/zsh-fzf-history-search' zsh/.zshrc && test "$(grep -n 'zi light marlonrichert/zsh-autocomplete' zsh/.zshrc | cut -d: -f1)" -gt "$(grep -n 'zi light joshskidmore/zsh-fzf-history-search' zsh/.zshrc | cut -d: -f1)" && echo order-ok</automated>
    <automated>grep -q "bindkey '^R' fzf-history-widget" zsh/.zshrc && grep -q 'sort -V' zsh/.zshrc && grep -q '\.zshrc\.local' zsh/.zshrc && echo wiring-ok</automated>
    <automated>test "$(printf '%s\n%s\n' '0.44.1' '0.48' | sort -V | head -n1)" = '0.44.1' && test "$(printf '%s\n%s\n' '0.49.0' '0.48' | sort -V | head -n1)" = '0.48' && echo probe-logic-ok</automated>
    <automated>rm -rf /tmp/zdot-tracer && mkdir -p /tmp/zdot-tracer && cp zsh/.zshrc /tmp/zdot-tracer/.zshrc && ZDOTDIR=/tmp/zdot-tracer zsh -i -c 'for i in 1 2 3; do source "$HOME/.zshrc" 2>/dev/null || source /tmp/zdot-tracer/.zshrc 2>/dev/null; done; dups=$(echo "$PATH" | tr ":" "\n" | sort | uniq -d | tr -d "[:space:]"); test -z "$dups" && echo path-deduped' 2>/dev/null | grep -q path-deduped && echo reload-ok</automated>
  </verify>
  <done>The single happy path works end-to-end and is committed: a fresh interactive shell loads without aborting, Ctrl+R is bound or warns, PATH survives triple sourcing with no dupes, and the HOME local file is sourced when present.</done>
  <acceptance_criteria>zsh -n exits 0; guard line number precedes first PATH export line number; fzf-history-search light line number precedes autocomplete light line number; Ctrl+R bind plus WARN plus sort -V plus local-guard strings all present; canned 0.44.1 selects legacy side and 0.49.0 selects modern side; hermetic triple-source leaves PATH with empty duplicate set.</acceptance_criteria>
</task>

<task type="auto">
  <name>Expansion: nvim local override plus gitignore plus documented templates</name>
  <files>nvim/.config/nvim/init.lua, .gitignore, zsh/.zshrc.local.example, nvim/.config/nvim/lua/local.lua.example</files>
  <read_first>nvim/.config/nvim/init.lua; nvim/.config/nvim/lua/lang/init.lua; .gitignore; zsh/.zshrc</read_first>
  <action>Per D-11 and D-12, deliver the editor half of machine-local overrides. Append a machine-local block at the very end of nvim/.config/nvim/init.lua after the vim.schedule colorscheme block, modeled on the lang loader pcall plus WARN idiom: protected require of the module named local, silent no-op when the error text reports the module is not found, vim.notify at WARN level otherwise, with a why-comment stating machine tweaks load last; never use ERROR level and never abort startup. Append a Phase 3 machine-local section to .gitignore with a header comment plus the star-dot-local pattern plus the deployed nvim lua local path plus the tracked shell-history path, keeping existing entries byte-identical. Create the shell template file showing a header that names the HOME destination as the only valid copy target and warns the real file is never committed, followed by commented examples for a PATH prepend, a prompt-element tweak, and an alias. Create the editor template file showing a header that names the deployed HOME config path as the only valid copy target and notes absence is a silent no-op, followed by commented examples for an option tweak and a keymap. Both templates are docs only and nothing sources or requires them. In-code rationale comments ship in the same commit per the atomic-docs rule.</action>
  <reversibility rating="reversible">Appended guard block plus gitignore lines plus two new docs-only files revert with one checkout; deployed local files live outside the repo.</reversibility>
  <verify>
    <automated>grep -q 'pcall(require, "local")' nvim/.config/nvim/init.lua && echo pcall-ok</automated>
    <automated>nvim --headless -c 'qa!' 2>&1 | tail -3; test "${PIPESTATUS[0]}" = '0' && echo nvim-clean-start</automated>
    <automated>test -f zsh/.zshrc.local.example && test -f nvim/.config/nvim/lua/local.lua.example && echo templates-exist</automated>
    <automated>git check-ignore -q 'zsh/.zshrc.local' && git check-ignore -q 'nvim/.config/nvim/lua/local.lua' && git check-ignore -q 'zsh/.zsh_history' && echo locals-ignored</automated>
    <automated>git check-ignore -q zsh/.zshrc.local.example; test "$?" -ne 0 && git check-ignore -q nvim/.config/nvim/lua/local.lua.example; test "$?" -ne 0 && echo templates-committable</automated>
  </verify>
  <done>Editor accepts machine-local tweaks that win without dirtying git, git ignores the three local paths while still accepting the two templates, and a clean headless start exits zero with the override absent.</done>
  <acceptance_criteria>nvim init tail contains the protected require of local; headless no-arg start exits 0; both template files exist; check-ignore succeeds for the HOME local name, the deployed lua local path, and the shell history path; check-ignore fails for both template paths.</acceptance_criteria>
</task>

<task type="auto">
  <name>Expansion: installer bootstrap plus README docs plus history untrack plus theme closure</name>
  <files>setup.sh, README.md</files>
  <read_first>setup.sh; README.md; .gitignore</read_first>
  <action>Per D-12 and D-13, finish the local-override story in the installer and docs. Add a function named ensure_local_files to setup.sh following the keyd-privileged preview idiom: first branch on DRY_RUN printing a Would-run preview naming both HOME destinations and returning success with zero writes, then create the HOME shell-local file only when absent with a file-existence test guarding touch, then create the deployed nvim lua local file only when absent after ensuring its parent directory, deriving that path from HOME at runtime and never from the repo dir, using local variables with quoted paths under the file-global strict mode; call it in the main live path after post_verify succeeds and before the chsh offer, leaving the DRY_RUN early-return branch and every other main step untouched. Add a Machine-local overrides subsection to README.md documenting the two HOME-only destinations, the copy-from-template commands for each template file, the absent-means-silent behavior, and the git-stays-clean note. Untrack the currently tracked shell-history file with a cached removal guarded by a tracked-check so it runs once, keeping the working-tree file itself untouched; attempt no history rewrite, no filter tooling, no force-push, with the full purge explicitly deferred out of this phase, and reproduce no secret values anywhere. Implement zero theme-code change: modify no per-app appearance file and add no central token, installer theme function, or mismatch warning. Record ratings: bootstrap and cached-untrack are reversible since the working files are preserved and re-trackable.</action>
  <reversibility rating="reversible">Bootstrap only adds absent files and the cached removal keeps the working-tree history file; both undo with one checkout plus one git add, and the full history rewrite stays out of scope.</reversibility>
  <verify>
    <automated>bash -n setup.sh && echo setup-syntax-ok</automated>
    <automated>grep -q 'ensure_local_files()' setup.sh && grep -q 'DRY RUN.*zshrc.local' setup.sh && grep -q '|| touch' setup.sh && grep -q 'post_verify.*ensure_local_files\|ensure_local_files' setup.sh && echo bootstrap-wired</automated>
    <automated>bash -c 'source ./setup.sh; DRY_RUN=true HOME=/tmp/fakehome-phase3 ensure_local_files' 2>&1 | grep -q 'DRY RUN' && test ! -e /tmp/fakehome-phase3/.zshrc.local && echo dryrun-clean</automated>
    <automated>bash setup.sh --mode server --shell zsh --dry-run < /dev/null 2>&1 | grep -q 'DRY RUN' && echo preview-ok</automated>
    <automated>grep -q 'Machine-local' README.md && grep -q 'zshrc.local.example' README.md && echo docs-ok</automated>
    <automated>! git ls-files | grep -q 'zsh/.zsh_history' && echo history-untracked</automated>
    <automated>git status --short | awk '{print $2}' | grep -Ev '^(zsh/.zshrc|nvim/.config/nvim/init.lua|.gitignore|setup.sh|README.md|zsh/.zshrc.local.example|nvim/.config/nvim/lua/local.lua.example)$' | grep -q .; test "$?" -ne 0 && echo change-set-clean</automated>
  </verify>
  <done>Installer bootstraps both empty HOME local files exactly once without touching existing content or writing under dry-run, README explains the HOME-only workflow, shell history is untracked with its file intact, and the change set contains only the seven expected paths with zero theme code.</done>
  <acceptance_criteria>bash -n exits 0; setup.sh contains the new function definition, its call in the live path, the DRY RUN preview branch, and the create-only-when-absent guards; sourced DRY_RUN run prints the preview marker and creates nothing under the stub HOME; server dry-run prints the preview marker; README contains the new section header and template filename; tracked-file listing no longer shows the history path; change-set listing beyond the seven expected paths is empty.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell-startup→HOME-local-file | New tail guard sources HOME-owned content into every interactive shell; content trust equals the zshrc itself |
| installer→HOME-filesystem | New bootstrap function writes two files under HOME during deployment |
| repo→git-index | New gitignore rules plus a cached untrack decide what machine content can leak into history |
| editor-startup→deployed-local-module | New tail pcall executes deployed Lua with full editor privileges on the machine account |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information disclosure | .gitignore machine-local section plus history untrack | high | mitigate | Ignore the star-dot-local pattern, the deployed lua local path, and the shell history path; untrack the history file while keeping the working copy; templates carry only commented examples so copying them leaks nothing; gate with check-ignore plus tracked-listing verification |
| T-03-02 | Elevation of privilege | zshrc HOME-local tail guard | low | accept | Sourced path is a fixed HOME-owned file equal in trust to the zshrc itself; template header documents that only the owner writes there; no repo-side or world-writable path is ever sourced |
| T-03-03 | Tampering | PATH precedence via local overrides | low | accept | Keep-first dedup preserves shipped precedence and the local file is owner-only, so hijack requires HOME write which already equals full account compromise; template shows append-style tweaks rather than blind prepends |
| T-03-04 | Elevation of privilege | nvim local module pcall | low | accept | Machine-owner trust boundary: deployed module runs as the editor owner; absent file is a silent no-op and a broken file warns without aborting startup |
| T-03-05 | Tampering | setup.sh ensure_local_files writes | medium | mitigate | Create-only-when-absent guards never truncate user content; DRY_RUN preview branch returns before any write; deployed lua path derives from HOME at runtime and never from the repo dir |
| T-03-SC | Tampering | package installs | low | accept | No new packages are installed by this phase; shell plugins arrive through the existing pinned Zinit path and fzf through the distro toolchain list, so no registry verification applies |
</threat_model>

<verification>
Automated: zsh syntax check, guard-position and load-order greps, canned-version probe assertions, hermetic triple-source PATH-dedup check, nvim headless clean start, check-ignore gates for locals versus templates, setup.sh syntax plus sourced-DRY_RUN no-write proof, server dry-run preview marker, README section greps, tracked-listing history gate, and the seven-path change-set gate. Flagged assumptions below carry the eight unresolved specless probes as explicit review items. Human check at end of phase: open one interactive shell, confirm completions auto-show while typing, press Ctrl+R for history search, and confirm the prompt renders.
</verification>

<success_criteria>
All six must_haves truths hold with all three prohibitions unbroken; every D-01 through D-13 decision is implemented and cited; the change set holds exactly the seven listed paths; THEM-01 is closed with zero theme code; plan commits with docs atomically.
</success_criteria>

## Artifacts this phase produces

New and changed symbols the review-convergence grounding pass must treat as intended additions, not drift: the `typeset -U path` guard line near the top of zsh/.zshrc; the dedicated `zi light` line for the fzf history plugin with autocomplete kept as the final load; the fzf version probe block comparing against 0.48 with the modern versus legacy branches; the unconditional Ctrl+R bind plus missing-widget WARN block; the HOME local-file tail guard block between tool inits and prompt apply; the portable HOME-variable bun source line; the protected-require local block at the end of the nvim init file; the `ensure_local_files` installer function plus its call in the main live path; the `.gitignore` machine-local section entries; the two documented template files and their copy-to-HOME destination paths; the README machine-local overrides subsection. The cached untrack of the shell history path is an intended index change with the working file preserved.

## Flagged assumptions (specless probe fallback — unresolved, kept visible)

SPEC is absent, so no edge is formally resolved; each item below is carried as reviewable behavior rather than dropped. SHEL-02 unclassified history-search probe: the ladder tolerates total fzf absence with a warning, and review should confirm the warning text names the install action. SHEL-03 adjacency probe for exactly-equal entries: keep-first dedup drops later dupes and the triple-source smoke proves convergence empirically. SHEL-03 empty probe for empty or single-element PATH: the guard is a no-op on degenerate input and review should confirm no error text appears. SHEL-03 ordering probe for equal elements: order is never reordered, only later dupes dropped, proven by the smoke keeping tool precedence. SHEL-04 idempotency probe for running twice: create-only-when-absent guards plus guarded tail sourcing make reruns safe no-ops. SHEL-04 concurrency probe for interruption or parallel runs: touch-based creation is atomic enough for dotfile bootstrap and a half-run simply resumes on the next run. EDIT-04 unclassified local-module probe: absent file stays silent while a broken file warns, proven by the clean headless start. THEM-01 unclassified theme probe: closed as intended-drift with zero work, so the standing assumption is that no theme code is authored and the change-set gate enforces it.

## Source coverage audit (GOAL plus REQ plus RESEARCH plus CONTEXT)

GOAL Daily Zsh feels finished: history search just works maps to the tracer shell task; PATH is stable maps to the tracer PATH guard plus reload smoke; machine-local tweaks stay gitignored maps to the tracer tail guard plus the editor gitignore task plus the installer bootstrap task; theme is consistent is reshaped to intended-drift closure in the installer task. REQ SHEL-02 maps to the tracer task; SHEL-03 maps to the tracer task; SHEL-04 maps to all three tasks (guard, gitignore plus templates, bootstrap plus docs); EDIT-04 maps to the editor task plus the installer task; THEM-01 maps to the installer task as an explicit zero-code closure. RESEARCH plugin-block finding, version-branch pattern, tied PATH semantics, conditional-source idiom, pcall pattern, DRY_RUN-safe bootstrap, gitignore plus template guidance, and the bun portability rider all map to the tasks named above, with the diagnose-before-fixing rule for the compinit question inside the tracer and the veto-at-review history untrack inside the installer task. CONTEXT D-01 through D-05 map to the tracer plugin and ladder work; D-06 through D-09 map to the tracer PATH work; D-10 maps to the tracer tail guard; D-11 maps to the editor task; D-12 maps to the editor plus installer tasks; D-13 maps to the installer task closure. Deferred ideas (branch-wide history plus secret purge, automated keyd build path, backup snapshot plus CI matrix) map to no task by design and are restated in the installer action as explicit exclusions.

<output>
Create `.planning/phases/03-polished-shell-theme-local-overrides/03-01-SUMMARY.md` when done
</output>
