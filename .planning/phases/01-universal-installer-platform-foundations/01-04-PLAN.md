---
phase: 01-universal-installer-platform-foundations
plan: 04
type: execute
wave: 1
gap_closure: true
gap_ids: [G-01-13, G-01-14, G-01-15, G-01-16]
files_modified: [setup.sh]
autonomous: true
requirements: [INST-01, INST-04, DEPS-02, SHEL-01]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "User sees mode → shell → package checklist BEFORE any dependency installation (before apt update/install or pacman -Sy)"
    - "Checklist shows all 7 packages toggleable with clear ON/OFF state on TTY; non-TTY shows guidance not truncated preset summary"
    - "Installer runs apt update (debian) or pacman -Sy (arch) before installing missing deps and previews it in --dry-run"
    - "After selecting zsh, installer offers to change login shell via chsh with explicit confirmation"
  artifacts:
    - path: "setup.sh"
      provides: "Checklist-before-install ordering, full 7-package toggle, repo refresh, chsh offer"
      contains: "prompt_checklist"
  key_links:
    - from: "setup.sh:prompt_checklist"
      to: "setup.sh:install_deps"
      via: "checklist selection precedes any package-manager write"
      pattern: "checklist-before-install"
    - from: "setup.sh:install_deps"
      to: "setup.sh:reverify_deps"
      via: "repo refresh then install then re-verify"
      pattern: "repo-refresh-install"
    - from: "setup.sh:post_verify"
      to: "setup.sh:maybe_change_shell"
      via: "post-verify success triggers explicit chsh offer"
      pattern: "chsh-offer"
  prohibitions:
    - "No writes before checklist (including apt update/pacman -Sy) except detection"
    - "No auto chsh without explicit user confirmation"
    - "No bashisms in repo-refresh guard before checklist"
---

# Plan 01-04: Checklist ordering, full toggle, repo refresh, shell offer (G-01-13..16)

> **As a** user who runs `bash setup.sh`, **I want to** confirm my exact package set before anything is installed, see all 7 choices, have repos refreshed automatically, and get my shell actually switched **so that** one interactive run finishes correctly.

<objective>
Fix 4 UAT gaps where installer writes before checklist, shows truncated checklist, omits repo refresh, and never changes login shell. Reorder main() so mode/shell/checklist precede any write, ensure all 7 packages render toggleable on TTY, insert apt/pacman repo refresh before install with dry-run preview, and offer chsh after stow with confirmation.

Purpose: Make Phase 1 match ROADMAP criterion 5 BEFORE any write and close basic expectations for repo hygiene and shell activation that Phase 2 was deferring.
Output: Fresh clone checklist before deps; all 7 visible; `apt update`/`pacman -Sy` runs when needed; `chsh` offered with confirmation.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-universal-installer-platform-foundations/01-CONTEXT.md
@.planning/phases/01-universal-installer-platform-foundations/01-UAT.md
@.planning/debug/checklist-order-guard.md
@.planning/debug/package-toggle-count.md
@.planning/debug/repo-update-missing.md
@.planning/debug/shell-not-changed.md
@setup.sh
</context>

<tasks>

<task type="auto">
  <name>Reorder checklist before any write and preview together</name>
  <files>setup.sh</files>
  <read_first>setup.sh:663-850 main() ordering; setup.sh:577-641 checklist_read TTY vs non-TTY; setup.sh:216-250 install_deps/reverify</read_first>
  <action>Move `prompt_checklist` to immediately after `detect_family` and mode/shell resolution and BEFORE `get_deps`/`verify_deps`/`install_deps`. Current order is verify/install (lines 688-730) then checklist (738); change to checklist (new ~690) then verify/install/reverify then stow. Keep `FAMILY/MODE` detection before checklist for preset computation, but defer any package-manager writes until after selection. Update DRY_RUN to preview BOTH deps install and stow together after checklist: after selection, print `=== Preview ===` with deps missing partitioned and `preview_selection` stow simulation in one block, returning before any mutation. Ensure `get_deps` still uses `FAMILY/MODE` (not yet filtering by SELECTED_PACKAGES) — filtering can remain as future enhancement, but ordering alone fixes before-any-write guarantee. Preserve outside-root guard and help-wins-anywhere before checklist.</action>
  <verify>
    <automated>bash -n setup.sh</automated>
    <automated>bash setup.sh --mode server --shell zsh --dry-run 2>&1 | awk '/Package Selection/{found=1} /Verifying dependencies/{if(found==0) print "BAD order: verify before checklist"; else print "OK order"}' | grep -q "OK order"</automated>
    <automated>bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "Package Selection"</automated>
    <automated>printf '2\n1\n\n' | HOME=/tmp/fake-order PATH=/tmp/stbbin:/tmp/fakebin:/tmp/noladderbin bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "DRY RUN"</automated>
  </verify>
  <done>Checklist prompts before any deps verification/install output; dry-run shows single preview after selection; bash -n clean.</done>
</task>

<task type="auto">
  <name>Fix full 7-package toggle rendering and Termux detection</name>
  <files>setup.sh</files>
  <read_first>setup.sh:18 ALL_PACKAGES; setup.sh:136-172 detect_family; setup.sh:457-500 whiptail/dialog; setup.sh:577-641 checklist_read; setup.sh:642-661 prompt_checklist ladder</read_first>
  <action>Ensure interactive checklist always renders all 7 packages with ON/OFF markers regardless of preset: fix `checklist_read` TTY branch to loop ALL_PACKAGES (already does) but ensure non-TTY fast path does NOT silently return preset summary when stdin is a TTY — add explicit note `echo "7 packages offered" >&2` after selection and ensure preset logic does not drop items. For whiptail/dialog, change window size from fixed `20 78 10` to adaptive: compute `rows=$(stty size 2>/dev/null | cut -d' ' -f1)` and if rows <20, fall back to `checklist_read` instead of clipping; otherwise use `min(7, rows-8)` list height so all 7 fit visible. Tighten Termux detection: change Tier 1 from `command -v pkg` alone to `(command -v pkg && [[ -n "${TERMUX_VERSION-}" || "${PREFIX-}" == *"com.termux"* ]])` or require ID=termux, so Debian with stray `pkg` shim does not mis-detect. Add post-checklist line `echo "Final selection: ${SELECTED_PACKAGES[*]} (offered 7, selected ${#SELECTED_PACKAGES[@]})" >&2` so user sees count. Preserve strip_termux_disabled warning.</action>
  <verify>
    <automated>bash -n setup.sh</automated>
    <automated>printf '2\n1\n\n' | HOME=/tmp/fake-toggle PATH=/tmp/noladderbin bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "Final selection"</automated>
    <automated>grep -q "offered 7" setup.sh</automated>
    <automated>HOME=/tmp/fake-toggle2 PATH=/tmp/noladderbin bash setup.sh --mode local --shell zsh --dry-run 2>&1 | grep -q "7"</automated>
    <automated>command -v pkg >/dev/null 2>&1 && echo "pkg present" || echo "pkg absent ok"; bash -c 'source ./setup.sh; OS_RELEASE_FILE=/tmp/fix-pop detect_family' | grep -q debian && echo "not mis-detected as termux on debian"</automated>
  </verify>
  <done>All 7 packages visible with ON/OFF on TTY; small terminals fall back gracefully; Termux not mis-detected on Debian/Arch; count shown.</done>
</task>

<task type="auto">
  <name>Add repo refresh before install and explicit chsh offer</name>
  <files>setup.sh</files>
  <read_first>setup.sh:216-244 install_deps; setup.sh:663-850 post_verify tail; zsh/.zshrc Zinit region</read_first>
  <action>In `install_deps()` before the install array/loop, insert repo-refresh once when missing>0: for arch `echo "Refreshing pacman DB..."; if [[ "$DRY_RUN" == true ]]; then echo "[DRY RUN] Would run: sudo pacman -Sy"; else sudo pacman -Sy || echo "Warning: pacman -Sy failed, continuing" >&2; fi`; for debian `echo "Refreshing apt lists..."; if DRY_RUN echo "[DRY RUN] Would run: sudo apt update" else sudo apt update || warning`; for termux `pkg update -y` similarly. Run refresh once per invocation before install, not per-package, and preview it in DRY_RUN alongside install preview. After `post_verify` success, add `maybe_change_shell()` function that checks `if [[ "$SHELL_CHOICE" == "zsh" ]] && command -v zsh && [[ "${SHELL-}" != *zsh ]]`; prints `Selected shell: zsh is $(which zsh), current \$SHELL is $SHELL. Change login shell?`; if `[[ "$YES" == true ]]` skip prompt with notice; elif `! -t 0` skip with `Skipping shell change (non-interactive, run chsh manually)`; else if `command -v gum` use `gum confirm "Change login shell to $(which zsh)?"` else `read -p "Type 'yes' to change shell: " reply` and on `yes` run `chsh -s $(which zsh)` (no sudo) and warn `Log out/in required` else `Skipping shell change`. Handle nushell similarly if chosen. Never auto-changes without explicit yes.</action>
  <verify>
    <automated>bash -n setup.sh</automated>
    <automated>grep -q "apt update" setup.sh && grep -q "pacman -Sy" setup.sh && echo "refresh present"</automated>
    <automated>grep -q "chsh" setup.sh && echo "chsh present"</automated>
    <automated>HOME=/tmp/fake-refresh PATH=/tmp/noladderbin bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "Would run: sudo apt update"</automated>
    <automated>bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "Refresh"</automated>
    <automated>grep -q "maybe_change_shell" setup.sh && grep -q "gum confirm" setup.sh</automated>
  </verify>
  <done>Repo refresh runs once before install with dry-run preview; chsh offered with explicit confirmation and respects --yes/non-TTY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User checklist → install | User selection must precede any write |
| Package manager → network | apt/pacman refresh contacts remote mirrors |
| chsh → /etc/passwd | Login shell change is privileged user action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | checklist ordering | high | mitigate | Reorder so install never precedes selection; dry-run previews together |
| T-04-02 | Information Disclosure | Termux mis-detection | low | mitigate | Tighten pkg probe to require TERMUX_VERSION/PREFIX + pkg, not pkg alone |
| T-04-03 | Denial of Service | apt update failure | low | mitigate | Warn and continue, not abort, if refresh fails |
| T-04-04 | Elevation of Privilege | chsh | medium | mitigate | Explicit confirmation required, never auto, message requires re-login |

</threat_model>

<verification>
- Dry-run order shows Package Selection before Verifying dependencies and before Would run: sudo apt update
- TTY checklist shows 7 entries; non-TTY shows offered 7 count
- setup.sh contains apt update, pacman -Sy, pkg update, and chsh with gum confirm fallback
- bash -n clean, dry-run still previews both deps and stow
</verification>

<success_criteria>
- Gap G-01-13: checklist before any write holds
- Gap G-01-14: all 7 visible/toggleable
- Gap G-01-15: repo refresh before install with preview
- Gap G-01-16: chsh offered with confirmation
</success_criteria>

## Artifacts this plan produces

**Edited files:** `setup.sh` (reorder, refresh, chsh)

**Key behaviors:** checklist-first ordering, adaptive whiptail, Termux guard, repo refresh, shell offer

<output>
Create `.planning/phases/01-universal-installer-platform-foundations/01-04-SUMMARY.md` when done
</output>
