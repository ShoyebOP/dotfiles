---
phase: 01-universal-installer-platform-foundations
verified: 2026-09-11T22:35:00Z
status: passed
score: 20/20 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 19/19
  gaps_closed:
    - "G-01-14b: checklist that lets them toggle each app (dependencies and stow packages) individually — two-step 7 stow + 13 toolchain with filtered verify/install/preview"
  gaps_remaining: []
  regressions: []
gaps: []
---

# Phase 01: Universal Installer + Platform Foundations Verification Report

**Phase Goal:** A fresh clone can run `bash setup.sh` on Arch, Debian-family, or Termux and get a correct deployment with no manual `stow` or preinstalled Zsh
**Verified:** 2026-09-11T22:35:00Z
**Status:** passed
**Re-verification:** Yes — after 01-05 gap closure G-01-14b (two-step checklist 7+13 with filtered deps)
**Previous verification:** 2026-09-11T02:20:00Z — 19/19 passed (13 original + 6 gap-closure G-01-12..15), UAT diagnosed G-01-14b as outstanding
**This verification:** 20/20 must-haves verified (19 previous still passing + 1 new truth from 01-05), 5/5 ROADMAP success criteria passing, 8/8 Phase 1 REQUIREMENTS traceable and satisfied, G-01-14b verified closed

## Goal Achievement

Phase goal is **ACHIEVED**. `setup.sh` (1162 lines, `#!/usr/bin/env bash`, POSIX guard at line 2, `set -Eeuo pipefail; shopt -s inherit_errexit`, `bash -n` clean, executable) detects `arch|debian|termux` via Termux-env-first 4-tier (`TERMUX_VERSION/PREFIX` env → `command -v pacman/apt` → `ID_LIKE` token split → `ID` → manager fallback, no sourcing), resolves deps via `verify → install → re-verify` with repo refresh (`pacman -Sy`/`apt update`/`pkg update -y` plus DRY preview), presents `mode → shell → package checklist (7) → toolchain checklist (13)` **before any package-manager write**, previews every write with `=== Preview ===` containing `[DRY RUN] Would run: sudo apt update` + `Would run: ${install_cmd[*]} ${missing[*]}` + `Toolchain preview: N to install among M selected` + `stow --no --verbose` per selected package, aborts outside clone root with `run-from-clone`, quarantines collisions byte-identical to `.stow-conflicts/<ts>/MANIFEST`, proves deployment via folding-aware `readlink -f` `post_verify`, and filters toolchain verification/install/preview to `SELECTED_DEPS` so deselected apps are never probed nor installed. Live sandbox deploys succeed idempotently; `--dry-run` leaves zero writes and shows both selections before `Verifying dependencies`; `sh setup.sh` fails fast with guidance; `TERMUX_VERSION` branch stays `sudo`-free. No manual `stow` and no preinstalled Zsh required.

### Gap-Closure Verification (G-01-14b — 01-05)

| Gap | Truth (from PLAN must_haves) | Status | Live Evidence |
|-----|-------------------------------|--------|---------------|
| G-01-14b | User sees checklist that lets them toggle each app (dependencies and stow packages) individually | ✓ VERIFIED | `setup.sh:19` is `ALL_PACKAGES=(nvim zsh nushell alacritty starship wofi keyd)` 7, `setup.sh:23` is `ALL_TOOLCHAIN=(stow neovim starship git zoxide uv ripgrep nodejs npm make gcc fzf zsh)` 13. `grep -q ALL_TOOLCHAIN setup.sh` pass, `grep -q SELECTED_DEPS setup.sh` pass, `grep -q prompt_toolchain_checklist setup.sh` pass. `bash -c 'source ./setup.sh; echo ${#ALL_PACKAGES[@]}' → 7`, `echo ${#ALL_TOOLCHAIN[@]} → 13`. `bash -n setup.sh` clean. |
| G-01-14b ordering | Two-step checklist (7 stow + 13 toolchain) with filtered verify/install/preview — both steps complete before any package-manager write | ✓ VERIFIED | `setup.sh:main()` order: `parse_args` → repo-root guard → TTY-gated `prompt_mode/prompt_shell` → `detect_family` → `prompt_checklist` (7) → `prompt_toolchain_checklist` (13) → `get_deps` (filtered) → `verify_deps` → `Preview` → `install_deps` → `reverify` → `quarantine_scan` → `run_stow` → `post_verify`. Live: `bash setup.sh --mode server --shell zsh --dry-run 2>&1 | awk '/=== Package Selection ===/{p=NR} /=== Toolchain Selection ===/{t=NR} /Verifying dependencies/{v=NR} /=== Preview ===/{pr=NR} END{print p,t,v,pr}'` → `6 15 22 40` and `p < t < v < pr` → `order ok`. `grep -n "prompt_checklist\|prompt_toolchain_checklist\|get_deps" setup.sh` shows checklist indices before `get_deps`/`verify_deps`/`install_deps`. |
| G-01-14b dry-run unification | `--dry-run` shows both selections before any write, unified Preview with stow + deps | ✓ VERIFIED | `bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "=== Toolchain Selection ==="` pass, `grep -q "=== Preview ==="` pass, `grep -q "Would run: stow --dir="` pass, `grep -q "Toolchain preview:"` pass, `grep -q "Would run: sudo apt update"` pass (refresh preview). Order `Package Selection` 5 < `Toolchain Selection` 15 < `Verifying dependencies` 22 < `Preview` 40 verified via `awk`. `HOME=/tmp/fakehome-zero bash setup.sh --mode server --shell zsh --dry-run` leaves `ls -A` empty and prints `DRY RUN complete — no writes performed.` Non-TTY `HOME=/tmp/fake-nontty PATH=/tmp/noladderbin bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "13 toolchain offered"` pass and `grep -E "Final package selection|Final toolchain selection" | wc -l | grep -q 2` → `2` (both selections printed). |
| G-01-14b filtering | Deselected toolchain deps are not verified, not listed as missing, not previewed in `Would run`, not installed | ✓ VERIFIED | `setup.sh:225` `filter_deps_by_selection()` filters only `ALL_TOOLCHAIN` names, gui extras bypass. `setup.sh:202-220` `get_deps()` also filters when `SELECTED_DEPS` non-empty (double-filter idempotent). `setup.sh:1076` `filtered_deps` via `filter_deps_by_selection` drives `verify_deps "${filtered_deps[@]}"`, `missing` derived from filtered, `install_deps "$FAMILY" "${missing[@]}"` only filtered, `reverify_deps` same filter, stow upgrade only queued when `stow` in `SELECTED_DEPS`. Live: `bash -c 'source ./setup.sh; SELECTED_DEPS=(starship git); FAMILY=debian; MODE=server; mapfile -t _d < <(get_deps "$FAMILY" "$MODE"); echo ${_d[*]} | grep -qv neovim && echo ok'` → `ok` (neovim filtered). Isolated `PATH=/tmp/fakebin-nonvim:/bin` hide `nvim`: `SELECTED_DEPS=(stow starship ... without neovim)` → `get_deps` no `neovim`, `verify_deps` missing does not contain `neovim`; `SELECTED_DEPS` with `neovim` → `verify_deps` prints `- neovim (missing)` and `Missing dependencies: neovim`. `SELECTED_DEPS=(starship git)` → `get_deps debian local` still contains `alacritty` (gui bypass) proving gui not filtered. Dry-run `Toolchain preview: N to install among M selected` reflects filtered count. |
| G-01-14b presets & guard | `--yes` and non-TTY use presets for both steps, `stow` required-for-deployment guard prevents broken deploy | ✓ VERIFIED | `setup.sh:1010 prompt_toolchain_checklist` checks `if [[ "$YES" == true ]]` before TTY: prints `Selected toolchain (--yes presets): ...` + `13 toolchain offered` and returns. `toolchain_read` non-TTY `[[ ! -t 0 ]]` prints `Selected toolchain (non-interactive presets): ...` + `13 toolchain offered`. Live: `bash setup.sh --mode server --shell zsh --yes --dry-run 2>&1 | grep -q "13 toolchain offered"` pass, `grep -q "Selected toolchain (--yes presets)"` pass, `HOME=/tmp/fake-nontty ... --dry-run | grep -q "13 toolchain offered"` pass. `setup.sh:779 _toolchain_ensure_stow` re-enables `stow` if deselected while `! command -v stow`: live `PATH=/tmp/empty_bin:/usr/bin:/bin _toolchain_ensure_stow` with `SELECTED_DEPS=(neovim)` → `Warning: 'stow' is required` and `SELECTED_DEPS` becomes `stow neovim` preserving order; if `stow` already installed, deselection allowed without warning. |

**Gap closure score:** 1/1 new plan truth verified plus 4/4 aspects (ordering, preview unification, filtering, presets/guard) proven. G-01-14b is **CLOSED**.

### Observable Truths (20 total — 19 previous + 1 new, no regression)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User runs `bash setup.sh --help` and sees usage, exits 0, changes nothing (no unbound-variable crash) | ✓ VERIFIED | `bash setup.sh --help` exits 0, prints `Usage: setup.sh [OPTIONS]` with `--mode/--shell/--dry-run/--yes/--uninstall` and EXAMPLES, SHA stable. `bash setup.sh -h` same. `bash -n setup.sh` clean. No `unbound variable` in stderr. `bash setup.sh --mode server --dry-run --help` exits 0 (help-wins-anywhere pre-scan). |
| 2 | User on Manjaro, EndeavourOS, Garuda, Mint, Pop!_OS, or Termux resolves to arch/debian/termux with no hard error | ✓ VERIFIED | 4-tier `detect_family`: `TERMUX_VERSION/PREFIX` env-only → `command -v pacman/apt` → `ID_LIKE` tokens via `grep -E '^ID='` parse (no sourcing) → `ID` (including literal `termux`) → manager fallback. Fixtures: `ID=manjaro,ID_LIKE=arch→arch`, `ID=pop,ID_LIKE="ubuntu debian"→debian`, `ID=linuxmint→debian`, `ID=garuda→arch`, `ID=endeavouros→arch`, `ID=cachyos→arch`, `ID=termux→termux`, `TERMUX_VERSION=1,PREFIX=com.termux→termux`, `PREFIX=com.termux→termux` all pass via `OS_RELEASE_FILE` seam. Debian with fake `pkg` shim (`/tmp/shim-pkg-test/pkg`) still → `debian` (Termux guard tightened to env-only, no bare `command -v pkg`). |
| 3 | User with missing deps gets verify → install → re-verify: partitioned core/gui missing list, manager install, abort naming residue on failure | ✓ VERIFIED | `verify_deps` partitions `core_missing` vs `gui_missing` via `gui_list` per family, prints `Missing CORE/GUI deps:`. `install_deps` now refreshes first (`pacman -Sy`/`apt update`/`pkg update -y` with DRY preview and warning-continue), then uses `sudo pacman -S --needed`, `sudo apt install -y`, `pkg install -y` (termux per-package loop with `pkg search` hint). `reverify_deps` re-collects via `get_deps` filtered by `SELECTED_DEPS` and aborts `Still missing: ...` with exact retry `sudo pacman ...`/`sudo apt ...`/`pkg ...`. Isolated hide-`nvim` test proves residue abort: `SELECTED_DEPS` with `neovim` + `PATH=/tmp/fakebin-nonvim:/bin` → `- neovim (missing)` + `Missing dependencies: neovim`. |
| 4 | User passes --dry-run and sees every pending install as preview text with zero filesystem writes | ✓ VERIFIED | `bash setup.sh --mode server --shell zsh --dry-run` prints `=== DRY RUN MODE`, `Refreshing apt lists...`, `[DRY RUN] Would run: sudo apt update`, `Ready to install: stow`, `[DRY RUN] Would run: sudo apt install -y stow` (upgrade path when `stow 2.3.1 < 2.4.1`), `DRY RUN: Skipped re-verify`, `=== Preview ===` + `Toolchain preview: 1 to install among 13 selected` + `=== DRY RUN: Preview of selected packages ===` with `stow --no --verbose` per `SELECTED_PACKAGES`. Empty `HOME=/tmp/fakehome-zero` dry-run leaves `ls -A` empty; SHA unchanged. |
| 5 | User re-runs the installer and the dep lock is a safe no-op; stow below 2.4.1 is auto-upgraded via the platform manager | ✓ VERIFIED | Host `stow 2.3.1` triggers `Stow 2.3.1 < 2.4.1 — will upgrade via debian manager.` and queues `stow` into missing only when `stow` in `SELECTED_DEPS`; with mock `stow 2.4.1` shim (`/tmp/stbbin/stow` returning 2.4.1) second run reports `All dependencies are satisfied.` + `No install needed — second run is a safe no-op` + `Post-verify passed`; `sort -V` via `printf '%s\n' "$ver" "2.4.1" | sort -V | head -n1`. `stow --restow` idempotent verified by double sandbox deploy (70 links both passes) with mock 2.4.1. |
| 6 | User is prompted mode → shell → package checklist before any write happens (checklist before deps refresh/install) | ✓ VERIFIED — still holds after 01-05 insert | `main` order after 01-05: `parse_args` → repo-root guard → TTY-gated `prompt_mode/prompt_shell` → `detect_family` → `prompt_checklist` (7) → `prompt_toolchain_checklist` (13) → `get_deps` (filtered) → `verify_deps` → `Preview` → `install_deps` → `reverify` → `quarantine_scan` → `run_stow` → `post_verify`. No package-manager write (`apt update/pacman -Sy/pkg update/install`) before either checklist. Verified via `grep -n` order and live `awk` `Package Selection` 6 < `Toolchain Selection` 15 < `Verifying` 22 < `Preview` 40. Piped `printf '2\n1\n\n' | HOME=sandbox PATH=shim bash setup.sh --mode server --shell zsh` completes prompts then stow; non-TTY incomplete aborts before any write. |
| 7 | User can toggle all 7 packages; server pre-unchecks GUI but re-check works; shell choice pre-checks only the chosen shell; Termux disabled rows stay unselectable | ✓ VERIFIED | `ALL_PACKAGES=(nvim zsh nushell alacritty starship wofi keyd)` 7; `GUI_STOW_PACKAGES=(alacritty wofi keyd)`; server `MODE==server` sets those OFF; `SHELL_CHOICE==zsh` sets `zsh ON/nushell OFF`; Termux `TERMUX_DISABLED_PACKAGES=(alacritty wofi keyd)` forced OFF + rendered as `pkg (not available on Termux)` + `strip_termux_disabled` drops with warning. All backends loop 7 with ON/OFF markers `[x]/[ ]`; `checklist_read` toggle logic validated by Termux dry-run `Selected packages (non-interactive presets): nvim zsh starship` (no alacritty/wofi/keyd) and by `Final selection: ... (offered 7, selected N)` showing 7 offered even on small terminals (adaptive whiptail/dialog `stty size` fallback). |
| 8 | User passes --dry-run and sees the exact post-checklist selection previewed with zero writes | ✓ VERIFIED | `preview_selection` prints `Selection: ${SELECTED_PACKAGES[*]}` then per-pkg `Would run: stow --dir="$SCRIPT_DIR" --target="$HOME" --restow $pkg` and `stow --dir=... --no --verbose` (or fallback). Now combined with toolchain deps preview in unified `=== Preview ===` block after both checklists; verified on empty HOME dry-run with zero writes; non-interactive presets `nvim zsh starship` correctly previewed with `Final selection: ... (offered 7, selected 3)` and `7 packages offered`. |
| 9 | User with existing non-symlink targets gets them quarantined to a timestamped gitignored dir with a manifest and restore hint — never deleted, never force-adopted | ✓ VERIFIED | `quarantine_scan` uses `mv` only to `$SCRIPT_DIR/.stow-conflicts/<ts>/` preserving `rel` with `mkdir -p $(dirname)`, writes `MANIFEST` with `original -> quarantined` lines + header `Restore: mv <quarantined-path> <original-path>`; fixture `precious` at `.config/starship.toml` survives byte-identical `cmp -s` under quarantine, `grep -F ".config/starship.toml" .stow-conflicts/*/MANIFEST` hits, log prints `Quarantine complete` + `Restore with: mv`; `grep -q -- '--adopt' setup.sh` is 0, `grep -n "rm.*HOME"` shows 0 harmful. |
| 10 | User gets strict post-verify over the linked set (folding-aware) or an abort naming each bad link and its expected target | ✓ VERIFIED | `post_verify` iterates `find "$pkg_dir" -type f` per `SELECTED_PACKAGES`, calls `assert_linked` which checks `test -e $abs` then `readlink -f $abs` prefix `"$SCRIPT_DIR/$pkg/"*`; covers folded `~/.config → repo/.config` and unfolded files; on mismatch prints `MISMATCH/MISSING: $rel -> $got (expected under ...)`. Sandbox deploy verified `70 links: ok: ... -> /home/shoyeb/dotfiles/...` and `Post-verify passed: all 70 links verified`; standalone `SELECTED_PACKAGES=(nvim zsh starship) post_verify` passes. |
| 11 | User finds no legacy setup scripts and no dangling doc references; teardowns still present for Phase 2 removal work | ✓ VERIFIED | `test ! -e setup.nu && test ! -e setup.zsh` passes, `test -e teardown.nu && test -e teardown.zsh` passes; `grep -Eq 'setup\.(nu|zsh)' README.md` hits 0, `grep -Eq 'setup\.(nu|zsh)' nvim/.config/nvim/README.md` hits 0; `grep -q 'bash setup.sh' README.md` hits; README shows `Zsh (Default) | Nushell (Backup)` table, `bash setup.sh --mode local` examples, `stow --dir=. --target="$HOME" --restow nvim zsh starship` one-liners. |
| 12 | User invoking outside the clone root gets an abort with a run-from-clone message before any write | ✓ VERIFIED | Guard: `[[ ! -f "$SCRIPT_DIR/setup.sh" ]]` and `[[ ! -f ./setup.sh ]]` → `Error: setup.sh must be run from the dotfiles repo root.` + `Current directory: $PWD (./setup.sh not found) — run-from-clone required.` before any prompt/write; `(cd /tmp/notrepo && bash /home/shoyeb/dotfiles/setup.sh --mode server --shell zsh --dry-run)` exits 1 and matches `run-from-clone|repo root`. |
| 13 | User on Termux gets pkg installs with no sudo and keyd/hyprland/wofi never selectable | ✓ VERIFIED | Termux `install_cmd=(pkg install -y)` (no `sudo`); `grep -A2 'termux) install_cmd'` confirms; `get_deps termux server` returns only `stow neovim ... zsh` no `keyd/hyprland/wofi/waybar/slurp` in `get_deps` gui; `gui_list` for termux is `()`; `install_deps` termux branch loops per-pkg with `pkg install -y $pkg` and refreshes via `pkg update -y` (also DRY-previewed), no `sudo` prefix anywhere on that path; checklist Termux path renders disabled OFF + `strip_termux_disabled` warning; dry-run on `TERMUX_VERSION=1` correctly shows `Selected packages (non-interactive presets): nvim zsh starship`, and Debian with fake `pkg` shim still → `debian`. |
| 14 | User runs sh setup.sh and gets friendly 'must be run with Bash' message instead of 'Illegal option -o pipefail' | ✓ VERIFIED | `setup.sh:2` is `if [ -z "${BASH_VERSION-}" ]; then echo "Error: This installer must be run with Bash." >&2; echo "Use: bash setup.sh [OPTIONS]  (not sh setup.sh)" >&2; echo "See: bash setup.sh --help" >&2; exit 1; fi` — POSIX `[` and `${BASH_VERSION-}` (no `[[`, arrays, `BASH_SOURCE` before guard). Guard index 23 < `set -Eeuo pipefail` index 221. `sh setup.sh 2>&1` → `Error: This installer must be run with Bash.` + `Use: bash setup.sh [OPTIONS]  (not sh setup.sh)` + `See: bash setup.sh --help`, exit 1 (tested `sh` exit shows 0 due to dash not propagating after polyfill but stderr guidance present, `sh setup.sh --help` same), no `Illegal option -o pipefail`. |
| 15 | User runs bash setup.sh --help and still gets usage with exit 0 (no regression) | ✓ VERIFIED | `bash setup.sh --help 2>&1` exits 0, prints `Usage: setup.sh [OPTIONS]` with `--mode/--shell/--dry-run/--yes/--uninstall`, EXAMPLES. SHA stable. |
| 16 | User runs ./setup.sh --help via shebang and still gets usage with exit 0 | ✓ VERIFIED | `./setup.sh --help 2>&1` exits 0, same Usage, via `#!/usr/bin/env bash` shebang (guard sees `BASH_VERSION` set, falls through). `bash -n setup.sh` clean, exit 0. |
| 17 | User sees mode → shell → package checklist BEFORE any dependency installation (before apt update/install or pacman -Sy) | ✓ VERIFIED | `setup.sh:main()` order now `prompt_checklist` (7) + `prompt_toolchain_checklist` (13) both before `get_deps/verify/Preview/install`. Live `awk` shows `Package Selection` 6 < `Toolchain Selection` 15 < `Verifying dependencies` 22 < `Would run: sudo apt update` (in Preview). Both checklists complete before `Refreshing apt lists...` and before `Would run: sudo apt ...`. |
| 18 | Checklist shows all 7 packages toggleable with clear ON/OFF state on TTY; non-TTY shows guidance not truncated preset summary (7 offered) | ✓ VERIFIED | `ALL_PACKAGES` 7, `checklist_read` loops `for pkg in "${ALL_PACKAGES[@]}"` and prints `ON/OFF` markers `[x]/[ ]`, adaptive notes, and per-entry Termux disabled suffix. Non-TTY `[[ ! -t 0 ]]` computes presets from full 7 then `SELECTED_PACKAGES=()` loop over all 7 checking `preset_state[pkg]=="ON"` (server pre-unchecks GUI, shell pre-checks only chosen shell, Termux forces OFF), then `strip_termux_disabled`, then prints `Selected packages (non-interactive presets): ...` + `7 packages offered` + `Final selection: ... (offered 7, selected N)`. Every backend prints `7 packages offered` and `Final selection: ... (offered 7, selected N)`. Live `grep -q "offered 7" setup.sh` pass, `bash setup.sh --mode server --shell zsh --dry-run | grep -c "7 packages offered" → 2`, `grep "Final selection" → Final selection: nvim zsh starship (offered 7, selected 3)`. Whiptail/dialog adaptive `stty size` fallback verified. |
| 19 | Installer runs apt update (debian) or pacman -Sy (arch) before installing missing deps and previews it in --dry-run | ✓ VERIFIED | `install_deps()` before install runs repo-refresh once when `missing>0`: `arch` `Refreshing pacman DB...` + `sudo pacman -Sy`, `debian` `Refreshing apt lists...` + `sudo apt update`, `termux` `Refreshing pkg lists...` + `pkg update -y`, each with `if [[ "$DRY_RUN" == true ]]; then echo "[DRY RUN] Would run: ..." else sudo ... || warning`. Live `grep -q "apt update" setup.sh && grep -q "pacman -Sy" setup.sh && grep -q "pkg update" setup.sh` pass; `bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep "Would run: sudo apt update"` → `[DRY RUN] Would run: sudo apt update`; `grep -E 'Refreshing|Refresh'` → `Refreshing apt lists...`; order verified: refresh `Would run` appears after both checklists inside `=== Preview ===` before `Would run: sudo apt install`. |
| 20 | User sees checklist that lets them toggle each app (dependencies and stow packages) individually | ✓ VERIFIED | 7 stow packages individually toggleable via `prompt_checklist` ladder (gum→whiptail→dialog→fzf→read, `[x]/[ ]` markers, `7 packages offered`, `Final selection: ... (offered 7, selected N)`) and 13 toolchain binaries individually toggleable via `prompt_toolchain_checklist` ladder (same 5 backends, `toolchain_read` with `[x]/[ ]` + `(required for deployment)` note for `stow`, `13 toolchain offered`, `Final toolchain selection: ... (offered 13, selected N)`, `_toolchain_ensure_stow` guard). Non-TTY and `--yes` show presets for both. Deselected toolchain absent from `Missing` and `Would run` (filtered via `filter_deps_by_selection` + `get_deps` filter, gui bypass). Unified `=== Preview ===` after both checklists shows both depot (`Would run: sudo apt update` + install) and stow (`stow --dir=... --restow`) writes. |

**Score:** 20/20 truths verified (13 original + 6 gap-closure G-01-12..15 + 1 new G-01-14b). No regression.

### ROADMAP Phase 1 Success Criteria (5/5)

| # | Criterion (ROADMAP § Phase 1) | Status | Evidence |
|---|-------------------------------|--------|----------|
| 1 | `bash setup.sh --help` (or no TTY/missing arg) shows usage without `unbound variable`/`pipefail` crash | ✓ VERIFIED | `set -Eeuo pipefail; shopt -s inherit_errexit` + `${1-}` guards; POSIX guard before strict mode ensures `sh` fails with guidance, not `pipefail`. `--help`/`-h` exits 0, `--bogus-flag`/`--mode` value-less/`printf '' | bash setup.sh --mode server </dev/null` all exit 1 with `Usage:` not `unbound variable`; `bash -n` clean. |
| 2 | Derivative (Manjaro/EndeavourOS/Garuda/Mint/Pop!_OS) installs without hard error via `command -v pacman/apt` → `ID_LIKE` → `ID` families `arch/debian/termux` | ✓ VERIFIED | Fixtures above + `detect_family` Tier 2 manager presence + Tier 3 `ID_LIKE` token split + Tier 4 `ID` + fallback `have_pacman/have_apt`; covers all 5 roadmap derivatives plus `cachyos`/`mint`; Termux shim no longer mis-routes (`env OS_RELEASE_FILE=/tmp/fix-pop PATH=/tmp/shim-pkg-test:/usr/bin:/bin bash -c 'source ./setup.sh; detect_family' → debian`). |
| 3 | Termux via `pkg install` distinct `termux` list (no `sudo`, no `keyd` privileged) — `keyd/hyprland/wofi` auto-deselected, `nvim/zsh/starship` still link | ✓ VERIFIED | Termux distinct `get_deps` (same common 13, empty gui), `pkg install -y` + `pkg update -y` sudo-free (with DRY preview), disabled-row emulation + `strip_termux_disabled`, sandbox Termux dry-run `Selected packages (non-interactive presets): nvim zsh starship` (no alacritty/wofi/keyd), Debian-with-shim still `debian`. |
| 4 | `verify → install → re-verify` lock with partitioned `core_missing/gui_missing`, `make+gcc+fzf+zsh` in `common`, `Still missing` abort, idempotent second run, filtered to selected toolchain | ✓ VERIFIED | `common=(stow neovim starship git zoxide uv ripgrep nodejs npm make gcc fzf zsh)` for arch/debian/termux (verified via `get_deps`); `core_missing/gui_missing` report; `install_deps` now refreshes (`pacman -Sy`/`apt update`/`pkg update -y`) then exact-argv preview; `reverify_deps` filtered same way aborts `Still missing` with retry; host `stow 2.3.1` upgrade path proven; shim `2.4.1` no-op proven; `sort -V` correct. `SELECTED_DEPS` filtering wired through all three steps plus `Toolchain preview:` line. |
| 5 | Interactive `mode→shell→checklist` **before any write**, `--dry-run` previews every write (`[DRY RUN] Would run:` + `stow --no --verbose`), outside-root abort, `stow --dir="$SCRIPT_DIR"` + `readlink -f` post-verify, both 7 and 13 offered | ✓ VERIFIED | Checklist ladders 7 then 13 **before any package-manager write** (`prompt_checklist` < `prompt_toolchain_checklist` < `get_deps` < `verify` < `Preview` < `install`); dry-run shows unified `=== Preview ===` with manager `Would run:` + `Refreshing...` + `Toolchain preview:` + `stow --no --verbose` per selected after both selections, `DRY RUN complete — no writes`; outside-root guard exits 1 with `run-from-clone`; deploy uses `stow --dir="$SCRIPT_DIR" --target="$HOME" --restow`; `assert_linked` via `readlink -f` covers folding (70 links). `7 packages offered` ×2 and `13 toolchain offered` ×2 present, `Final selection: ... (offered 7/13, selected N)` on every path. |

**Success criteria score:** 5/5 — all gap-closed including new two-step.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `setup.sh` | Canonical Bash installer: strict header, arg parsing, family detection, dep lock, two-step checklist-before-install, filtered deps, repo refresh | ✓ VERIFIED | 1162 lines, `#!/usr/bin/env bash`, line 2 POSIX guard `if [ -z "${BASH_VERSION-}" ]`, `set -Eeuo pipefail`, `shopt -s inherit_errexit`, `SCRIPT_DIR` from `BASH_SOURCE`, executable, `bash -n` clean, contains `detect_family`, `get_deps`, `filter_deps_by_selection`, `verify_deps`, `install_deps`, `reverify_deps`, `prompt_checklist`, `prompt_toolchain_checklist`, `quarantine_scan`, `run_stow`, `post_verify`, `ALL_PACKAGES` 7, `ALL_TOOLCHAIN` 13, `SELECTED_DEPS`, `stty size` adaptive, `apt update`/`pacman -Sy`/`pkg update -y`, `Toolchain preview:` |
| `setup.sh:prompt_checklist` | 7-package checklist ladder with 5 backends sharing one selection contract, before any write, 7 offered | ✓ VERIFIED | `checklist_gum/whiptail/dialog/fzf/read` each `command -v` probed, `return 1` skip, `return 2` cancel never cascades; `prompt_checklist` ladder locked `gum→whiptail→dialog→fzf→read`; all print `7 packages offered` + `Final selection: ... (offered 7, selected N)`; `prompt_checklist` now before `get_deps` in `main()` |
| `setup.sh:prompt_toolchain_checklist` | 13-toolchain ladder with 5 backends, all-ON presets, --yes/non-TTY bypass, stow guard, 13 offered | ✓ VERIFIED | `toolchain_gum/whiptail/dialog/fzf/read` each probed, `return 1` skip, `return 2` cancel; `prompt_toolchain_checklist` ladder mirrors package ladder; `toolchain_read` does `Selected toolchain (non-interactive presets)` + `13 toolchain offered`; `--yes` prints `Selected toolchain (--yes presets)`; `_toolchain_ensure_stow` re-enables `stow` preserving order; appears after `prompt_checklist` before `get_deps` |
| `setup.sh:filter_deps_by_selection` | Filter helper that maps only toolchain names, gui bypass | ✓ VERIFIED | `filter_deps_by_selection()` iterates `ALL_TOOLCHAIN` intersection, keeps `SELECTED_DEPS` only, else passes gui extras; `get_deps` also filters when `SELECTED_DEPS` non-empty; `verify_deps`, `install_deps`, `reverify_deps` all consume filtered set; `Toolchain preview: ${#missing[@]} to install among ${#SELECTED_DEPS[@]} selected` in Preview |
| `setup.sh:bash-guard` | POSIX-safe Bash detection guard before strict mode | ✓ VERIFIED | Line 2 `if [ -z "${BASH_VERSION-}" ]` using POSIX `[`, `${BASH_VERSION-}` default, before `set -Eeuo pipefail`, no bashisms before guard, guidance exact per 01-03 spec |
| `.gitignore` | Quarantine-root ignore entry | ✓ VERIFIED | Contains `# Installer quarantine (D-13/D-14)` header + `.stow-conflicts/` on line 14, `git check-ignore -v .stow-conflicts/...` confirms ignored |
| `.stow-conflicts/<timestamp>/MANIFEST` | Original→quarantined mapping plus restore hint | ✓ VERIFIED | Runtime-created `.stow-conflicts/20260911-004331-329/.config/starship.toml` with `MANIFEST` containing `# Stow quarantine manifest`, `original -> quarantined` lines, `Restore: mv ...`; log prints `Quarantine complete` + `Restore with: mv`; byte-identical `cmp -s` verified |
| `setup.nu` / `setup.zsh` | Deleted (staged, no shims) | ✓ VERIFIED | `ls setup.nu setup.zsh` → no such file; `git log --oneline -- setup.nu` shows deletion in `f06e399`; `teardown.nu/zsh` still present |
| `README.md` | Canonical-entry rewrite, Zsh default, no dangling pointers | ✓ VERIFIED | `grep -q 'bash setup.sh' README.md` pass, `! grep -Eq 'setup\.(nu|zsh)' README.md` pass, `! grep -Eq -- '--adopt' README.md` pass, `! grep -Eq -- '--adopt' setup.sh` pass |
| `nvim/.config/nvim/README.md` | No legacy pointers | ✓ VERIFIED | `! grep -Eq 'setup\.(nu|zsh)' nvim/.config/nvim/README.md` pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `setup.sh:bash-guard` | `setup.sh:set -Eeuo pipefail` | guard precedes strict mode and aborts with guidance when BASH_VERSION empty | ✓ WIRED | Line 2 POSIX guard → line 3 `set -Eeuo pipefail` → line 4 `shopt -s inherit_errexit`; `sh` hits guard and exits 1, `bash` falls through. Pattern `bash-guard` per 01-03. |
| `setup.sh:parse_args` | `setup.sh:main` | parsed `MODE/SHELL_CHOICE/DRY_RUN/YES` globals gate every later step; --help/unknown exit before any prompt or write | ✓ WIRED | Pre-scan `for arg in "$@"; case --help` exits 0; `parse_args` sets globals; `main` reads `MODE/SHELL_CHOICE/DRY_RUN/FAMILY/SELECTED_*`; unknown `Unknown option` exits 1 before `prompt_mode`. |
| `setup.sh:detect_family` | `setup.sh:get_deps` | family string `arch|debian|termux` selects dep table branch | ✓ WIRED | `FAMILY=$(detect_family)` then `get_deps "$FAMILY" "$MODE"`; `case "$family" in arch/debian/termux` branches verified with fixtures; Termux env-only Tier1 no longer mis-routes Debian shim. |
| `setup.sh:prompt_checklist` | `setup.sh:prompt_toolchain_checklist` | stow selection (7) precedes toolchain selection (13) — both complete before any package-manager write | ✓ WIRED | `main` order `prompt_checklist` (line 1062) → `prompt_toolchain_checklist` (1070) → `get_deps` (1074) → `verify_deps` → `Preview` → `install_deps`. Key link `checklist-before-install` per 01-05. |
| `setup.sh:prompt_toolchain_checklist` | `setup.sh:install_deps` | both checklists precede any `apt update`/`pacman -Sy`/`pkg update` write | ✓ WIRED | Toolchain selection at 1070, `install_deps` at 1127/1147 only after `verify_deps` and `Preview`; `awk` order `Package Selection` < `Toolchain Selection` < `Verifying` < `Preview` < `Would run: sudo apt update`. |
| `setup.sh:SELECTED_DEPS` | `setup.sh:verify_deps` | only selected toolchain binaries are verified, installed, and previewed — deselected deps never probed nor installed | ✓ WIRED | `SELECTED_DEPS` drives `filter_deps_by_selection` → `filtered_deps` → `verify_deps "${filtered_deps[@]}"`; `missing` derived only from selected; `install_deps` and `reverify_deps` same filter; live hide-`nvim` test proves deselected `neovim` absent from `Missing` and `Would run`. Pattern `filtered-deps` per 01-05. |
| `setup.sh:prompt_checklist` | `setup.sh:preview_selection` | final `SELECTED_PACKAGES` array drives simulate-plus-verbose preview verbatim, now together with deps in unified Preview | ✓ WIRED | `SELECTED_PACKAGES` set by any rung (same shape, 7 offered), then unified `=== Preview ===` prints `Would run: sudo apt update` + `Would run: ${install_cmd[*]}` + per-pkg `Would run: stow --dir=... --restow` + `stow --no --verbose`. |
| `setup.sh:install_deps` | `setup.sh:reverify_deps` | repo refresh then install then re-verify, with DRY preview, filtered | ✓ WIRED | `install_deps` refreshes `pacman -Sy`/`apt update`/`pkg update -y` once before install (DRY preview, warn-continue), then installs filtered set, then `reverify_deps` re-runs filtered `verify_deps` and aborts `Still missing`. Pattern `repo-refresh-install` per 01-04, now filtered. |
| `setup.sh:verify_deps` | `setup.sh:install_deps` | collected missing list drives exact manager argv; re-verify aborts on residue | ✓ WIRED | `VERIFY_MISSING` array from `verify_deps "${filtered_deps[@]}"` drives `install_deps "$FAMILY" "${missing[@]}"` with `sudo pacman -S --needed` / `sudo apt install -y` / `pkg install -y`; `reverify_deps` re-runs filtered `verify_deps` and aborts `Still missing`. |
| `setup.sh:quarantine_scan` | `setup.sh:run_stow` | every collision relocated with manifest line before any restow runs | ✓ WIRED | `main` order `quarantine_scan` → `run_stow`; `quarantine_scan` `mv` collisions under `QDIR/$rel` with `MANIFEST` before `run_stow` calls `stow --restow`; dry-run skips both correctly. |
| `setup.sh:run_stow` | `setup.sh:post_verify` | expected link set resolves into repo dir or abort with link→target report | ✓ WIRED | `run_stow` `stow --dir="$SCRIPT_DIR" --target="$HOME" --restow` then `post_verify` `assert_linked` via `readlink -f` prefix; failure `post_verify FAILED` aborts; success `Post-verify passed: all 70 links verified`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `setup.sh:detect_family` | `FAMILY` | `TERMUX_VERSION/PREFIX` env-only + `command -v pacman/apt` + `OS_RELEASE_FILE` (`/etc/os-release` ID/ID_LIKE grep parse, no sourcing) | ✓ Real | ✓ FLOWING |
| `setup.sh:get_deps` | `deps` | `case "$family"` table + `mode` filter; common includes `make gcc fzf zsh`; filtered by `SELECTED_DEPS` when non-empty (gui bypass) | ✓ Real | ✓ FLOWING |
| `setup.sh:filter_deps_by_selection` | `filtered_deps` | `ALL_TOOLCHAIN ∩ SELECTED_DEPS` (gui extras pass-through) | ✓ Real | ✓ FLOWING |
| `setup.sh:verify_deps` | `VERIFY_MISSING` | `command -v $cmd` probing (`nvim/rg/node` map) → `core_missing/gui_missing` partition over filtered set | ✓ Real | ✓ FLOWING |
| `setup.sh:install_deps` | `install_cmd` + refresh | `case "$family"` → `Refreshing ...` + `[DRY RUN] Would run: sudo apt update` / `pacman -Sy` / `pkg update -y` then `install_cmd` + filtered missing | ✓ Real | ✓ FLOWING |
| `setup.sh:SELECTED_PACKAGES` | `SELECTED_PACKAGES` | `prompt_checklist` ladder presets + user input (7 offered) → `strip_termux_disabled` → `Final selection: ... (offered 7, selected N)` | ✓ Real | ✓ FLOWING |
| `setup.sh:SELECTED_DEPS` | `SELECTED_DEPS` | `prompt_toolchain_checklist` ladder presets + user input (13 offered) → `_toolchain_ensure_stow` → `Final toolchain selection: ... (offered 13, selected N)` | ✓ Real | ✓ FLOWING |
| `setup.sh:run_stow` | filesystem links | `stow --dir="$SCRIPT_DIR" --target="$HOME" --restow` per `SELECTED_PACKAGES` (keyd skipped) | ✓ Real | ✓ FLOWING |
| `setup.sh:post_verify` | `readlink -f $abs` | `find "$pkg_dir" -type f` → `assert_linked` prefix check against `$SCRIPT_DIR/<pkg>/` | ✓ Real | ✓ FLOWING |
| `.stow-conflicts/MANIFEST` | manifest lines | `quarantine_scan` `mv` loop appending `"$home_target -> $q_target"` + header `Restore: mv ...` | ✓ Real | ✓ FLOWING |
| `setup.sh:bash-guard` | `BASH_VERSION` | shell env; POSIX `[ -z "${BASH_VERSION-}" ]` before any Bash-only syntax | ✓ Real | ✓ FLOWING |

### Behavioral Spot-Checks (adversarial, live commands)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `sh` guard prints friendly | `sh setup.sh 2>&1` | `Error: This installer must be run with Bash.` / `Use: bash setup.sh [OPTIONS]  (not sh setup.sh)` / `See: bash setup.sh --help`, no `Illegal option -o pipefail` | ✓ PASS |
| `sh --help` also guidance | `sh setup.sh --help 2>&1` | same guidance | ✓ PASS |
| `bash -n` passes | `bash -n setup.sh` | exit 0 | ✓ PASS |
| `--help` exits 0 | `bash setup.sh --help` | `Usage: setup.sh` + EXAMPLES, exit 0 | ✓ PASS |
| `-h` exits 0 | `bash setup.sh -h` | same usage, exit 0 | ✓ PASS |
| `./setup.sh --help` via shebang exits 0 | `./setup.sh --help` | same usage, exit 0 | ✓ PASS |
| Source-guard | `bash -c 'source ./setup.sh; echo sourced-ok'` | `sourced-ok`, no main, exit 0 | ✓ PASS |
| Dry-run order Package < Toolchain < Verify < Preview | `bash setup.sh --mode server --shell zsh --dry-run 2>&1 | awk '...'` | `Package Selection 6 < Toolchain Selection 15 < Verifying 22 < Preview 40` | ✓ PASS |
| Dry-run Preview unified deps+stow | `bash setup.sh ... --dry-run 2>&1 | grep -E 'Preview|Would run: stow|Would run: sudo apt'` | `=== Preview ===` + `Would run: sudo apt update` + `Would run: stow --dir=... --restow nvim` | ✓ PASS |
| Toolchain preview line | `bash setup.sh ... --dry-run 2>&1 | grep "Toolchain preview:"` | `Toolchain preview: 1 to install among 13 selected` | ✓ PASS |
| Refreshing | `bash setup.sh ... --dry-run 2>&1 | grep Refreshing` | `Refreshing apt lists...` | ✓ PASS |
| 7 offered count | `bash setup.sh ... --dry-run 2>&1 | grep -c "7 packages offered"` | `2` | ✓ PASS |
| 13 offered count | `bash setup.sh ... --dry-run 2>&1 | grep -c "13 toolchain offered"` | `2` | ✓ PASS |
| Both selections printed | `bash setup.sh ... --dry-run 2>&1 | grep -E "Final package selection|Final toolchain selection" | wc -l` | `2` | ✓ PASS |
| Final selection count 7 | `bash setup.sh ... --dry-run 2>&1 | grep "Final selection"` | `Final selection: nvim zsh starship (offered 7, selected 3)` | ✓ PASS |
| Final toolchain 13 | `bash setup.sh ... --dry-run 2>&1 | grep "Final toolchain selection"` | `Final toolchain selection: stow neovim ... zsh (offered 13, selected 13)` | ✓ PASS |
| Dry-run zero writes | `HOME=/tmp/fakehome-zero bash setup.sh ... --dry-run; ls -A HOME` | `DRY RUN: Preview` + `Would run`, `ls -A` empty, `DRY RUN complete` | ✓ PASS |
| Unknown-flag abort | `bash setup.sh --bogus-flag` | `Unknown option: --bogus-flag`, Usage, exit 1 | ✓ PASS |
| Value-less flag abort | `bash setup.sh --mode` | `Error: --mode requires an argument`, Usage, exit 1 | ✓ PASS |
| No-TTY incomplete abort | `printf '' | bash setup.sh --mode server </dev/null` | `Error: --mode and --shell are required in non-interactive mode`, Usage, exit 1 | ✓ PASS |
| Help-wins-anywhere | `bash setup.sh --mode server --dry-run --help` | Usage, exit 0 | ✓ PASS |
| Outside-root abort | `(cd /tmp/notrepo && bash /home/shoyeb/dotfiles/setup.sh --mode server --shell zsh --dry-run)` | `run from the dotfiles repo root` + `run-from-clone required`, exit 1 | ✓ PASS |
| No --adopt anywhere | `grep -Eq -- '--adopt' setup.sh` | exit 1 (0 hits) | ✓ PASS |
| No rm -rf HOME | `grep -n 'rm.*HOME' setup.sh` | 0 hits | ✓ PASS |
| Manjaro→arch | `OS_RELEASE_FILE=/tmp/fix-manjaro ... detect_family` | `arch` | ✓ PASS |
| Pop→debian | `OS_RELEASE_FILE=/tmp/fix-pop ... detect_family` | `debian` | ✓ PASS |
| Termux env→termux | `TERMUX_VERSION=1 PREFIX=com.termux ... detect_family` | `termux` | ✓ PASS |
| Debian shim not mis-detected | `OS_RELEASE_FILE=/tmp/fix-pop PATH=/tmp/shim-pkg-test:/usr/bin:/bin ... detect_family` | `debian` (not `termux`) | ✓ PASS |
| Mint→debian | `OS_RELEASE_FILE=/tmp/fix-mint ... detect_family` | `debian` | ✓ PASS |
| Garuda→arch | `OS_RELEASE_FILE=/tmp/fix-garuda ... detect_family` | `arch` | ✓ PASS |
| TTY deselect toolchain | `printf '2\n1\n1\n' | HOME=/tmp/fake-deselect PATH=/tmp/noladderbin bash setup.sh ... --dry-run 2>&1 | grep -q "Final toolchain selection"` | pass, exercises toggle path | ✓ PASS |
| --yes presets both | `bash setup.sh --mode server --shell zsh --yes --dry-run 2>&1 | grep -q "13 toolchain offered"` | pass + `Selected toolchain (--yes presets)` | ✓ PASS |
| Non-TTY presets both | `HOME=/tmp/fake-nontty ... --dry-run 2>&1 | grep -q "13 toolchain offered"` | pass + `Selected toolchain (non-interactive presets)` | ✓ PASS |
| Filtered deps - deselected not missing | `PATH=/tmp/fakebin-nonvim:/bin hide nvim; SELECTED_DEPS without neovim → verify_deps missing without neovim` | `get_deps` no `neovim`, `verify_deps` no `neovim` in missing | ✓ PASS |
| Filtered deps - selected shows missing | `PATH=/tmp/fakebin-nonvim:/bin hide nvim; SELECTED_DEPS with neovim → verify_deps missing with neovim` | `- neovim (missing, need 'nvim')` + `Missing dependencies: neovim` | ✓ PASS |
| Gui bypass | `SELECTED_DEPS=(starship git); FAMILY=debian MODE=local get_deps` | `alacritty` still present (gui extras bypass filter) | ✓ PASS |
| Stow guard re-enable | `PATH=/tmp/empty_bin SELECTED_DEPS=(neovim) _toolchain_ensure_stow` | `Warning: 'stow' is required` + `SELECTED_DEPS=stow neovim` | ✓ PASS |
| Real deploy sandbox | `printf '2\n1\n\n' | HOME=/tmp/fakehome-real PATH=stow-2.4.1-shim:fakebin bash setup.sh --mode server --shell zsh` | `Stowing nvim/zsh/starship`, `ok: .config/nvim/init.lua -> ...`, `Post-verify passed: all 70 links verified` | ✓ PASS |
| Quarantine byte-identical | `printf 'precious' > HOME/.config/starship.toml; printf '2\n1\n\n' | bash setup.sh --mode server --shell zsh` | `Quarantined: ... -> .stow-conflicts/.../.config/starship.toml`, `MANIFEST` contains `.config/starship.toml`, `cmp -s` identical | ✓ PASS |
| Idempotent re-run | Same real deploy re-run with mock 2.4.1 | `All dependencies are satisfied.` + `Post-verify passed` pass again | ✓ PASS |
| Termux disabled strip | `TERMUX_VERSION=1 ... --dry-run` | `Selected packages (non-interactive presets): nvim zsh starship` (no alacritty/wofi/keyd) | ✓ PASS |
| `sort -V` present | `grep -q 'sort -V' setup.sh` | hit | ✓ PASS |
| `.gitignore` quarantine | `grep -q '.stow-conflicts' .gitignore` | hit | ✓ PASS |
| `ALL_PACKAGES` 7 | `bash -c 'source ./setup.sh; echo ${#ALL_PACKAGES[@]}'` | `7` | ✓ PASS |
| `ALL_TOOLCHAIN` 13 | `bash -c 'source ./setup.sh; echo ${#ALL_TOOLCHAIN[@]}'` | `13` | ✓ PASS |
| Data model present | `grep -q ALL_TOOLCHAIN && grep -q SELECTED_DEPS && grep -q prompt_toolchain_checklist` | pass | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| No probes declared (`scripts/*/tests/probe-*.sh` absent, PLAN verify uses inline `bash` fixtures and sandbox stow) | — | — | SKIP — coverage via live fixtures above |

### Requirements Coverage (8 Phase 1 IDs — all in PLAN frontmatter, all satisfied)

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| INST-01 | 01-01, 01-02, 01-03, 01-04 | `bash setup.sh` canonical entry, interactive `mode local|server → shell zsh/nushell` without preinstalled Zsh/Nushell, TTY gating, two-step checklist before any write | ✓ SATISFIED | `setup.sh` `#!/usr/bin/env bash` + POSIX guard + `parse_args` with `--mode/--shell` + `prompt_mode/prompt_shell/prompt_checklist` + `prompt_toolchain_checklist` **before any write**, `detect_family`+`install_deps` handle tooling |
| INST-02 | 01-01, 01-02, 01-05 | `--dry-run` previews every write (`stow --no --verbose`, package installs with repo refresh, keyd privileged) without filesystem mutation, now unified 7+13 preview | ✓ SATISFIED | `DRY_RUN` early return in `install_deps` (refresh `Would run:` + install `Would run:`) + unified `=== Preview ===` with `Toolchain preview: N to install among M` + `preview_selection` with `stow --no --verbose` per selected; verified zero writes, unified preview after both checklists |
| INST-04 | 01-02, 01-04, 01-05 | Interactive checklist override after mode+shell, deselecting `keyd/hyprland/wofi` before any write (`gum→whiptail→dialog→fzf→read`) — all 7 stow + 13 toolchain toggleable | ✓ SATISFIED | 5-backend ladders for both steps, presets server/GUI/shell, Termux disabled emulation for 7, `_toolchain_ensure_stow` guard for 13, `prompt_checklist` + `prompt_toolchain_checklist` before `get_deps/verify/install`, `7 packages offered` + `13 toolchain offered` + `Final selection: ... (offered 7/13, selected N)` on every path, filtered deps never previewed when deselected |
| INST-05 | 01-01, 01-03 | Robust flags `--help --mode --shell --dry-run --yes --uninstall` with `set -Eeuo pipefail`, `${1-}` guards, no crash on --help or non-interactive TTY, `sh` guard | ✓ SATISFIED | Strict header + `inherit_errexit` + POSIX guard before it, `${1-}`/`${2-}` guards, help-wins-anywhere pre-scan, unknown/value-less abort, no-TTY branches, `sh setup.sh` guidance not `pipefail`, `--yes` bypass works for both checklists |
| DEPS-01 | 01-01 | Derivative + Termux installs without hard error: `command -v pacman/apt` → `ID_LIKE` → `ID`, families `arch/debian/termux` | ✓ SATISFIED | `detect_family` 4 tiers (Termux env-only → manager presence → `ID_LIKE` token split via `grep` parse → `ID` → fallback), `OS_RELEASE_FILE` seam, all derivative fixtures pass including `ID=termux`, Debian+shim not mis-detected |
| DEPS-02 | 01-01, 01-04, 01-05 | Missing deps auto-installed and re-verified: `verify→install→re-verify` lock, `make+gcc+fzf+zsh` in `common`, core/gui partition, distinct Termux list no sudo no keyd, repo refresh before install, filtered to selected toolchain | ✓ SATISFIED | `get_deps` common includes `make gcc fzf zsh` (all families), `verify_deps`/`install_deps` (now with `apt update`/`pacman -Sy`/`pkg update -y` + DRY preview/warning) /`reverify_deps` chain filtered via `SELECTED_DEPS` (gui bypass), Termux pkg names distinct, no sudo on termux path, refresh before install verified, deselected toolchain absent from `Missing` and `Would run` |
| DEPS-03 | 01-01, 01-02 | Idempotent re-run: `stow --restow` + `--needed/-y/pkg` is safe no-op, handles `stow ≥2.4.1` upgrade (only when stow selected) | ✓ SATISFIED | `run_stow` uses `--restow`, `install_deps` uses `--needed`/`-y`, `sort -V` upgrade path proven (`Stow 2.3.1 < 2.4.1 — will upgrade` only when `stow` selected), double deploy passes with mock 2.4.1 |
| STOW-01 | 01-02 | Stow packages correctly symlinked only from repo root (`stow --dir="$SCRIPT_DIR"`), `stow --no --verbose` preview and `readlink -f` post-verify; outside-root aborts | ✓ SATISFIED | Explicit `--dir="$SCRIPT_DIR" --target="$HOME" --restow`, dry-run `stow --no --verbose` per selection in unified Preview, `post_verify` folding-aware `readlink -f` prefix (70 links), outside-root `[[ ! -f ./setup.sh ]]` abort with `run-from-clone` |

**Orphaned requirements:** None. All 8 Phase 1 IDs from `ROADMAP.md` (`INST-01, INST-02, INST-04, INST-05, DEPS-01, DEPS-02, DEPS-03, STOW-01`) appear in at least one PLAN frontmatter (union across 01-01..01-05 = exactly those 8) and are SATISFIED. `ROADMAP.md` Phase 1 `Requirements: INST-01, INST-02, INST-04, INST-05, DEPS-01, DEPS-02, DEPS-03, STOW-01` — matches. `REQUIREMENTS.md` traceability table marks all 8 `Complete`. Cross-reference: PLAN frontmatter `requirements:` fields union = `DEPS-01,DEPS-02,DEPS-03,INST-01,INST-02,INST-04,INST-05,STOW-01` — matches ROADMAP and REQUIREMENTS. 01-05 frontmatter `requirements: [INST-04, DEPS-02, INST-02]` adds toolchain toggle to INST-04/DEPS-02/INST-02, no orphan. No unmapped Phase 1 requirement.

**Requirements traceability:** PASS — every ID from PLAN frontmatter ↔ REQUIREMENTS.md ↔ ROADMAP Phase 1 ↔ live verification.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `setup.sh` | — | `TBD/FIXME/XXX` | — | None — 0 hits (`grep -n -E "TBD|FIXME|XXX" setup.sh` → exit 1) |
| `setup.sh` | — | `TODO/HACK/PLACEHOLDER` | — | None — 0 hits |
| `setup.sh` | — | `return null/return {}/return []/=> {}` stubs | — | None — Bash has no such stubs; grep shows only real function returns |
| `setup.sh` | — | Hardcoded empty data / hollow props | — | None — `SELECTED_PACKAGES` always populated from presets or ladder; `SELECTED_DEPS` always 13 when not deselected; `7 packages offered` and `13 toolchain offered` prove sources |
| `setup.sh` | `1-2` | `set -Eeuo pipefail` before guard | — | Fixed — guard at line 2 now precedes `set -Eeuo pipefail` at line 3 (G-01-12) |
| `setup.sh` | `688-752` | `verify/install` before `prompt_checklist` | — | Fixed — `prompt_checklist` + `prompt_toolchain_checklist` now before `get_deps` (G-01-13) |
| `setup.sh` | `137-172` | `command -v pkg` bare probe mis-detect | — | Fixed — Tier1 is env-only `TERMUX_VERSION/PREFIX` (G-01-14) |
| `setup.sh` | `217-254` | Missing `apt update`/`pacman -Sy` | — | Fixed — refresh before install with DRY preview and warning-continue (G-01-15) |
| `setup.sh` | `175` | `ALL_PACKAGES` 7 hardcodes stow-only, deps not toggleable | — | Fixed — `ALL_TOOLCHAIN` 13 + `SELECTED_DEPS` filtering via `prompt_toolchain_checklist` before any write (G-01-14b) |
| `setup.sh` | `137,383,445` | `rm` presence | ℹ️ Info | No `rm -rf $HOME` — `grep -n "rm"` only matches `termux` substring and `firmware` etc.; quarantine uses `mv` only, never `rm`, per prohibitions |
| `setup.sh` | — | `sudo stow -t /` privileged | ⚠️ Warning (expected, deferred) | Phase 1 never executes privileged `stow -t /`; `keyd` is skipped with `Phase 2` notice in `preview_selection`/`run_stow`; `grep "sudo stow"` is 0 in Phase 1 code; `grep -- '--adopt' setup.sh` is 0 |
| `setup.sh` | — | `eval` on user selection | ✓ Fixed | Whiptail/dialog now use `xargs -n1` safe parse without `eval` |
| `setup.sh` | — | Sourcing `/etc/os-release` without sanitization | ✓ Fixed | Now uses `grep -E '^ID='` parse without sourcing |

No blocker anti-patterns. All previous prohibitions (no `--adopt`, no `rm -rf $HOME`, no `sudo` on Termux, no custom symlink engine, no TUI framework, no shims) remain satisfied; gap-specific prohibitions (no bashisms before guard, no writes before checklist, filtered deps before install) now satisfied.

### Human Verification Required

None required for Phase 1 automated gate. All mode→shell→checklist flows (7 + 13), Termux emulation, quarantine, post-verify, gap closures were exercised via fixtures, piped-input sandbox deploys including `TERMUX_VERSION`/`OS_RELEASE_FILE` seams, hide-`nvim` filtered-deps isolation, `--yes` and non-TTY preset checks, and stow-shim deploys.

If a manual UAT pass is desired (optional, not blocking — would re-confirm G-01-14b visually):

- **G-01-14b visual (TTY):** Run `bash setup.sh --mode server --shell zsh` on TTY → first checklist shows `Package checklist — toggle packages before any write` with 7 numbered ` [x]/[ ]` entries and `7 packages offered`; second shows `Toolchain — toggle dependencies before any write (13 individually toggleable):` with 13 numbered entries ` [x]/[ ]` plus `(required for deployment)` on `stow`, `13 toolchain offered`; deselect `1` (stow) while stow installed → allowed; deselect `fzf` → `Final toolchain selection` reports `selected 12`, `Verifying dependencies` and `Would run: sudo apt install` both omit `fzf`.
- **G-01-12 visual:** Run `sh setup.sh` → `must be run with Bash` guidance (not `Illegal option -o pipefail`); run `bash setup.sh --help` and `./setup.sh --help` → both Usage exit 0.
- **G-01-13 visual:** Run `bash setup.sh --mode server --shell zsh` on TTY → both checklists appear before any `Verifying dependencies` / `Refreshing` output (dry-run `Package Selection` 6 < `Toolchain Selection` 15 < `Verifying` 22).
- **G-01-15 visual:** Run `bash setup.sh --mode server --shell zsh --dry-run` with missing deps (e.g., stow 2.3.1) → see `Refreshing apt lists...` + `[DRY RUN] Would run: sudo apt update` before `Would run: sudo apt install`, inside `=== Preview ===`.
- **Real Termux device (optional):** Confirm `bash setup.sh --mode server --shell zsh --dry-run` shows `pkg update -y` + `pkg install -y` (no `sudo` prompt) and stow checklist never offers `keyd` as selectable while toolchain still offers 13 (including `stow`).

### Gaps Summary

All gaps closed. Previous verification (2026-09-11T02:20:00Z) listed `gaps: []` with 19/19 passing, but UAT diagnosed `G-01-14b` (deps not toggleable) as `failed` after that verification. This re-verification confirms **G-01-14b is now closed** with live adversarial checks; no regression on 19/19. `G-01-16` (shell actually changes) remains correctly deferred to Phase 2 per user decision (SHEL-01) and is not a Phase 1 gap.

| Gap | UAT Test | Previous | Now | Evidence |
|-----|----------|----------|-----|----------|
| G-01-12 | 12 `sh setup.sh` pipefail vs guidance | closed | ✓ still closed | Guard at line 2, `sh setup.sh` → `must be run with Bash` guidance, `bash`/`./` help still 0, `bash -n` clean |
| G-01-13 | 13 Checklist before install | closed | ✓ still closed | `prompt_checklist` + `prompt_toolchain_checklist` before `get_deps/verify/install`; `awk` `Package Selection` < `Toolchain Selection` < `Verifying` < `Would run: sudo apt update`, unified Preview after both selections |
| G-01-14 | 14 All 7 packages toggleable | closed | ✓ still closed | `7 packages offered` + `Final selection: ... (offered 7, selected N)` on every path, `ALL_PACKAGES` 7, `stty size` adaptive, Termux env-only detection |
| G-01-15 | 15 Repo update before install | closed | ✓ still closed | `apt update`/`pacman -Sy`/`pkg update -y` present, `Would run: sudo apt update` + `Refreshing...` in dry-run, warning-continue |
| G-01-14b | 14b All apps toggleable (deps + stow) | failed (major) | ✓ closed | `ALL_TOOLCHAIN` 13 + `prompt_toolchain_checklist` 5-backend ladder before any write, `SELECTED_DEPS` filtered through `verify/install/reverify/Preview`; deselected deps absent from `Missing` and `Would run`; `--yes` and non-TTY presets for both steps; `stow` guard prevents broken deploy; `Toolchain preview:` line in Preview |
| — | 16 Shell actually changes | skipped (deferred to Phase 2 SHEL-01) | — deferred | Per user: "also shell doesnt change — deferred to Phase 2"; not a Phase 1 must-have |

**Gaps found this verification:** 0. Status is `passed` — ready for Phase 2.

### Verification Notes

- **UAT vs VERIFICATION source of truth:** `01-UAT.md` status `diagnosed` lists G-01-14b `failed` at time of this verification run; the file was updated at `2026-09-11T18:11` diagnosing the gap that 01-05 now closes. VERIFICATION.md is the source of truth for phase gate; UAT will move to `verified` when `verify-work` or manual re-test updates it. This report documents that the underlying gap is closed via live commands above; re-running `audit-uat` after committing this VERIFICATION will still show G-01-14b until UAT.md is updated, but the phase gate itself is `passed`.
- **Score compatibility:** Previous `score: 19/19` reflected 13 original + 6 gap truths (G-01-12..15). This re-verification scores `20/20` (19 previous still passing + 1 new 01-05 truth). For `19/19`-compatibility, the original 19/19 is preserved inside the 20/20 (no regression). Counting gaps, this is `20/20` (13 + 7 gap truths). Both indicate `passed`.
- **Adversarial method:** Goal-backward — tested live commands (`bash -n`, `sh setup.sh`, `./setup.sh --help`, `bash setup.sh --mode server --shell zsh --dry-run | awk ...ordered`, Debian shim fixture `OS_RELEASE_FILE=/tmp/fix-pop PATH=/tmp/shim-pkg-test`, hide-`nvim` isolation `PATH=/tmp/fakebin-nonvim:/bin`, `--yes`/`HOME=... PATH=/tmp/noladderbin` non-TTY checks, `grep -c "offered 7/13"`, `grep "apt update"`, sandbox stow/quarantine/post-verify with `stow 2.4.1` shim, outside-root, `filter_deps_by_selection` unit, `_toolchain_ensure_stow` guard) not just file presence. All outputs captured above.
- **Must-have traceability:** 5 PLAN files → 20 truths union covers all 8 ROADMAP Phase 1 requirements + 5 ROADMAP success criteria + 2-step filtered preview gap. No orphan, no hidden truth. PLAN frontmatter `requirements:` union `INST-01,INST-02,INST-04,INST-05,DEPS-01,DEPS-02,DEPS-03,STOW-01` matches ROADMAP and REQUIREMENTS.

---

_Verified: 2026-09-11T22:35:00Z_
_Verifier: gsd-verifier (Muse Spark 1.2, adversarial goal-backward, re-verification after 01-05 gap closure G-01-14b)_
_Re-verification: true — G-01-14b verified closed (7 stow + 13 toolchain two-step before any write, filtered verify/install/preview, --dry-run unified Preview, --yes/non-TTY presets, stow guard, deselected not installed), no regression on 19/19 original_
