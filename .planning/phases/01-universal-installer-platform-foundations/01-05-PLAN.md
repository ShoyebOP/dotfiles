---
phase: 01-universal-installer-platform-foundations
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [setup.sh]
autonomous: true
requirements: [INST-04, DEPS-02, INST-02]
gap_closure: true
gap_ids: [G-01-14b]
estimate:
  tokens: 14500
  raw_tokens: 14500
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "User sees checklist that lets them toggle each app (dependencies and stow packages) individually"
  artifacts:
    - path: "setup.sh"
      provides: "Two-step checklist (7 stow configs + 13 toolchain binaries) with filtered verify/install/preview"
      contains: "prompt_toolchain_checklist"
  key_links:
    - from: "setup.sh:prompt_checklist"
      to: "setup.sh:prompt_toolchain_checklist"
      via: "stow selection (7) precedes toolchain selection (13) — both complete before any package-manager write"
      pattern: "checklist-before-install"
    - from: "setup.sh:SELECTED_DEPS"
      to: "setup.sh:verify_deps"
      via: "only selected toolchain binaries are verified, installed, and previewed — deselected deps are never probed nor installed"
      pattern: "filtered-deps"
---

# Plan 01-05: Gap closure — all apps toggleable (deps + stow) per G-01-14b

> **As a** user who runs `bash setup.sh`, **I want to** toggle every app individually — both the 7 stow configs and the 13 toolchain binaries shown in `Verifying dependencies` — **so that** I control exactly what gets installed without non-toggleable surprises.

<objective>
Close G-01-14b where `prompt_checklist` only offered 7 stow packages (ALL_PACKAGES) while `get_deps`/`verify_deps`/`install_deps` (setup.sh:175/203/217) resolved 13 toolchain binaries unconditionally. User explicitly reported they need all apps toggleable, not just stow dirs, and saw the `Verifying dependencies...` bullet list as identically presented but non-toggleable apps.

Decision (A) vs (B) evaluation:
- (B) keep deps non-toggleable and add UI notice `Core toolchain (always installed)` + clarify that checklist is only for 7 stow configs — rejected: directly contradicts the user's explicit decision that they want all apps toggleable. Would leave G-01-14b truth failing.
- (A) make deps individually toggleable via filtering — accepted. Justification: honors the user's locked decision, directly maps the 13 binaries the user listed to checklist entries, keeps existing 7-package toggle intact (required by Phase 2 dependencies 02-01 etc.), and can be delivered as minimal viable second checklist step without breaking dry-run/preview/non-TTY contracts.

Minimal viable (A) scope for this plan: add a second interactive step `prompt_toolchain_checklist` immediately after the existing 7-package `prompt_checklist` and before `get_deps`/`verify_deps`, backed by a new `ALL_TOOLCHAIN` array and `SELECTED_DEPS` state, then filter `get_deps` output to `SELECTED_DEPS` before verify/install/re-verify and unify both stow and deps previews in the existing `=== Preview ===` block. No change to `ALL_PACKAGES` handling, ladder order, or repo-refresh placement. `stow` is the only toolchain entry with a deployment-required guard (warn and re-enable if user deselects it while not installed), so `run_stow` never loses its prerequisite.

Purpose: Satisfy the user's expectation that every bullet under `Verifying dependencies` is toggleable, eliminate the presentation confusion between stow packages and toolchain deps, and keep --dry-run / --yes / non-TTY contracts correct.
Output: `bash setup.sh --dry-run` shows `=== Package Selection ===` (7) then `=== Toolchain Selection ===` (13) then unified `=== Preview ===` with both stow and dep previews; TTY shows two toggleable checklists, non-TTY/--yes uses presets for both with `offered N, selected N` counts; deselecting a dep skips its verify/install/preview.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-universal-installer-platform-foundations/01-CONTEXT.md
@.planning/phases/01-universal-installer-platform-foundations/01-UAT.md
@.planning/phases/01-universal-installer-platform-foundations/01-VERIFICATION.md
@.planning/phases/01-universal-installer-platform-foundations/01-04-PLAN.md
@.planning/phases/01-universal-installer-platform-foundations/01-04-SUMMARY.md
@setup.sh
</context>

<tasks>

<task type="auto">
  <name>Add toolchain toggle data model and second checklist step</name>
  <files>setup.sh</files>
  <read_first>setup.sh:18-22 ALL_PACKAGES/GUI_STOW_PACKAGES/TERMUX_DISABLED_PACKAGES/SELECTED_PACKAGES; setup.sh:137-199 detect_family/get_deps; setup.sh:316-330 strip_termux_disabled/preview_selection; setup.sh:436-724 checklist_gum/whiptail/dialog/fzf/read + prompt_checklist ladder; setup.sh:726-760 main() ordering prompt_checklist before get_deps/verify</read_first>
  <action>Implement Option (A) minimal viable toolchain toggle — do not modify existing 7-package flow except to insert after it.

1. Data model: near line 19 add `ALL_TOOLCHAIN=(stow neovim starship git zoxide uv ripgrep nodejs npm make gcc fzf zsh)` — exactly the 13 names from `get_deps common` for arch/debian/termux and matching the user's reported `Verifying dependencies` list (`stow` maps to probe `stow`, `neovim`→`nvim`, `ripgrep`→`rg`, `nodejs`→`node`, others 1:1). Add `declare -a SELECTED_DEPS=()` alongside `SELECTED_PACKAGES`. Keep `ALL_PACKAGES` untouched.

2. Second checklist UI: add `prompt_toolchain_checklist` immediately after `prompt_checklist` definition (before main). Reuse the same five-backend ladder pattern and contracts as `prompt_checklist` (gum→whiptail→dialog→fzf→read, cancel code 2 never cascades, 1 skip, 0 success) but operating on `ALL_TOOLCHAIN` instead of `ALL_PACKAGES`. Provide at minimum a production-quality `toolchain_read` fallback and wire the full ladder if scope allows; at minimum `read` path must be fully correct. Checklist semantics:
   - TTY branch: print header `=== Toolchain Selection ===` then `Toolchain — toggle dependencies before any write (13 individually toggleable):` then numbered list `i marker pkg` where marker `[x]` for ON `[ ]` for OFF mirroring `checklist_read` markers, with note ` (required for deployment)` for `stow` only. Initialize presets all ON. Allow toggle via `Enter numbers to toggle (e.g., '1 3')`, `Enter` keeps presets, `c` cancels. Normalize `,` to space, warn on invalid/out-of-range tokens, toggle state in associative map `toggled`. After resolution populate `SELECTED_DEPS` in `ALL_TOOLCHAIN` order. Print `7 packages offered` is not changed for first checklist; for toolchain print `13 toolchain offered` and `Final toolchain selection: ${SELECTED_DEPS[*]} (offered 13, selected ${#SELECTED_DEPS[@]})` to stderr, matching existing `offered/selected` idiom. Do not special-case Termux disabled for toolchain — on Termux all 13 remain toggleable (gui deps are separate from toolchain).
   - Non-TTY branch: if `[[ ! -t 0 ]]` then `SELECTED_DEPS=()` loop `ALL_TOOLCHAIN` with preset ON, print `Selected toolchain (non-interactive presets): ...` + `13 toolchain offered` + `Final toolchain selection: ... (offered 13, selected N)` and return 0 immediately. No prompts.
   - --yes bypass: if `[[ "$YES" == true ]]` then treat identically to non-TTY presets (no prompts) even on TTY — set `SELECTED_DEPS` to all ON and print `Selected toolchain (--yes presets): ...` + `13 toolchain offered` + `Final toolchain selection: ...` and return 0 before any backend probe. This preserves CI bypass contract for the new step without touching existing `--yes` reserved semantics.
   - Stow-required guard: after either branch, if `stow` is not in `SELECTED_DEPS` and `! command -v stow >/dev/null 2>&1` then warn `Warning: 'stow' is required for deployment — re-enabling.` to stderr and re-add `stow` to `SELECTED_DEPS` preserving `ALL_TOOLCHAIN` order (iterate `ALL_TOOLCHAIN` and rebuild). If `stow` already installed, allow deselection without warning (user has it externally).
   - Cancel contract: if read input is `c`/`cancel` or any backend returns 2, propagate `return 1` so caller aborts with `Installation cancelled at toolchain checklist.` — never fall through to next backend on cancel.

3. Integration point: in `main()` immediately after the existing `prompt_checklist` success and the `if [[ ${#SELECTED_PACKAGES[@]} -eq 0 ]]` guard and the two `echo "Final package selection ..."` lines (currently ~752-757), insert:

   echo "" ; echo "=== Toolchain Selection ==="
   if ! prompt_toolchain_checklist; then echo "Installation cancelled at toolchain checklist." >&2; exit 1; fi
   echo "" ; echo "Final toolchain selection: ${SELECTED_DEPS[*]:-<none>}" ; echo "Final toolchain selection: ${SELECTED_DEPS[*]} (offered 13, selected ${#SELECTED_DEPS[@]})" >&2 ; echo "13 toolchain offered" >&2

   Do not move or duplicate the existing `prompt_checklist` block, `FAMILY` detection, mode/shell resolution, repo-root guard, or `DRY_RUN` banner. Toolchain selection stays before `get_deps` so no package-manager write precedes either checklist.

Reference existing code per gap reason: ALL_PACKAGES hardcodes 7 toggleable; get_deps unconditionally resolves 13 — this task adds the missing toolchain toggle surface while keeping 7 intact.</action>
  <verify>
    <automated>bash -n setup.sh</automated>
    <automated>grep -q "ALL_TOOLCHAIN" setup.sh && grep -q "SELECTED_DEPS" setup.sh && grep -q "prompt_toolchain_checklist" setup.sh && echo "data model present"</automated>
    <automated>grep -q "13 toolchain offered" setup.sh && grep -q "Final toolchain selection" setup.sh && echo "counts present"</automated>
    <automated>bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "=== Toolchain Selection ===" && echo "toolchain step appears before preview"</automated>
    <automated>bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -E "Final package selection|Final toolchain selection" | wc -l | grep -q "2" && echo "both selections printed"</automated>
    <automated>printf '2\n1\n\n' | HOME=/tmp/fake-tc-tty PATH=/tmp/noladderbin bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "Final toolchain selection"</automated>
    <automated>HOME=/tmp/fake-tc-nontty PATH=/tmp/noladderbin bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "13 toolchain offered"</automated>
  </verify>
  <done>setup.sh exposes 13 toolchain toggle entries via prompt_toolchain_checklist after the existing 7-package checklist and before any write; TTY shows numbered toggle list with markers, non-TTY and --yes show presets with offered 13 / selected counts; stow re-enable guard present; bash -n clean; existing 7-package checklist unmodified.</done>
</task>

<task type="auto">
  <name>Wire filtered deps through verify/install/re-verify and unified preview</name>
  <files>setup.sh</files>
  <read_first>setup.sh:175-215 get_deps + verify_deps header; setup.sh:217-271 install_deps refresh + install; setup.sh:273-288 reverify_deps; setup.sh:758-830 main preview + install + quarantine + run_stow + post_verify after checklist insertion point</read_first>
  <action>Make the toolchain selection actually filter the dependency lifecycle — complete the (A) mapping without breaking existing 7-package stow behavior.

1. Filter helper: add `filter_deps_by_selection()` or inline filtering in `main()` after `mapfile -t deps < <(get_deps ...)` and before `verify_deps`. Logic: build `filtered_deps` array by iterating `deps` and keeping entry iff it appears in `SELECTED_DEPS`. For entries not in `SELECTED_DEPS`, skip entirely — they never reach `verify_deps`, `install_deps`, or `reverify_deps`. Preserve `get_deps` package-name spelling (neovim, ripgrep, nodejs) so filtering matches `ALL_TOOLCHAIN` names exactly; gui deps (`hyprland waybar grim slurp wl-copy`) are not in `ALL_TOOLCHAIN` so they pass through only if mode==local and family gui includes them — but since the user's gap is about toolchain, gui deps should remain governed by mode alone, not by toolchain selection. To keep minimal viable, filter only the 13 toolchain names; gui extras bypass the filter (always included when get_deps emits them, matching pre-gap behavior). Document via comment that toolchain filter applies to common deps only.

2. Update main flow:
   - After `mapfile -t deps` capture, compute `filtered_deps` via the filter, then call `verify_deps "${filtered_deps[@]}"` instead of `verify_deps "${deps[@]}"`. Keep `VERIFY_MISSING` capture as before but now only over selected set.
   - Derive `missing` from `VERIFY_MISSING` (already filtered). Partition into `core_missing`/`gui_missing` as before — deselected toolchain deps will not appear as missing at all, which is the desired preview behavior.
   - Stow version self-upgrade check: only queue `stow` into missing if `stow` is in `SELECTED_DEPS` (i.e., user did not deselect it while outdated). If `stow` was deselected but outdated and not installed, the earlier stow-required guard would have re-enabled it, so this is consistent.
   - Dry-run preview: keep unified `=== Preview ===` block but ensure it previews only `missing` (which is now filtered). The `install_deps` call already handles `DRY RUN Would run: sudo apt update` + `Would run: ${install_cmd[*]} ${missing[*]}`. After that, `preview_selection` still prints stow previews for `SELECTED_PACKAGES`. Together the dry-run output after both checklists must contain both dep install preview and stow simulation. Add a single echo line before `preview_selection` in the DRY_RUN branch: `echo "Toolchain preview: ${#missing[@]} to install among ${#SELECTED_DEPS[@]} selected"` to make the toolchain count visible in preview without duplicating install logic.
   - Non-dry-run path: call `install_deps "$FAMILY" "${missing[@]}"` only with filtered missing; then `reverify_deps` must re-derive deps filtered the same way. Patch `reverify_deps` to apply the same filter: after `mapfile -t deps`, build `filtered` from `SELECTED_DEPS` (or skip filtering if `SELECTED_DEPS` empty meaning no toolchain selected) then `verify_deps "${filtered[@]}"`; abort only on residue among selected.
   - Keep `quarantine_scan`, `run_stow`, `post_verify` operating solely on `SELECTED_PACKAGES` (unchanged). Ensure no `SELECTED_PACKAGES` logic is altered.

3. Preview and write-order invariants:
   - Both checklist steps must complete before any echo of `Verifying dependencies` or `Refreshing apt lists` or `Would run: sudo apt update`. Verify by checking that `=== Toolchain Selection ===` appears before `Verifying dependencies` in dry-run output.
   - Non-TTY: with `HOME=/tmp/fake-nonTTY PATH=/tmp/noladderbin bash setup.sh --mode server --shell zsh --dry-run` the output must show `7 packages offered` and `13 toolchain offered` and `Final toolchain selection: ... (offered 13, selected 13)` and then `Verifying dependencies` — zero prompts.
   - --yes: with `bash setup.sh --mode server --shell zsh --yes --dry-run` on TTY, neither checklist prompts; presets used for both, `Selected packages (--yes presets)` or `Selected packages (non-interactive presets)` plus `Selected toolchain (--yes presets)` and preview proceeds.
   - Deselect behavior: dry-run with toolchain deselection (e.g., simulate by pre-setting `SELECTED_DEPS` via non-interactive toggle input `1` to deselect stow) must not list that deselected dep as `missing` nor in `Would run:`. Verify that deselected dep is absent from Missing CORE/GUI line.

4. Preserve existing flags: do not change `DRY_RUN` early return in `install_deps`, repo-refresh once-per-invocation, warning-continue on refresh failure, `sort -V` upgrade, idempotent second run, outside-root guard, help-wins-anywhere, source-guard.

Reference: gap missing "If (A): add DEP_OWNERS associative map..." — this plan implements the minimal viable variant via second checklist step with direct filtering instead of ownership map, which is equivalent and smaller scope while fully satisfying toggle per dep. If reviewer prefers ownership map coupling (deselect nvim → skip neovim), note that ownership coupling can be added as follow-up without invalidating this filter — the two approaches are composable.
</action>
  <verify>
    <automated>bash -n setup.sh</automated>
    <automated>bash setup.sh --mode server --shell zsh --dry-run 2>&1 | awk '/=== Package Selection ===/{p=1} /=== Toolchain Selection ===/{t=1} /Verifying dependencies/{v=1} END{ if(p && t && v) print "order ok"; else print "order bad"; exit (p && t && v ? 0 : 1)}' && echo "checklist order holds"</automated>
    <automated>bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "=== Preview ===" && bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "Would run: stow --dir=" && echo "unified preview holds"</automated>
    <automated>HOME=/tmp/fake-verify-filter PATH=/tmp/noladderbin bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "Verifying dependencies"</automated>
    <automated>bash setup.sh --mode server --shell zsh --yes --dry-run 2>&1 | grep -q "13 toolchain offered" && echo "--yes toolchain presets hold"</automated>
    <automated>HOME=/tmp/fake-filter-none PATH=/tmp/noladderbin bash -c 'source ./setup.sh; SELECTED_DEPS=(starship git); FAMILY=debian; MODE=server; mapfile -t _d < <(get_deps "$FAMILY" "$MODE"); echo ${_d[*]} | grep -qv neovim && echo "filter demo ok"'</automated>
    <automated>printf '2\n1\n1\n' | HOME=/tmp/fake-deselect PATH=/tmp/noladderbin bash setup.sh --mode server --shell zsh --dry-run 2>&1 | grep -q "Final toolchain selection" && echo "tty deselect path exercises"</automated>
  </verify>
  <done>Only selected toolchain deps are verified/installed/re-verified and previewed; deselected deps never appear as missing nor in Would run; unified Preview still prints both dep install and stow simulation after both checklists; --dry-run zero writes, --yes and non-TTY use presets for both steps, stow-required guard holds, and existing 7-package flow is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User checklist(s) → filtered install | Both selections (7 stow + 13 toolchain) must complete before any package-manager write; filtered missing must be subset of selected |
| Toolchain selection → package manager | Only selected deps reach `apt`/`pacman`/`pkg` argv; deselected must not be probe-installed |
| Package manager → network | `apt update` / `pacman -Sy` / `pkg update` contacts remote mirrors after selection, before install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Tampering | toolchain selection filtering | high | mitigate | Verify only `SELECTED_DEPS ∩ get_deps` drives `verify_deps`/`install_deps`/`reverify_deps`; deselected deps absent from `Missing` and `Would run`; dry-run preview shows `selected N` count; stow re-enable guard prevents broken deploy |
| T-01-12 | Elevation of privilege | install via `sudo apt` / `sudo pacman` / `pkg` with filtered deps | medium | mitigate | Filter reduces attack surface (fewer packages installed); manager argv is still allowlist-derived from `get_deps` tables filtered by user selection, never free-form input; Termux branch stays sudo-free |
| T-01-13 | Tampering | preview spoofing (deps shown but not installed, or vice versa) | low | mitigate | Unified `=== Preview ===` after both checklists prints `Would run: sudo apt update` + `Would run: ${install_cmd[*]} ${missing[*]}` for filtered set plus `stow --no --verbose` per selected package — verified by awk order check |
| T-01-14 | Denial of Service | deselecting `stow` when not installed | low | mitigate | Guard re-enables `stow` with warning if not installed; otherwise allows deselection — prevents `run_stow` failure without trapping user who already has `stow` |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new registry installs added; toolchain deps are platform-manager packages only (arch `pacman`, debian `apt`, termux `pkg`). If any future `npm`/`pip`/`cargo` install is added for toolchain, insert blocking-human checkpoint per package-legitimacy gate (not needed for this plan) — retained for convergence |
</threat_model>

<verification>
- `bash -n setup.sh` clean; `grep -q ALL_TOOLCHAIN` and `prompt_toolchain_checklist` present
- `bash setup.sh --mode server --shell zsh --dry-run` shows `=== Package Selection ===` before `=== Toolchain Selection ===` before `Verifying dependencies` before `=== Preview ===`
- Dry-run unified preview contains both `Would run: sudo apt update` (or pacman/pkg variant) and `Would run: stow --dir=... --target=... --restow` lines
- TTY: `printf '2\n1\n\n' | bash setup.sh --mode server --shell zsh --dry-run` prompts toolchain after packages and completes with `Final toolchain selection: ... (offered 13, selected N)`
- Non-TTY: `HOME=/tmp/x bash setup.sh --mode server --shell zsh --dry-run </dev/null` prints `7 packages offered` and `13 toolchain offered` with presets, exits 0, zero writes
- --yes: `bash setup.sh --mode server --shell zsh --yes --dry-run` on TTY uses presets for both steps, no prompts
- Deselect: selecting `1` at toolchain prompt deselects `stow` only if already installed; if not installed, guard re-enables with warning and preview excludes only actually-deselected deps
- Existing 7-package behavior intact: `grep -q "ALL_PACKAGES=(nvim zsh nushell alacritty starship wofi keyd)" setup.sh` still passes; Phase 2 02-01 reversible uninstall expectations unchanged
</verification>

<success_criteria>
- Gap G-01-14b closed: user can toggle each toolchain binary individually via second checklist; no confusion with `Verifying dependencies` list
- `ALL_TOOLCHAIN` 13 and `SELECTED_DEPS` filtering wired through verify→install→re-verify; deselected deps invisible to all three and to preview
- Unified dry-run preview after both checklists shows both depot and stow writes; --yes and non-TTY preserve correct preset behavior for both steps
- `stow` required guard prevents broken deploy while still allowing explicit toggle when already installed
- Existing Phase 1/2 contracts unbroken: 7 stow toggle, outside-root abort, help-wins-anywhere, bash -n clean, no new registry installs
</success_criteria>

<output>
Create `.planning/phases/01-universal-installer-platform-foundations/01-05-SUMMARY.md` when done
</output>
