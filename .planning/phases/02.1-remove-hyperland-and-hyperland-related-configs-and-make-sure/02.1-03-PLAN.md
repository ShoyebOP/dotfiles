---
phase: 02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [setup.sh]
autonomous: true
gap_closure: true
requirements: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every remaining installable app is toggleable: tick means install the binary AND stow its config when one exists; untick means never install, never stow — mode and shell only set pre-selection defaults, and an empty SELECTED_DEPS with an active selection does not bypass the filter (no false hard-fail)"
    - "On Debian with keyd ticked, the user gets a manual-build pointer at https://github.com/rvaiya/keyd; keyd is never routed through apt, privileged stow runs only when the keyd binary exists, and uninstall never offers apt remove keyd on Debian (family-aware allowlist at setup.sh:665)"
    - "The five-backend ladder order, cancel-never-cascades contract, Termux disabled-row emulation, --yes and no-TTY fast paths, DRY_RUN previews (zero sudo writes, Termux and Debian gates mirrored), strict mode, repo-root guard, and privileged keyd gate all behave exactly as before"
  artifacts:
    - path: "setup.sh"
      provides: "SELECTION_ACTIVE flag and single tick-authoritative filter enforcement (get_deps pure, filter_deps_by_selection sole gate), family-aware uninstall allowlist, and DRY_RUN uninstall preview without sudo plus Termux/Debian mirrors"
      contains: "SELECTION_ACTIVE"
  key_links:
    - from: "setup.sh:SELECTION_ACTIVE + filter_deps_by_selection"
      to: "setup.sh:get_deps/verify_deps/install_deps/reverify_deps"
      via: "SELECTION_ACTIVE true forces filtering even when SELECTED_DEPS empty; get_deps is pure family/mode source, filtering happens only in filter_deps_by_selection"
      pattern: "SELECTION_ACTIVE"
    - from: "setup.sh:offer_system_package_removal _allowed"
      to: "setup.sh:apt/pacman/pkg remove preview"
      via: "family-aware allowlist excludes keyd on debian, mirroring install_keyd_privileged gate"
      pattern: "_allowed"
    - from: "setup.sh:run_uninstall DRY_RUN"
      to: "setup.sh:privileged stow --target=/ keyd"
      via: "DRY_RUN uninstall preview echoes Would run only, never executes sudo, and mirrors TERMUX + debian no-binary skips from preview_selection"
      pattern: "Would run: sudo stow"
---

<objective>
Gap-closure for VERIFICATION.md 7/9 — close the three failed truths without replanning passing work (7 truths intact). Fix the tick-authoritative empty-selection bypass (WR-01/WR-02/IN-01), the Debian uninstall apt-remove-keyd lie (WR-05), and the DRY_RUN uninstall sudo execution plus missing Termux/Debian mirrors (WR-04/IN-04). Cheap cleanup (IN-01 dedup, WR-07 audit casing) rides along; heavy debt (WR-03 549-line dead code, WR-06 AGENTS.md stale refs, CR-01 secret) is documented as not-blocking and not in this plan's scope unless the executor finds deletion trivially safe.

Purpose: After this plan the unified 16-row checklist is truly tick-authoritative end-to-end (including empty-toolchain selections), Debian keyd never appears in any apt path on install or uninstall, and DRY_RUN never crosses the privilege boundary.

Output: Patched setup.sh (plus git history preserved) — no new files, no doc churn beyond what setup.sh needs for correctness.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-VERIFICATION.md
@.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-CONTEXT.md
@.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-02-SUMMARY.md
@.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-REVIEW.md
@setup.sh
</context>

<tasks>
<!-- planner-discipline-allow: SELECTED_DEPS -->

<task type="auto">
  <name>Fix tick-authoritative empty-selection bypass — SELECTION_ACTIVE flag and single filter enforcement (WR-01, WR-02, IN-01)</name>
  <files>setup.sh</files>
  <read_first>setup.sh:184-272; setup.sh:346-367; setup.sh:1521-1558; setup.sh:1900-1910; .planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-VERIFICATION.md:8-19; .planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-REVIEW.md:66-102</read_first>
  <action>
    Per VERIFICATION gaps truth 6 (WR-01 setup.sh:352-356, WR-02 setup.sh:211,236-238, IN-01 duplicate filter) close the empty-selection bypass:
    - Declare SELECTION_ACTIVE=false alongside SELECTED_PACKAGES/SELECTED_DEPS near setup.sh:23-25. Set SELECTION_ACTIVE=true inside _split_unified_to_selected (1521) after SELECTED_PACKAGES/SELECTED_DEPS are populated and inside _unified_apply_presets (1545) after the split — these are the two sole producers of a user selection (interactive rungs and --yes/no-TTY presets). Also set it in any unified_checklist_* rung that directly assigns SELECTED_* if one bypasses the helper (grep confirms all rungs go through _split_unified_to_selected, so flag there suffices; keep grep as guard).
    - Make get_deps (184-239) a pure family/mode source: delete the inline tick-authoritative filter block at 211,236-238 (the if [[ ${#SELECTED_DEPS[@]} -gt 0 ]] branch and its _f loop). After this, get_deps only builds common+gui per family/mode and prints _all unfiltered. Keep the case tables arch gui=(alacritty keyd), debian gui=(alacritty), termux gui=() untouched. Update the stale comment at 223-227 to note filtering now lives solely in filter_deps_by_selection.
    - Keep filter_deps_by_selection (250-272) as the single enforcement point per IN-01 — no second copy. It already handles empty SELECTED_DEPS correctly (toolchain sel loop returns empty when DEPS empty, gui branch checks SELECTED_PACKAGES tick). No logic change needed except to ensure it is the only filter; do not add a second early-return for empty.
    - Fix reverify_deps (346-367) 352-356: replace the emptiness check with the flag. Change `if [[ ${#SELECTED_DEPS[@]} -eq 0 ]]; then filtered=("${deps[@]}")` to `if [[ "${SELECTION_ACTIVE:-false}" == true ]]; then mapfile -t filtered < <(filter_deps_by_selection "${deps[@]}") else filtered=("${deps[@]}"); fi` (or equivalent — never use array length as proxy). This mirrors the fix for WR-01.
    - Fix main install flow (1900-1905 region calling get_deps then filter_deps_by_selection): replace the `if [[ ${#SELECTED_DEPS[@]} -gt 0 ]]` guard with `if [[ "${SELECTION_ACTIVE:-false}" == true ]]` so an active empty-toolchain selection (e.g., nushell+alacritty+keyd ticked, all non-toolchain) still filters. When SELECTION_ACTIVE is false (pre-checklist, early --help, outside-repo guard), skip filtering and use unfiltered deps as today.
    - Preserve strict header, repo-root guard, DRY_RUN early-returns, keyd privileged gate, and Termux disabled handling. Do not touch offer_system_package_removal in this task (handled in task 2).
  </action>
  <verify>
    <automated>bash -n setup.sh</automated>
    <automated>grep -q 'SELECTION_ACTIVE' setup.sh && grep -q 'SELECTION_ACTIVE=true' setup.sh && echo "SELECTION_ACTIVE flag present"</automated>
    <automated>bash -c 'grep -n "get_deps" ./setup.sh | head; echo "---get_deps body---"; sed -n "184,239p" ./setup.sh | grep -v "^#" | grep -c "SELECTED_DEPS" | xargs -I{} bash -c "test {} -eq 0 && echo get_deps-pure-no-SELECTED_DEPS-ref || (echo get_deps-still-references-SELECTED_DEPS:{}; exit 1)"'</automated>
    <automated>bash -c 'source ./setup.sh; SELECTION_ACTIVE=true; SELECTED_PACKAGES=(nvim zsh starship); SELECTED_DEPS=(); mapfile -t _all < <(get_deps arch local); echo "get_deps arch local (pure) count:${#_all[@]} includes-alacritty:$(printf "%s\n" "${_all[@]}" | grep -qx alacritty && echo yes || echo no)"; printf "%s\n" "${_all[@]}" | grep -qx alacritty || (echo "get_deps should include alacritty when pure (for filtering test)"; exit 1); mapfile -t _f < <(filter_deps_by_selection "${_all[@]}"); printf "filtered:"; printf " %s" "${_f[@]}"; echo ""; printf "%s\n" "${_f[@]}" | grep -qv "^alacritty$" && echo "✓ unticked alacritty dropped (tick-authoritative)" || (echo "✗ alacritty not dropped when unticked"; exit 1); printf "%s\n" "${_f[@]}" | grep -qv "^keyd$" && echo "✓ unticked keyd dropped" || (echo "✗ keyd not dropped"; exit 1)'</automated>
    <automated>bash -c 'source ./setup.sh; SELECTION_ACTIVE=true; SELECTED_PACKAGES=(nushell alacritty keyd); SELECTED_DEPS=(); mapfile -t _deps < <(get_deps arch local); mapfile -t _filt < <(filter_deps_by_selection "${_deps[@]}"); echo "empty-toolchain filtered:${_filt[*]:-<empty-toolchain>)}"; printf "%s\n" "${_filt[@]}" | grep -qx "alacritty" && echo "✓ ticked alacritty kept with empty DEPS" || (echo "✗ ticked alacritty lost"; exit 1); printf "%s\n" "${_filt[@]}" | grep -qx "keyd" && echo "✓ ticked keyd kept" || (echo "✗ ticked keyd lost"; exit 1); printf "%s\n" "${_filt[@]}" | grep -qx "stow" && (echo "✗ stow leaked when DEPS empty and not ticked"; exit 1) || echo "✓ unticked toolchain stow absent"'</automated>
    <automated>bash -c 'grep -A6 "reverify_deps" ./setup.sh | grep -q "SELECTION_ACTIVE" && echo "reverify uses SELECTION_ACTIVE" || (echo "reverify still uses array emptiness"; exit 1); grep -n "reverify_deps" ./setup.sh'</automated>
  </verify>
  <done>SELECTION_ACTIVE governs filtering; get_deps is pure with zero references to SELECTED_DEPS; unticked alacritty/keyd are dropped even when SELECTED_DEPS empty and active, ticked ones kept; reverify no longer bypasses filter on empty selection and no false Still missing hard-fail occurs for legitimate deselections.</done>
  <acceptance_criteria>setup.sh contains SELECTION_ACTIVE=false declaration and SELECTION_ACTIVE=true assignments in _split_unified_to_selected and _unified_apply_presets; get_deps body contains zero greps for SELECTED_DEPS (pure source); filter_deps_by_selection remains the sole filter; reverify_deps branches on SELECTION_ACTIVE not array length; fixture SELECTION_ACTIVE=true SELECTED_PACKAGES=(nvim zsh starship) SELECTED_DEPS=() with arch local filtered excludes alacritty and keyd; fixture SELECTED_PACKAGES=(nushell alacritty keyd) with empty DEPS keeps alacritty/keyd and drops unticked stow.</acceptance_criteria>
  <reversibility rating="reversible">Flag and pure-source change reversible via git revert; no migration, no config file change.</reversibility>
</task>

<task type="auto">
  <name>Make uninstall allowlist family-aware — exclude keyd on debian (WR-05, D-06)</name>
  <files>setup.sh</files>
  <read_first>setup.sh:650-710; setup.sh:538-565; setup.sh:410-426; .planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-VERIFICATION.md:20-29; .planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-REVIEW.md:133-149</read_first>
  <action>
    Per VERIFICATION truth 8 gap (WR-05 setup.sh:665 offer_system_package_removal _allowed, 679-710 candidates loop) and D-06 never-apt invariant:
    - At setup.sh:665 replace the family-independent `local -a _allowed=("${ALL_TOOLCHAIN[@]}" alacritty keyd)` with family-aware allowlist mirroring install_keyd_privileged gate: `local -a _allowed=("${ALL_TOOLCHAIN[@]}" alacritty); if [[ "${FAMILY:-}" != "debian" ]]; then _allowed+=(keyd); fi`. Keep ALL_TOOLCHAIN order intact. Do not add keyd to _allowed on debian under any selection state.
    - Ensure the candidates-extension loop (667-678) and _scoped filter (679-687) then naturally exclude keyd on debian because _known check fails. Also ensure the later filter_deps_by_selection branch (688-696) respects SELECTION_ACTIVE if task 1 has landed (it will gate on flag, not emptiness).
    - Do not touch the Debian install-time notice path (prompt_debian_keyd_notice, install_keyd_privileged gate, preview_selection 415-425) — those are already correct and verified. This task only fixes the uninstall offer path.
    - Keep DRY_RUN preview prefix and manager-specific remove commands (arch pacman -Rns, debian apt remove -y, termux pkg uninstall) unchanged; only the keyd inclusion changes.
    - If AGENTS.md stale refs (WR-06) are trivially close, do NOT include here — doc rewrite is out of scope for this file-only task; note in commit message that AGENTS.md remains deferred per verification.
  </action>
  <verify>
    <automated>bash -n setup.sh</automated>
    <automated>grep -n "_allowed" ./setup.sh | head -n 20; grep -A2 'local -a _allowed' ./setup.sh | grep -q 'FAMILY.*debian' && echo "allowlist family-aware" || (echo "allowlist still unconditional"; exit 1)</automated>
    <automated>bash -c 'source ./setup.sh; FAMILY=debian; DRY_RUN=true; SELECTED_PACKAGES=(nvim zsh nushell alacritty starship keyd); SELECTED_DEPS=(stow neovim starship git zoxide uv ripgrep nodejs npm make gcc fzf zsh); echo "=== debian offer_system_package_removal DRY_RUN ==="; out=$(offer_system_package_removal 2>&1); echo "$out"; echo "$out" | grep -q "apt remove.*keyd" && (echo "✗ FAIL: debian still offers apt remove keyd"; exit 1) || echo "✓ debian does NOT offer apt remove keyd"; echo "$out" | grep -q "apt remove" && echo "✓ debian still offers other apt removes" || echo "note: no apt remove lines (maybe empty candidates)"'</automated>
    <automated>bash -c 'source ./setup.sh; FAMILY=arch; DRY_RUN=true; SELECTED_PACKAGES=(nvim zsh nushell alacritty starship keyd); SELECTED_DEPS=(stow neovim starship git zoxide uv ripgrep nodejs npm make gcc fzf zsh); out=$(offer_system_package_removal 2>&1); echo "$out" | grep -q "pacman -Rns.*keyd" && echo "✓ arch still offers pacman -Rns keyd when ticked" || (echo "✗ arch should offer keyd"; exit 1)'</automated>
    <automated>bash -c 'FAMILY=debian bash ./setup.sh --uninstall --mode local --shell zsh --yes --dry-run < /dev/null 2>&1 | tee /tmp/021-debian-uninstall.txt | grep -q "Would run: sudo apt remove -y keyd" && (echo "✗ end-to-end debian uninstall still offers keyd"; exit 1) || echo "✓ end-to-end debian uninstall DRY_RUN has no apt remove keyd"; cat /tmp/021-debian-uninstall.txt | grep -i "apt remove" | head'</automated>
  </verify>
  <done>Debian uninstall DRY_RUN never prints Would run: sudo apt remove -y keyd (even when keyd ticked), arch still offers pacman -Rns keyd, and the allowlist code is family-aware mirroring the install gate.</done>
  <acceptance_criteria>setup.sh around line 665 contains family-aware _allowed construction with FAMILY != debian guard adding keyd; sourced offer_system_package_removal with FAMILY=debian outputs no line containing apt remove.*keyd while FAMILY=arch with same selection does; end-to-end bash setup.sh --uninstall --mode local --shell zsh --yes --dry-run on debian prints no apt remove keyd.</acceptance_criteria>
  <reversibility rating="reversible">Single array construction change, revertible; does not affect install path or privileged gate.</reversibility>
</task>

<task type="auto">
  <name>Fix DRY_RUN uninstall privileged boundary and mirror Termux/Debian gates (WR-04, IN-04, WR-07)</name>
  <files>setup.sh</files>
  <read_first>setup.sh:410-450; setup.sh:822-862; setup.sh:413; .planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-VERIFICATION.md:30-40; .planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-REVIEW.md:118-133,164-178,210-218</read_first>
  <action>
    Per VERIFICATION truth 9 gap (WR-04 setup.sh:837-843 sudo inside DRY_RUN, IN-04 missing Termux/Debian mirrors 826-844, WR-07 lowercased selection audit at 413):
    - In run_uninstall DRY_RUN branch (822-862): replace the sudo execution at 840 `sudo stow --dir="$SCRIPT_DIR" --target=/ --no --verbose --delete keyd 2>&1 | sed` with echo-only preview. The branch should print `echo "[DRY RUN] Would run: sudo stow --dir=\"$SCRIPT_DIR\" --target=/ --no --verbose --delete keyd"` and never execute sudo under DRY_RUN. Mirror the install preview_selection gates at 415-425: before the privileged keyd delete preview, add two early gates in the same order: if FAMILY==termux then `echo "[DRY RUN] Would skip keyd — not available on Termux"` and skip the privileged preview (continue); else if FAMILY==debian && ! command -v keyd >/dev/null then `echo "[DRY RUN] Would skip privileged keyd stow — keyd binary not found (build from https://github.com/rvaiya/keyd first)"` and skip. This mirrors preview_selection exactly and fixes IN-04.
    - Keep the non-keyd DRY_RUN loop (826-836) as echo + unprivileged `stow --no --verbose --delete` without sudo — that path is already correct; do not add sudo there.
    - WR-07 fix: restore the canonical header casing. The verification audit should assert on `=== Unified Selection` or `Selected unified` header, not on case-sensitive `Selection` count. If preview_selection at 413 still has lowercased `selection:`, either restore it to `Selection:` and delete the duplicate audit line, or keep it but ensure the audited header is `=== Unified Selection`/`=== DRY RUN: Preview` and the grep in verify checks that header count ==1. Prefer removing the duplicate audit line that games grep -c Selection: keep one canonical header in the unified dispatcher output (Selected unified checklist) and make preview_selection use the same DRY RUN prefix without introducing a second Selection line. Document whichever approach in commit message.
    - Also mirror the install path's Termux gate for completeness: the uninstall DRY_RUN should not preview privileged keyd delete on Termux at all (IN-04).
    - Preserve DRY_RUN zero-writes contract: no filesystem writes, no sudo timestamp updates, no stow outside --no mode. Keep live uninstall path (863+) untouched — its sudo execution, typed-yes guard, and keyd reload remain.
    - Optional cheap cleanup if it does not enlarge diff: remove the duplicate General comments or gate notes that duplicate the install preview; do not delete the 549-line legacy checklist (WR-03) in this task unless the executor judges deletion trivially safe after the three gap fixes — if touched, delete 943-1491 and commit separately with history note, but do not block gap closure on it.
  </action>
  <verify>
    <automated>bash -n setup.sh</automated>
    <automated>bash -c 'sed -n "822,862p" ./setup.sh | grep -c "sudo stow.*--target=/.*--delete keyd" | xargs -I{} bash -c "echo sudo-occurrences-in-uninstall-DRY_RUN:{}"; sed -n "822,862p" ./setup.sh | grep "Would run: sudo stow.*--target=/.*--delete keyd" | grep -q "echo" && echo "✓ DRY_RUN privileged preview is echo-only" || (echo "✗ privileged preview not echo-only"; exit 1); sed -n "822,862p" ./setup.sh | grep -v "echo" | grep -q "sudo stow.*--target=/.*--delete keyd" && (echo "✗ bare sudo still executes under DRY_RUN"; exit 1) || echo "✓ no bare sudo execution in DRY_RUN uninstall"'</automated>
    <automated>bash -c 'grep -n "Would skip keyd.*Termux" ./setup.sh | head; sed -n "822,862p" ./setup.sh | grep -q "termux" && echo "✓ Termux gate mirrored in uninstall DRY_RUN" || (echo "✗ Termux gate missing in uninstall DRY_RUN"; exit 1); sed -n "822,862p" ./setup.sh | grep -q "rvaiya/keyd" && echo "✓ Debian no-binary gate mirrored" || (echo "✗ Debian gate missing"; exit 1)'</automated>
    <automated>bash -c 'FAMILY=termux DRY_RUN=true bash -c "source ./setup.sh; FAMILY=termux; DRY_RUN=true; SELECTED_PACKAGES=(nvim zsh nushell alacritty starship keyd); run_uninstall 2>&1" | tee /tmp/021-termux-uninstall-dry.txt | grep -q "Would skip keyd.*Termux" && echo "✓ Termux uninstall DRY_RUN skips privileged keyd" || (echo "✗ Termux skip missing"; cat /tmp/021-termux-uninstall-dry.txt; exit 1); cat /tmp/021-termux-uninstall-dry.txt | grep -v "echo" | grep -q "sudo stow.*keyd" && (echo "✗ sudo executed on Termux DRY_RUN"; exit 1) || echo "✓ no sudo on Termux DRY_RUN"'</automated>
    <automated>bash -c 'bash ./setup.sh --uninstall --mode local --shell zsh --yes --dry-run < /dev/null 2>&1 | tee /tmp/021-uninstall-dry.txt | grep -q "DRY RUN" && echo "✓ uninstall DRY_RUN exits with DRY RUN marker" || (echo "✗ uninstall DRY_RUN missing marker"; exit 1); cat /tmp/021-uninstall-dry.txt | grep -q "Would run: sudo stow.*--delete keyd" && echo "note: privileged Would run line present (echo-only)" || echo "note: no privileged Would run line (maybe keyd unticked or Termux/debian skip)"; bash -n ./setup.sh && echo "bash -n ok"'</automated>
    <automated>bash -c 'echo "=== Unified Selection header count ==="; bash ./setup.sh --mode local --shell zsh --dry-run < /dev/null 2>&1 | grep -c "Unified Selection" | xargs -I{} bash -c "test {} -eq 1 && echo ✓ single Unified Selection header || (echo ✗ Unified Selection count:{}; exit 1)"; bash ./setup.sh --mode server --shell zsh --dry-run < /dev/null 2>&1 | grep -c "Unified Selection" | xargs -I{} bash -c "test {} -eq 1 && echo ✓ server single header || (echo ✗ server count:{}; exit 1)"'</automated>
  </verify>
  <done>DRY_RUN uninstall preview prints Would run: sudo stow --target=/ --no --verbose --delete keyd via echo only, never executes sudo, crosses no privilege boundary, and correctly skips on Termux and on Debian without keyd binary; Unified Selection header appears exactly once on local and server dry-runs.</done>
  <acceptance_criteria>setup.sh DRY_RUN uninstall block (822-862) contains echo "[DRY RUN] Would run: sudo stow --target=/ --no --verbose --delete keyd" and contains no bare sudo stow execution line outside an echo; block contains FAMILY==termux skip and FAMILY==debian && ! command -v keyd skip mirroring preview_selection; sourced run_uninstall DRY_RUN with FAMILY=termux prints Would skip keyd — not available on Termux and executes no sudo; end-to-end bash setup.sh --uninstall --mode local --shell zsh --yes --dry-run prints DRY RUN and does not prompt for sudo password; bash setup.sh --mode local/server --shell zsh --dry-run each print Unified Selection exactly once.</acceptance_criteria>
  <reversibility rating="reversible">Echo-only change and added early-skips are display-only, revertible; live uninstall path unchanged.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-checklist→installer (WR-01/WR-02/IN-01) | Unified ticks flow into SELECTED_PACKAGES/SELECTED_DEPS; SELECTION_ACTIVE flag is trust anchor — empty array must not mean no-filter or escalate unticked deps into install set |
| installer→package-manager (WR-05) | Uninstall allowlist flows into pacman -Rns / apt remove -y / pkg uninstall; family-aware scoping prevents Debian apt remove of manually-built keyd (never apt-managed) |
| DRY_RUN preview→privilege boundary (WR-04/IN-04) | DRY_RUN must never execute sudo or stow --target=/ — only echo Would run; Termux and Debian no-binary gates must mirror install preview to keep preview honest |
| tui-stdout→installer | Five backend outputs remain untrusted allow-list matched input; task 1/3 preserve xargs -n1 + allow-list matching, never eval — no change here but preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-06 | Tampering/EoP | setup.sh:get_deps + reverify_deps + filter_deps_by_selection (WR-01/WR-02/IN-01) | high | mitigate | Introduce SELECTION_ACTIVE flag set by _split_unified_to_selected/_unified_apply_presets; make get_deps pure (remove inline filter); make reverify_deps and main branch on flag not array length; keep filter_deps_by_selection sole enforcement; gate with tick-authoritative fixtures proving unticked alacritty/keyd dropped even when SELECTED_DEPS empty and active |
| T-02.1-07 | Tampering | setup.sh:offer_system_package_removal _allowed (WR-05) | high | mitigate | Allowlist family-aware: local -a _allowed=("${ALL_TOOLCHAIN[@]}" alacritty); if [[ $FAMILY != debian ]]; then _allowed+=(keyd); fi; gate with sourced and end-to-end DRY_RUN fixtures proving debian never offers apt remove keyd, arch still does when ticked |
| T-02.1-08 | EoP/Denial of service | setup.sh:run_uninstall DRY_RUN privileged preview (WR-04/IN-04) | high | mitigate | Replace bare `sudo stow --target=/ --no --verbose --delete keyd` execution with `echo "[DRY RUN] Would run: sudo stow ... --delete keyd"` only; mirror TERMUX skip and debian no-binary skip from preview_selection; gate with no-bare-sudo grep plus Termux/debian DRY_RUN skip fixtures and end-to-end uninstall DRY_RUN marker check |
| T-02.1-SC | Tampering | npm/pip/cargo installs | low | accept | No new package-manager packages introduced; system packages from distro managers only; informational rvaiya/keyd URL never fetched |
</threat_model>

<verification>
Automated (all runnable non-interactively, <60s):
- bash -n setup.sh clean
- SELECTION_ACTIVE flag presence + get_deps purity (zero SELECTED_DEPS refs in 184-239) + reverify flag branch grep
- Tick-authoritative fixtures: active empty-toolchain selection keeps ticked alacritty/keyd and drops unticked stow; unticked alacritty/keyd dropped via filter_deps_by_selection even when SELECTED_DEPS empty but active
- Family-aware allowlist: sourced offer_system_package_removal DRY_RUN with FAMILY=debian prints no apt remove keyd, with FAMILY=arch prints pacman -Rns keyd
- End-to-end: FAMILY=debian bash setup.sh --uninstall --mode local --shell zsh --yes --dry-run has no apt remove keyd; bash setup.sh --mode local/server --shell zsh --dry-run and --uninstall dry-runs show single Unified Selection header and DRY RUN marker
- DRY_RUN privilege boundary: no bare sudo execution in run_uninstall DRY_RUN block (only echo Would run), Termux and debian gates mirrored and verified via sourced run_uninstall DRY_RUN skips
- Human check (end-of-phase per workflow.human_verify_mode): one interactive dry-run on a TUI backend confirming single tick-authoritative page before any write, cancel aborts without cascading, Termux disabled rows rendered — deferred to same cycle on a TTY
</verification>

<success_criteria>
All three failed truths now VERIFIED: (1) Every remaining installable app tick-authoritative including empty-toolchain selections — no empty bypass, single filter enforcement, reverify passes; (2) Debian with keyd ticked gets rvaiya/keyd pointer, never apt, and uninstall never offers apt remove keyd; (3) Five-backend ladder, cancel-never-cascades, Termux disabled emulation, --yes/no-TTY fast paths, DRY_RUN zero-writes with no sudo execution and mirrored Termux/Debian gates, strict mode, repo-root guard, and privileged keyd gate all behave exactly as before. 7/9 → 9/9; no regressions on previously passing 7 truths.
</success_criteria>

<output>
Create `.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-03-SUMMARY.md` when done
</output>
