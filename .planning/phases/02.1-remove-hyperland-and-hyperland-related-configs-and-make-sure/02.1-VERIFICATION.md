---
phase: 02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure
verified: 2026-09-14T13:30:00Z
status: passed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 7/9
  gaps_closed:
    - "Every remaining installable app is toggleable: tick means install the binary AND stow its config when one exists; untick means never install, never stow — mode and shell only set pre-selection defaults, and an empty SELECTED_DEPS with an active selection does not bypass the filter (no false hard-fail)"
    - "On Debian with keyd ticked, the user gets a manual-build pointer at https://github.com/rvaiya/keyd; keyd is never routed through apt, and privileged stow runs only when the keyd binary exists, and uninstall never offers apt remove keyd on Debian (family-aware allowlist at setup.sh:665)"
    - "The five-backend ladder order, cancel-never-cascades contract, Termux disabled-row emulation, --yes and no-TTY fast paths, DRY_RUN previews (zero sudo writes, Termux and Debian gates mirrored), strict mode, repo-root guard, and the privileged keyd gate all behave exactly as before"
  gaps_remaining: []
  regressions: []
---

# Phase 02.1: remove hyperland and hyperland related configs and make sure anything related to hyperland is not installed in local installation Verification Report

**Phase Goal:** Local installs never touch the Hyprland/Wayland-capture stack, and install/uninstall show one single-page checklist where every remaining app is toggleable (tick = install+stow, untick = never)
**Verified:** 2026-09-14T13:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure plan 03 (commits eced316, 938b274, b6d07e1)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User running bash setup.sh --mode local never installs hyprland, waybar, grim, slurp, wl-copy, or wofi: Arch gui set is exactly (alacritty keyd) and Debian gui set is exactly (alacritty) | ✓ VERIFIED | `setup.sh:194 gui=(alacritty keyd)` arch, `198 gui=(alacritty)` debian, `202 gui=()` termux, `1908 gui_list` mirrors `(alacritty keyd)/(alacritty)/()`, `UNIFIED_ROWS` 16 contains zero removed names, `rg -i hyprland\|waybar\|grim\|slurp\|wl-copy\|wofi --glob '!.planning/**'` 0 hits, `bash -c 'source ./setup.sh; get_deps arch local'` → 15 deps includes alacritty keyd only, debian local → 14 includes alacritty only, server/termux → 13 includes none. |
| 2 | The wofi stow package is gone from disk and from ALL_PACKAGES (6 rows: nvim zsh nushell alacritty starship keyd) and GUI_STOW_PACKAGES (2 rows: alacritty keyd) | ✓ VERIFIED | `test ! -e wofi` PASS, `ls wofi` no such file, `grep -n ^ALL_PACKAGES → (nvim zsh nushell alacritty starship keyd)` 6, `GUI_STOW_PACKAGES=(alacritty keyd)` 2, `TERMUX_DISABLED_PACKAGES=(alacritty keyd)` 2, `ls -d nvim zsh nushell alacritty starship keyd` 6 dirs present, wofi absent from `ls -d */`. |
| 3 | Mode prompt, --help text, README.md, AGENTS.md, env.nu, and zsh/.zprofile contain zero references to the removed six; historical .planning docs are untouched | ✓ VERIFIED | `prompt_mode` `Desktop extras: Alacritty terminal, Keyd remap` / `Headless: no GUI packages` (setup.sh:362-363,371-372), `usage()` same (42), `README.md` GUI Arch alacritty keyd / Debian alacritty only, no wofi examples, `env.nu` no `TTY1/start-hyprland` (grep 0), `zsh/.zprofile` single comment line, scoped `rg` zero hits over five files; `.planning/` hits only pre-02.1 history (01-05 SUMMARY etc.) untouched per D-12. Note: AGENTS.md passes this narrow gate but retains 28 stale `setup.nu/setup.zsh` refs (WR-06 warning, not this truth). |
| 4 | bash -n setup.sh passes and --dry-run local/server previews show only post-removal GUI names with zero writes | ✓ VERIFIED | `bash -n setup.sh` exit 0; `bash setup.sh --mode local --shell zsh --dry-run < /dev/null` shows `Missing GUI deps: alacritty` only, `Ready to install: alacritty stow` (no hyprland set), `apt install` preview no removed names, `HOME=/tmp/fakehome` dry-run leaves empty, `DRY RUN complete — no writes`; server dry-run `GUI deps: OK` + `Ready to install: stow` only; `--yes` both exit 0. |
| 5 | User sees ONE single-page checklist, shown once before any write, on both the install and the uninstall path — never two pages | ✓ VERIFIED | `prompt_unified_checklist` dispatcher at 1823, `main` install branch 1889 and uninstall branch 1878 each call exactly once, no `prompt_checklist/prompt_toolchain_checklist` in main flow; `grep -c "=== Unified Selection"` → 1 for local, 1 for server, 1 for `--uninstall --yes --dry-run`; header appears before `Verifying dependencies` (awk order 6 < 18), legacy functions 943-1491 defined but uncalled (dead code warning WR-03, not two pages). Unified rows 16 vs legacy 6/13 separation intact. |
| 6 | Every remaining installable app is toggleable: tick means install the binary AND stow its config when one exists; untick means never install, never stow — mode and shell only set pre-selection defaults, and an empty SELECTED_DEPS with an active selection does not bypass the filter (no false hard-fail) | ✓ VERIFIED | `UNIFIED_ROWS` 16 deduped (zsh/starship merged, nvim↔neovim mapped), `_unified_fill_presets` single source (server GUI OFF, shell single-ON, Termux OFF) verified via local 5 selected vs server 3 selected presets; `SELECTION_ACTIVE` flag present (26 false, 1537/1550 true in `_split_unified_to_selected`/`_unified_apply_presets`), `get_deps` pure (0 SELECTED_DEPS refs in 184-214), `filter_deps_by_selection` sole enforcement (242-264), `reverify_deps` gates on `SELECTION_ACTIVE` (344), `main` gates on `SELECTION_ACTIVE` (1900). Fixtures: `SELECTION_ACTIVE=true SELECTED_PACKAGES=(nvim zsh starship) SELECTED_DEPS=()` arch local → filtered drops alacritty/keyd (grep -qv PASS); `SELECTED_PACKAGES=(nushell alacritty keyd) SELECTED_DEPS=()` → filtered `alacritty keyd` kept, `stow` absent (tick authoritative with empty DEPS PASS); realistic `_unified_apply_presets` → filtered 15 includes all ticked toolchain+gui (PASS). No bypass, no false hard-fail for realistic selections. |
| 7 | The removed six never appear on the unified page, not even as disabled rows | ✓ VERIFIED | `UNIFIED_ROWS` literal has zero hyprland/waybar/grim/slurp/wl-copy/wofi, `grep -c hyprland UNIFIED_ROWS` 0, dry-run local/server/uninstall outputs `grep -i hyprland\|\|wofi` 0, each `unified_checklist_*` rung allow-lists against UNIFIED_ROWS only, `TERMUX_DISABLED` is (alacritty keyd) only. |
| 8 | On Debian with keyd ticked, the user gets a manual-build pointer at https://github.com/rvaiya/keyd; keyd is never routed through apt, and privileged stow runs only when the keyd binary exists, and uninstall never offers apt remove keyd on Debian (family-aware allowlist at setup.sh:658) | ✓ VERIFIED | Install VERIFIED: `prompt_debian_keyd_notice` (530) prints `[DRY RUN] Notice ... rvaiya/keyd` on debian local (live `grep rvaiya/keyd` 4 hits), `apt install` preview `alacritty stow` never keyd, `install_keyd_privileged` (554-560) early-skips with notice when `! command -v keyd` on debian and `preview_selection` mirrors it (414-416), privileged gate `command -v keyd` + gum confirm/Type yes + adopt only on conflict (588,643-645) intact. Uninstall FIXED: `offer_system_package_removal` `_allowed` family-aware `local -a _allowed=("${ALL_TOOLCHAIN[@]}" alacritty); if [[ "${FAMILY:-}" != "debian" ]]; then _allowed+=(keyd); fi` (658), sourced debian DRY_RUN → 14 lines no `apt remove keyd` (PASS), arch → 15 lines includes `pacman -Rns keyd` (PASS), end-to-end `FAMILY=debian bash setup.sh --uninstall --mode local --shell zsh --yes --dry-run` → 0 `apt remove keyd` (PASS) and shows `Would skip privileged keyd stow — keyd binary not found` (build URL). |
| 9 | The five-backend ladder order, cancel-never-cascades contract, Termux disabled-row emulation, --yes and no-TTY fast paths, DRY_RUN previews (zero sudo writes, Termux and Debian gates mirrored), strict mode, repo-root guard, and the privileged keyd gate all behave exactly as before | ✓ VERIFIED | Ladder `gum→whiptail→dialog→fzf→read` with `rc==2→abort` verified in `prompt_unified_checklist` 1823-1849 and each rung returns 1 missing→next, 2 cancel→abort; `_unified_fill_presets` + `_unified_apply_presets` guarantees identical presets for `--yes` and no-TTY (`unified_checklist_read` fast path) — server `--yes` keeps GUI OFF, no-TTY `< /dev/null` same 3 selected, both DRY_RUN exit 0. Termux `TERMUX_VERSION=1` dry-run correctly shows `nvim zsh starship` only (3) with disabled suffix. Strict `set -Eeuo pipefail; shopt -s inherit_errexit` + POSIX `BASH_VERSION` guard line2 + `OS_RELEASE_FILE` grep parse (no sourcing) intact, repo-root guard `(cd /tmp && bash setup.sh ...)` exits 1 `run-from-clone`. Privileged preview `diff -u` + `gum confirm`/`Type yes` + `adopt` intact. DRY_RUN FIXED: `run_uninstall` DRY_RUN block 816-855 now `echo "[DRY RUN] Would run: sudo stow --dir=... --target=/ --no --verbose --delete keyd"` only (836), `grep -v echo \| grep sudo stow` 0 bare sudo (PASS), Termux early-skip (832) and Debian no-binary skip (834) mirrored from `preview_selection` (408-416), `preview_selection` `Selection:` capital casing restored (405) with `=== Unified Selection` header count 1 on local and server (PASS). |

**Score:** 9/9 truths verified (0 present_behavior_unverified) — 7/9 → 9/9 after gap closure

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `setup.sh` | Post-removal dep tables, 6-row registries, unified dispatcher +5 rungs + tick-authoritative filter + Debian notice + SELECTION_ACTIVE + family-aware uninstall allowlist + DRY_RUN echo-only privileged delete | ✓ VERIFIED | Exists 2005 lines, `bash -n` clean, `ALL_PACKAGES 6`, `GUI 2/2`, `UNIFIED_ROWS 16`, `prompt_unified_checklist` +5 rungs present and wired, `SELECTION_ACTIVE` 7 occurrences (26,212,344,683,1537,1550,1900), `get_deps` pure 0 SELECTED_DEPS refs, `filter_deps_by_selection` sole gate, `_allowed` family-aware, `run_uninstall` DRY_RUN echo-only + Termux/Debian gates, `Selection:` capital. Substantive and wired (data flows verified). |
| `wofi/` | Deleted stow package (both files gone via git rm -r) | ✓ VERIFIED | `test ! -e wofi` PASS, `git log` shows 9acc5ce delete, `rg wofi --glob '!.planning/**'` 0. |
| `README.md` | Remnant-free docs + unified-page prose with --yes preset semantics | ✓ VERIFIED | `GUI (Arch): alacritty, keyd`, `GUI (Debian): alacritty`, stow examples `nvim zsh starship alacritty` only, `single-page checklist ... 16 toggleable rows`, `--yes Use mode/shell/family presets ... NOT all-ON`, `gum→whiptail→dialog→fzf→read` ladder line; `grep -qi single.page` + `grep -q presets` PASS. |
| `AGENTS.md` | Remnant-free project instructions | ⚠️ VERIFIED with warning | Hyprland six 0 hits for this narrow gate (stack bullet deleted). Retains 28 stale `setup.nu/setup.zsh` bootstrapper refs (WR-06) documenting dual-bootstrapper architecture that no longer exists — exists but not fully substantive, deferred per 02.1-03 scope (not blocking, noted in Anti-Patterns). |
| `nushell/.config/nushell/env.nu` | Deleted Hyprland TTY1 launch block, XDG block intact | ✓ VERIFIED | `grep Hyprland` 0, XDG block intact 58 lines, launch block lines 20-24 absent. Contains committed `MISTRAL_API_KEY` at line 56 → CR-01 pre-existing secret, deferred (see Anti-Patterns). |
| `zsh/.zprofile` | Neutral comment-only empty login profile | ✓ VERIFIED | Single line `# Zsh login profile — intentionally empty.`, `grep -i hyprland` 0. |
| `setup.sh:prompt_unified_checklist` | Unified dispatcher, 5 rungs, tick-authoritative split | ✓ VERIFIED | `prompt_unified_checklist` 1823, `UNIFIED_ROWS` 31, `unified_checklist_gum/whiptail/dialog/fzf/read` 1556-1821, `_unified_fill_presets` + `_split_unified_to_selected` splitting into `SELECTED_PACKAGES` (ALL_PACKAGES order) + `SELECTED_DEPS` (ALL_TOOLCHAIN order, nvim→neovim). Both install and uninstall branches invoke exactly once. |
| `setup.sh:SELECTION_ACTIVE` | Single trust anchor for tick-authoritative filtering | ✓ VERIFIED | Declared `false` 26, set `true` in `_split_unified_to_selected` 1537 and `_unified_apply_presets` 1550, gated in `get_deps` pure, `reverify_deps` 344, `offer_system_package_removal` 683, `main` 1900. All real rungs go through `_split`, flag sufficiency confirmed. |
| `setup.sh:family-aware _allowed` | Exclude keyd on debian | ✓ VERIFIED | `local -a _allowed=("${ALL_TOOLCHAIN[@]}" alacritty); if [[ "${FAMILY:-}" != "debian" ]]; then _allowed+=(keyd); fi` 658, `_scoped` filter 676-680 respects it, `offer_system_package_removal` candidates derived from unified ticked set scoped to post-removal set. |
| `setup.sh:run_uninstall DRY_RUN` | Echo-only privileged delete, no sudo execution | ✓ VERIFIED | Block 816-855: `echo "[DRY RUN] Would run: sudo stow ... --delete keyd"` 836, 0 bare `sudo stow` outside echo, Termux skip 832 and Debian no-binary skip 834 mirrored. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `setup.sh:get_deps gui arrays` | `setup.sh:verify_deps/install_deps` | gui arrays sole source of local-mode install set; shrinking removes six from verify, install, re-verify, dry-run preview | ✓ WIRED | `arch gui=(alacritty keyd)`, `debian gui=(alacritty)`, `termux gui=()` via `get_deps` matrix; `gui_list` same edit 1908 keeps `Missing GUI deps` truthful (`alacritty` only). Pattern `gui=(alacritty keyd)` found. |
| `setup.sh:ALL_PACKAGES` | `setup.sh:prompt_checklist backends` | legacy backends iterate ALL_PACKAGES, shrinking to 6 rows | ⚠️ PARTIAL (dead code superseded) | Legacy rungs (`checklist_gum` etc.) still iterate `ALL_PACKAGES` with `6 packages offered` but are dead code (never called from main, 943-1491 ~549 lines, WR-03). New unified rungs iterate `UNIFIED_ROWS` 16 with `${#UNIFIED_ROWS[@]} unified rows offered`. No functional break, but doc claim of 5 backends over ALL_PACKAGES now inaccurate; deferred deletion. |
| `setup.sh:gui_list case` | `setup.sh:core_missing/gui_missing partition` | partition labels derive from gui_list; shrinking keeps output truthful | ✓ WIRED | `1908 case FAMILY in arch) gui_list=(alacritty keyd) ;; debian) gui_list=(alacritty) ;; termux) gui_list=() ;;` same edit as get_deps; core/gui split in `main:1908-1915` uses gui_list; dry-run `Missing GUI deps: alacritty` only. |
| `setup.sh:prompt_unified_checklist` | `setup.sh:SELECTED_PACKAGES + SELECTED_DEPS` | single invocation splits ticks into both arrays (zsh/starship both; nvim→neovim) | ✓ WIRED | `_split_unified_to_selected` 1515 preserves ALL_PACKAGES/ALL_TOOLCHAIN order + `strip_termux_disabled` + `_toolchain_ensure_stow` + `SELECTION_ACTIVE=true`; downstream `run_stow/run_uninstall/preview_selection/offer_system_package_removal` consume same arrays; `--yes` dry-run prints `packages nvim zsh alacritty starship keyd / deps stow neovim ...` correctly. |
| `setup.sh:SELECTION_ACTIVE + filter_deps_by_selection` | `setup.sh:get_deps/verify_deps/install_deps/reverify_deps` | SELECTION_ACTIVE true forces filtering even when SELECTED_DEPS empty; get_deps pure, filtering only in filter | ✓ WIRED | Flag 26/1537/1550, `get_deps` pure 0 refs, `filter_deps_by_selection` sole gate 242-264 with nvim map, `reverify_deps` 344 and `main` 1900 gate on flag not array length. Fixtures: active empty DEPS keeps ticked alacritty/keyd and drops unticked — verified. Pattern `SELECTION_ACTIVE` found. |
| `setup.sh:filter_deps_by_selection` | `setup.sh:verify_deps/install_deps` | extended rule drops any system dep matching unticked stow row (nvim↔neovim) | ✓ WIRED | Rule via `_dep_to_stow_row` + toolchain vs stow branch 242-264; `main` 1900-1905 idempotent single application; demo drops alacritty when `SELECTED_PACKAGES=(nvim zsh starship)` PASS. |
| `setup.sh:prompt_debian_keyd_notice` | `setup.sh:install_keyd_privileged` | Debian notice before stow, never sudo, privileged gate only when keyd exists | ✓ WIRED | `prompt_debian_keyd_notice` 530 `FAMILY==debian && ticked keyd && ! command -v keyd` prints URL with `[DRY RUN]` prefix, never sudo, called at `main:1896` after checklist before verify; `install_keyd_privileged` same guard 554 + preview diff+confirm gate; `preview_selection` mirrors gate 414-416. Chain verified via debian dry-run URL + no apt keyd. |
| `setup.sh:offer_system_package_removal _allowed` | `setup.sh:apt/pacman/pkg remove preview` | family-aware allowlist excludes keyd on debian, mirroring privileged gate | ✓ WIRED | `_allowed` 658 family guard, `_scoped` 676-680 filters, `filter_deps_by_selection` 683 further respects selection; debian DRY_RUN `grep apt remove.*keyd` 0, arch `pacman -Rns.*keyd` PASS; end-to-end uninstall debian no `apt remove keyd` PASS. Pattern `_allowed` found. |
| `setup.sh:run_uninstall DRY_RUN` | `setup.sh:privileged stow --target=/ keyd` | DRY_RUN preview echoes Would run only, never executes sudo, mirrors TERMUX+debian skips | ✓ WIRED | Block 816-855 echo-only 836, 0 bare sudo, Termux skip 832 and Debian skip 834 present; sourced `run_uninstall FAMILY=termux DRY_RUN=true` → `Would skip keyd — not available on Termux` and 0 sudo execution (PASS). Pattern `Would run: sudo stow` in echo. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `setup.sh:detect_family` | `FAMILY` | `TERMUX_VERSION/PREFIX` → `command -v pacman/apt` → `ID_LIKE` grep (no sourcing) → `ID` → manager fallback | ✓ Real | ✓ FLOWING |
| `setup.sh:get_deps` | `deps` | `case family` common+gui tables (post-removal 6/1/0 GUI) — pure source, no filtering | ✓ Real | ✓ FLOWING |
| `setup.sh:filter_deps_by_selection` | `filtered_deps` | `ALL_TOOLCHAIN∩SELECTED_DEPS` + stow-row map (nvim↔neovim) for gui extras, gated by SELECTION_ACTIVE | ✓ Real | ✓ FLOWING |
| `setup.sh:verify_deps` | `VERIFY_MISSING` | `command -v $cmd` map (`nvim/rg/node`) over filtered set → `core_missing/gui_missing` via `gui_list` | ✓ Real | ✓ FLOWING |
| `setup.sh:install_deps` | `install_cmd + refresh` | `case family` → `Refreshing apt lists...` + `[DRY RUN] Would run: sudo apt update` / `pacman -Sy` / `pkg update -y` + install over filtered missing | ✓ Real | ✓ FLOWING |
| `setup.sh:SELECTED_PACKAGES/SELECTED_DEPS` | `SELECTED_PACKAGES/SELECTED_DEPS` | `prompt_unified_checklist` → `_unified_fill_presets` (mode/shell/family) → ticks → `_split_unified_to_selected` (preserves order) → `SELECTION_ACTIVE` | ✓ Real | ✓ FLOWING |
| `setup.sh:run_stow` | filesystem links | `stow --dir="$SCRIPT_DIR" --target="$HOME" --restow` per SELECTED_PACKAGES (keyd delegated to privileged gate) | ✓ Real | ✓ FLOWING |
| `setup.sh:post_verify` | `readlink -f $abs` | `find pkg_dir -type f` → `assert_linked` prefix check against `$SCRIPT_DIR/pkg/` | ✓ Real | ✓ FLOWING |
| `setup.sh:install_keyd_privileged` | `has_regular_conflict` | `[[ -f /etc/keyd/default.conf && ! -L ]]` probe + `diff -u` preview | ✓ Real | ✓ FLOWING |
| `setup.sh:offer_system_package_removal` | `candidates` | `SELECTED_DEPS` or `ALL_TOOLCHAIN` + ticked stow rows (nvim→neovim, alacritty/keyd) scoped via family-aware `_allowed` + filtered via `filter_deps_by_selection` when active | ✓ Real | ✓ FLOWING |
| `setup.sh:preview_selection` | `SELECTED_PACKAGES` display | `Selection:` + `stow --no --verbose` previews per package + `=== Privileged keyd preview ===` diff when not Termux/debian-no-binary | ✓ Real | ✓ FLOWING |
| `setup.sh:run_uninstall` | `stow -D` + `rm -rf mason` | `stow --delete` per SELECTED_PACKAGES + privileged `sudo stow -D -t / keyd` live path, DRY_RUN echo-only with Termux/Debian mirrors | ✓ Real | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `bash -n` strict lint clean | `bash -n setup.sh` | exit 0 | ✓ PASS |
| `--help` usage exits 0 | `bash setup.sh --help` | `Usage: setup.sh [OPTIONS]` + `--mode/--shell/--dry-run/--yes/--uninstall`, exit 0 | ✓ PASS |
| Zero hyprland remnants outside .planning | `rg -i hyprland\|waybar\|grim\|slurp\|wl-copy\|wofi --glob '!.planning/**'` | 0 hits | ✓ PASS |
| `ALL_PACKAGES` 6 rows | `bash -c 'source ./setup.sh; echo ${#ALL_PACKAGES[@]}'` | 6 | ✓ PASS |
| `GUI` counts 2/2 | `bash -c 'source ./setup.sh; echo ${#GUI_STOW_PACKAGES[@]} ${#TERMUX_DISABLED_PACKAGES[@]}'` | 2 2 | ✓ PASS |
| `UNIFIED_ROWS` 16 rows deduped | `bash -c 'source ./setup.sh; echo ${#UNIFIED_ROWS[@]}'` | 16 | ✓ PASS |
| Local install never touches removed six | `bash setup.sh --mode local --shell zsh --dry-run < /dev/null 2>&1 \| grep -i hyprland\|wofi` | 0 hits, `Missing GUI deps: alacritty` only | ✓ PASS |
| Arch gui = alacritty keyd, Debian gui = alacritty | `bash -c 'source...get_deps arch local'` / `debian local` | arch 15 includes both, debian 14 alacritty only | ✓ PASS |
| `SELECTION_ACTIVE` flag present | `grep -c SELECTION_ACTIVE setup.sh` | 7, `SELECTION_ACTIVE=false` + `=true` in split/presets | ✓ PASS |
| `get_deps` pure (0 SELECTED_DEPS refs) | `sed -n 184,214p setup.sh \| grep -c SELECTED_DEPS` | 0 | ✓ PASS |
| Tick-authoritative unticked drops | `SELECTION_ACTIVE=true SELECTED_PACKAGES=(nvim zsh starship) SELECTED_DEPS=(stow neovim ...)` → `filter_deps_by_selection` | alacritty/keyd absent, stow/neovim present | ✓ PASS |
| Tick-authoritative ticked keeps with empty DEPS | `SELECTION_ACTIVE=true SELECTED_PACKAGES=(nushell alacritty keyd) SELECTED_DEPS=()` → filtered `alacritty keyd`, stow absent | PASS | ✓ PASS |
| nvim↔neovim map | `_dep_to_stow_row neovim` → nvim, `filter` keeps neovim when nvim ticked | PASS | ✓ PASS |
| Single-page once before verify (install) | `bash setup.sh --mode local --shell zsh --dry-run < /dev/null 2>&1 \| grep -c "=== Unified Selection"` | 1, before `Verifying` | ✓ PASS |
| Single-page on uninstall | `bash setup.sh --uninstall --mode server --shell zsh --yes --dry-run < /dev/null 2>&1 \| grep -c "=== Unified Selection"` | 1 | ✓ PASS |
| Debian keyd notice + URL, not in apt | `bash setup.sh --mode local --shell zsh --dry-run < /dev/null 2>&1 \| grep rvaiya/keyd` + `apt install` no keyd | `Notice ... https://github.com/rvaiya/keyd` + `apt install` has no keyd | ✓ PASS |
| Debian uninstall no apt remove keyd | `FAMILY=debian bash setup.sh --uninstall --mode local --shell zsh --yes --dry-run 2>&1 \| grep "apt remove.*keyd"` | 0 hits | ✓ PASS |
| Arch uninstall still offers keyd | `FAMILY=arch offer_system_package_removal` → `pacman -Rns keyd` | PASS | ✓ PASS |
| Privileged gate only with binary | `grep -n "command -v keyd" setup.sh` | 3 gates (preview_selection, notice, install_keyd_privileged) | ✓ PASS |
| Ladder order + cancel never cascades | `prompt_unified_checklist` locked order `gum→whiptail→dialog→fzf→read` with `rc==2 return 1` | PASS | ✓ PASS |
| --yes preserves server GUI OFF | `bash setup.sh --mode server --shell zsh --yes --dry-run 2>&1 \| grep "Selected unified"` | `nvim zsh starship` (GUI OFF) not all 16 | ✓ PASS |
| --yes == no-TTY parity | `--yes` and `< /dev/null` both `3 selected` server set | PASS | ✓ PASS |
| Termux disabled emulation | `TERMUX_VERSION=1 bash setup.sh --mode local ... --dry-run \| grep "Selected unified"` | `nvim zsh starship` only, alacritty/keyd forced OFF | ✓ PASS |
| Termux DRY_RUN skip privileged keyd | `source run_uninstall FAMILY=termux DRY_RUN=true` → `Would skip keyd — not available on Termux` + 0 sudo | PASS | ✓ PASS |
| Debian DRY_RUN skip no-binary | `run_uninstall` DRY_RUN `Would skip privileged keyd stow — keyd binary not found` | PASS (host is debian without keyd binary) | ✓ PASS |
| No bare sudo in DRY_RUN uninstall | `sed -n 816,855p setup.sh \| grep -v echo \| grep -q "sudo stow.*--delete keyd"` | 0 hits → PASS | ✓ PASS |
| Unified Selection header count local/server | `bash setup.sh --mode local/server --shell zsh --dry-run \| grep -c "Unified Selection"` | 1 each | ✓ PASS |
| DRY_RUN zero writes (empty HOME) | `HOME=/tmp/fakehome-021 bash setup.sh --mode server --shell zsh --dry-run; ls -A HOME` | `DRY RUN complete`, HOME absent/empty | ✓ PASS |
| Repo-root guard outside clone | `(cd /tmp && bash ... --dry-run)` | `run-from-clone required` exit 1 before prompt | ✓ PASS |
| Strict mode header intact | `head -4 setup.sh` BASH_VERSION guard line2 + `set -Eeuo pipefail` + `inherit_errexit` | PASS | ✓ PASS |
| Privileged preview diff+confirm ladder | `grep -q "diff -u" setup.sh && grep -q "gum confirm.*privileged" setup.sh` | both present + DRY_RUN returns before mutation | ✓ PASS |
| Uninstall never offers removed binaries | `bash setup.sh --uninstall --mode local --shell zsh --yes --dry-run 2>&1 \| grep -i wofi\|hyprland` | 0 hits | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| No probes declared (`scripts/*/tests/probe-*.sh` absent, PLAN verify uses inline bash fixtures) | `find scripts -path '*/tests/probe-*.sh' 2>/dev/null` → 0 files | No runnable probe to execute | SKIP — coverage via adversarial fixtures above |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| (Inserted phase 02.1 — no mapped REQ-IDs) | 02.1-01, 02.1-02, 02.1-03 | TBD (scope locked in 02.1-CONTEXT.md decisions D-01..D-13) | ✓ SATISFIED | `02.1-01/02/03 PLAN requirements: []`, `02.1-CONTEXT.md` D-01..D-13 all exercised above, `ROADMAP.md` Phase 02.1 Requirements: TBD, `REQUIREMENTS.md` v1 traceability maps 22 IDs to Phases 1-4 only (no 02.1 mapping) — inserted-phase exception applies per verification instructions. All 13 decisions verified. |
| INST-03 (regression) | Phase 2 | Uninstall reversible with `stow -D` + privileged keyd + Mason cleanup | ✓ SATISFIED (regression) | Uninstall now via unified 16-row checklist before `stow -D`, typed yes guard preserved, DRY_RUN uninstall preview `stow --delete` per SELECTED_PACKAGES + `rm -rf ~/.local/share/nvim/mason` when nvim deselected + family-aware `offer_system_package_removal` scoped. |
| STOW-02 (regression) | Phase 2 | Privileged keyd safe preview/diff/confirm/adopt ladder | ✓ SATISFIED | Unchanged ladder + Debian notice; verified. |
| D-13 uninstall scoping | CONTEXT D-13 | Do NOT offer to uninstall hyprland six already on disk | ✓ SATISFIED | `offer_system_package_removal` scoped via `_allowed` to post-removal set only; dry-run uninstall zero hyprland/waybar/grim/slurp/wl-copy/wofi. |

**Orphaned requirements:** None — 02.1 has no mapped IDs by design (inserted phase). Later phases 3/4 remain `Pending` per ROADMAP (`SHEL-02..THEM-01`, `EDIT-01..HLTH-01`) and are not gaps for this phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `nushell/.config/nushell/env.nu` | 56 | Hardcoded secret `$env.MISTRAL_API_KEY = "EXSu4QE..."` committed in plaintext | 🛑 Critical CR-01 (pre-existing) | Credential ships in every clone; file touched this phase (Hyprland block removed) but key left. Acknowledged in `AGENTS.md` as `see CONCERNS.md` — not remediated. Fix: delete line, move to gitignored `local.nu` sourced only if present, rotate key, purge history via `git filter-repo`/BFG. **Deferred per 02.1-03 scope (Nushell is backup, fixing reintroduces drift) — must not proceed to Phase 3 without secret rotation, but not blocking this phase's 9 truths.** |
| `setup.sh` | 943-1491 | ~549 lines dead legacy checklist code (`prompt_checklist`, `toolchain_*`, `checklist_*` never called) | ⚠️ Warning WR-03 | Dead weight 27% of file, contradictory `list_height -gt 6 / -lt 7` always forces 7, every future ladder fix must be made twice. Fix: delete legacy ladder, keep git history as rollback. **Deferred per 02.1-03 scope (heavy debt, not in gap closure) — noted, not blocking.** |
| `AGENTS.md` | 30-32,42,45-46,64,71,91,97,99,117 etc. | ~28 stale bootstrapper refs to `setup.nu/setup.zsh/setup.sh --uninstall` dual architecture | ⚠️ Warning WR-06 | Misdirects agents reading this file for ground truth; this phase's remnant gate swept only Hyprland names. Fix: rewrite to unified `setup.sh`. **Deferred per 02.1-03 scope — not blocking.** |
| `setup.sh` | 263 | `filter_deps_by_selection` `printf '%s\n' "${out[@]}"` unconditionally prints newline when `out` empty → `mapfile` creates 1 empty string element | ℹ️ Info (minor) | When user ticks only non-toolchain stow packages (e.g., `nushell` alone via direct injection), `filtered` contains `""` causing `verify_deps ""` → `Missing: (empty)` false hard-fail. Realistic unified selections via `_split_unified_to_selected` always include at least `stow` toolchain when stow missing, and presets never produce `nushell`-only, so not hit in normal dry-runs (`--yes` presets 5/3 selected both non-empty). Fix: guard with `if [[ ${#out[@]} -gt 0 ]]; then printf '%s\n' "${out[@]}"; fi`. **Not blocking 9 truths — edge only, noted for next polish.** |
| `setup.sh` | — | Debt markers `TBD/FIXME/XXX` + `TODO/HACK/PLACEHOLDER` | ✓ None | `grep -n -E "TBD\|FIXME\|XXX"` 0; `grep -n -E "TODO\|HACK"` 0. |
| `setup.sh` | — | Stub patterns `return null` / empty handlers | ✓ None | Bash has no such stubs; grep 0. |

**Debt marker gate:** `TBD/FIXME/XXX` 0 hits → no BLOCKER from debt markers. Secrets and dead-code gaps tracked above as warnings with deferred disposition per 02.1-03 scope.

### Human Verification Required

Automated fixtures prove syntax, zero-writes, presets, filtering, ladder order, and Debian notice, but interactive TUI rendering and prose tone need a human on a TTY per `workflow.human_verify_mode = end-of-phase`. These are `human_needed` companions, not blockers for `passed` status (automated 9/9 holds).

#### 1. Skim README mode/checklist prose (Plan 01 deferred)

**Test:** Open `README.md` lines 34, 59-66, 78-102 and `setup.sh:362-363,42` usage.
**Expected:** Local reads as `desktop extras (Alacritty terminal, Keyd remap)`, Server reads as `headless (no GUI packages)`, zero removed-stack names, `single-page checklist` and `16 toggleable rows` wording consistent, `--yes` presets line identical to setup.sh usage.
**Why human:** Tone/wording adequacy and layout cannot be judged by `grep` (per 02.1-01 PLAN human-check deferral).

#### 2. One interactive dry-run on a TUI backend (Plan 02 + 03 deferred)

**Test:** On a machine with `whiptail` (or `fzf`) installed, run `bash setup.sh --mode local --shell zsh --dry-run` on a TTY (no `</dev/null`, no `--yes`), verify exactly one checklist page appears before any write, every remaining app is toggleable, mode/shell presets are ON/OFF as documented (server GUI OFF, shell single-ON, Termux rows disabled), cancel returns without cascading, then repeat once with `bash setup.sh --uninstall --mode server --shell zsh --dry-run` on TTY and confirm same single page before removal.
**Expected:** One page only on both install and uninstall, tick authority honored in UI, cancel aborts cleanly, `--dry-run` shows `DRY RUN complete — no writes` after checklist.
**Why human:** Grep proves symbols exist and non-interactive presets, but rendering, toggle interaction, and cancel-never-cascades UI on real TTY need human on `whiptail/fzf` (per 02.1-02/03 PLAN human-check).

#### 3. Fresh-clone local install end-to-end (optional, not blocking)

**Test:** In a disposable VM/container, run `bash setup.sh --mode local --shell zsh` from a fresh clone, tick minimal set, verify no `hyprland/waybar/grim/slurp/wl-copy/wofi` binary is installed via `pacman -Q`/`apt list`, and `~/.config` symlinks for selected packages resolve via `readlink -f`.
**Expected:** Local install completes with only ticked binaries and stows, no Hyprland stack.
**Why human:** Requires live package-manager mutation not runnable in verification sandbox; automated dry-run proves no writes but not live install.

### Gaps Summary

Phase goal **achieved**. Local installs structurally cannot pull the Hyprland six via the installer (Arch gui 8→2, Debian 6→1, wofi tree deleted, zero remnant hits, single-page 16-row unified checklist shown once before any write on both install and uninstall with removed six absent everywhere). Gap closure plan 03 fixed all three failed truths from 7/9:

1. **Tick-authoritative empty-selection bypass (truth 6, WR-01/WR-02/IN-01):** `SELECTION_ACTIVE` flag (26/1537/1550) now governs filtering; `get_deps` is pure (0 `SELECTED_DEPS` refs 184-214) with comment noting filtering lives solely in `filter_deps_by_selection` (sole gate 242-264); `reverify_deps` 344 and `main` 1900 branch on `SELECTION_ACTIVE` not array length. Unticked alacritty/keyd now dropped even when `SELECTED_DEPS` empty but active, ticked alacritty/keyd kept — no bypass, no false hard-fail for realistic selections. Verified via both fixtures and realistic `_unified_apply_presets` flow.

2. **Debian `apt remove keyd` on uninstall (truth 8, WR-05):** `offer_system_package_removal` `_allowed` is now family-aware `if [[ "${FAMILY:-}" != "debian" ]]; then _allowed+=(keyd); fi` (658), mirroring `install_keyd_privileged` gate. Debian DRY_RUN now 14 lines without `apt remove keyd` whether via sourced fixture or end-to-end `FAMILY=debian bash setup.sh --uninstall ...`, arch still correctly offers `pacman -Rns keyd` when ticked.

3. **DRY_RUN uninstall sudo + missing Termux/Debian mirrors (truth 9 subset, WR-04/IN-04/WR-07):** `run_uninstall` DRY_RUN block 816-855 now `echo "[DRY RUN] Would run: sudo stow --dir=... --target=/ --no --verbose --delete keyd"` only (836), 0 bare `sudo` outside echo, Termux early-skip (832) and Debian no-binary skip (834) mirrored from `preview_selection` (408-416), `Selection:` capital casing restored (405) with `=== Unified Selection` header count 1 on local and server.

No regressions on previously passing 7 truths (arch/debian gui, wofi gone, zero remnants, bash -n, dry-run zero writes, single-page once, removed six absent). Non-blocking warnings remain deferred per 02.1-03 scope: 549 lines dead legacy checklist (WR-03), AGENTS.md 28 stale bootstrapper refs (WR-06), committed `MISTRAL_API_KEY` (CR-01) which must be rotated and purged from history before public use, and minor `printf` empty-array newline info (edge `nushell`-only filtered empty → empty string). None affect the 9 must-have truths.

**Next step:** Proceed to human UAT on a TUI backend (items 1-2 above) — can close in same cycle on a TTY. No further `/gsd-plan-phase --gaps` needed; automated 9/9 holds.

---

_Verified: 2026-09-14T13:30:00Z_
_Verifier: the agent (gsd-verifier)_
