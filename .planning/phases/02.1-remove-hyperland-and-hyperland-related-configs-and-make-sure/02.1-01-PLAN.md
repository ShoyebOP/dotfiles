---
phase: 02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [setup.sh, wofi/.config/wofi/config, wofi/.config/wofi/style.css, nushell/.config/nushell/env.nu, zsh/.zprofile, README.md, AGENTS.md]
autonomous: true
requirements: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "User running bash setup.sh --mode local never installs hyprland, waybar, grim, slurp, wl-copy, or wofi: Arch gui set is exactly (alacritty keyd) and Debian gui set is exactly (alacritty)"
    - "The wofi stow package is gone from disk and from ALL_PACKAGES (6 rows: nvim zsh nushell alacritty starship keyd) and GUI_STOW_PACKAGES (2 rows: alacritty keyd)"
    - "Mode prompt, --help text, README.md, AGENTS.md, env.nu, and zsh/.zprofile contain zero references to the removed six; historical .planning docs are untouched"
    - "bash -n setup.sh passes and --dry-run local/server previews show only post-removal GUI names with zero writes"
  artifacts:
    - path: "setup.sh"
      provides: "Post-removal dep tables, 6-row registries, rewritten mode wording, updated 6-count strings"
      contains: "ALL_PACKAGES"
    - path: "wofi/"
      provides: "Deleted stow package (both files gone via git rm -r)"
      contains: "wofi/.config/wofi/config"
    - path: "README.md"
      provides: "Remnant-free user docs with corrected GUI lists and stow examples"
      contains: "GUI (Arch)"
    - path: "AGENTS.md"
      provides: "Remnant-free project instructions with corrected stack and stow-package lists"
      contains: "Stow packages"
  key_links:
    - from: "setup.sh:get_deps gui arrays"
      to: "setup.sh:verify_deps/install_deps"
      via: "gui arrays are the sole source of the local-mode install set; shrinking them removes the six from verify, install, re-verify, and dry-run preview"
      pattern: "gui=(alacritty keyd)"
    - from: "setup.sh:ALL_PACKAGES"
      to: "setup.sh:prompt_checklist backends"
      via: "all five backends iterate ALL_PACKAGES, so shrinking the array shrinks every backend uniformly to 6 rows"
      pattern: "ALL_PACKAGES"
    - from: "setup.sh:gui_list case"
      to: "setup.sh:core_missing/gui_missing partition"
      via: "partition labels derive from gui_list; shrinking it in the same edit keeps Missing GUI deps output truthful"
      pattern: "gui_list"
---

<objective>
Tracer slice for Phase 2.1 workstream A (per D-01 through D-05, D-07, D-12): surgically remove the Hyprland six from setup.sh tables, registries, prompts, and counts, delete the wofi/ stow tree, sweep every Hyprland text remnant from shell configs and docs, and prove it end-to-end with bash -n plus --dry-run.

Purpose: After this plan, local installs are structurally incapable of pulling the removed stack, and no doc or comment contradicts that. Workstream B (unified single-page checklist, D-08/D-09/D-10/D-11, filter extension, Debian keyd prompt D-06, uninstall scoping D-13) lands in Plan 02 on top of this clean base.

Output: Modified setup.sh, deleted wofi/ tree, edited env.nu, zsh/.zprofile, README.md, AGENTS.md — committed atomically per the Phase 1 D-04 atomic-docs rule.

Scope-gate notes (visible, recorded choice): spec-less probe fallback skipped — this inserted phase has no mapped REQ-IDs, so must_haves derive directly from CONTEXT.md D-01..D-13 and the ROADMAP goal. API Coverage checkpoint skipped — this phase integrates no external API/SDK/service (Bash installer surgery only). Assumption-delta scan returned detected:false; schema-push scan found no ORM schema files — both skipped.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@setup.sh
@.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-CONTEXT.md
@.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-RESEARCH.md
@.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-PATTERNS.md
@.planning/phases/01-universal-installer-platform-foundations/01-05-SUMMARY.md
@.planning/phases/02-safe-reversible-server-safe-deployment/02-01-SUMMARY.md
</context>

<tasks>
<!-- planner-discipline-allow: hyprland|waybar|grim|slurp|wl-copy|wofi -->
<!-- planner-discipline-allow: hyprland -->
<!-- planner-discipline-allow: waybar -->
<!-- planner-discipline-allow: grim -->
<!-- planner-discipline-allow: slurp -->
<!-- planner-discipline-allow: wl-copy -->
<!-- planner-discipline-allow: wofi -->

<task type="tracer">
  <name>Tracer: setup.sh dep-table, registry, mode-wording, and count surgery end-to-end</name>
  <files>setup.sh</files>
  <read_first>setup.sh; .planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-CONTEXT.md; .planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-PATTERNS.md</read_first>
  <action>Per D-01/D-02/D-03/D-05/D-07, hard-delete (no commented-out lines; git log -p setup.sh is the recovery path) the six removed stack names from setup.sh: set ALL_PACKAGES to exactly (nvim zsh nushell alacritty starship keyd), GUI_STOW_PACKAGES to exactly (alacritty keyd), TERMUX_DISABLED_PACKAGES to exactly (alacritty keyd); set get_deps arch gui to exactly (alacritty keyd) and debian gui to exactly (alacritty), leaving termux gui empty and both common arrays untouched; shrink the line-1461 gui_list case to arch (alacritty keyd), debian (alacritty), termux empty in the same edit; rewrite the line-227-228 filter comment to neutral tick-contract wording that names only toolchain versus the remaining gui extras alacritty and keyd (filter logic itself is extended in Plan 02 — comment only here); rewrite both prompt_mode echo lines so Local reads as desktop extras naming the Alacritty terminal and Keyd remap with no other app names, and Server reads as headless with no GUI packages, keeping >&2 routing, read -r, and exit-1-on-EOF; update the usage() --mode line to match (desktop extras versus headless, no removed names); update every stow-package 7-count string to 6 (the 7 packages offered and offered 7 sites around lines 898, 956, 1014, 1046, 1064, 1095, 1117, 1435-1436, 1446-1447) and both package-backend -gt 7 list_height clamps to 6, leaving all 13-toolchain strings and -gt 13 clamps untouched for the Plan 02 merge. Preserve set -Eeuo pipefail, ${1-} guards, help-wins pre-scan, outside-repo-root guard, and DRY_RUN early-returns; touch nothing in install_keyd_privileged, quarantine_scan, post_verify, offer_chsh, or the toolchain ladder.</action>
  <reversibility rating="reversible">Hard-deletes recoverable via git log -p setup.sh per D-02; no migration needed.</reversibility>
  <verify>
    <automated>bash -n setup.sh</automated>
    <automated>grep -q '^ALL_PACKAGES=(nvim zsh nushell alacritty starship keyd)$' setup.sh && grep -q '^GUI_STOW_PACKAGES=(alacritty keyd)$' setup.sh && grep -q '^TERMUX_DISABLED_PACKAGES=(alacritty keyd)$' setup.sh</automated>
    <automated>grep -q 'gui=(alacritty keyd)' setup.sh && grep -q 'gui=(alacritty)$' setup.sh && grep -q 'gui_list=(alacritty keyd)' setup.sh</automated>
    <automated>bash setup.sh --mode local --shell zsh --dry-run < /dev/null 2>&1 | grep -qi 'alacritty' && bash setup.sh --mode server --shell zsh --dry-run < /dev/null 2>&1 | tee /tmp/p01-dry.txt | grep -q '6 packages offered'</automated>
  </verify>
  <done>setup.sh carries only the post-removal sets, mode text names no removed app, counts read 6, syntax check passes, and non-interactive dry-runs for local and server complete with zero writes.</done>
  <acceptance_criteria>setup.sh contains ALL_PACKAGES=(nvim zsh nushell alacritty starship keyd); setup.sh contains GUI_STOW_PACKAGES=(alacritty keyd); no commented-out removed-name line remains in setup.sh; prompt_mode output contains Alacritty and Keyd and Server headless wording; bash -n setup.sh exits 0; server dry-run prints 6 packages offered.</acceptance_criteria>
</task>

<task type="auto">
  <name>Delete wofi tree and sweep shell-config remnants</name>
  <files>wofi/.config/wofi/config, wofi/.config/wofi/style.css, nushell/.config/nushell/env.nu, zsh/.zprofile</files>
  <read_first>nushell/.config/nushell/env.nu; zsh/.zprofile; wofi/.config/wofi/config</read_first>
  <action>Per D-04 and D-12, delete the entire wofi stow package with git rm -r wofi (both wofi/.config/wofi/config and wofi/.config/wofi/style.css go; no delist-only shortcut); delete the commented tty1 launch block in nushell/.config/nushell/env.nu (the Only-run-on-TTY1 comment plus the Launching message, exec start-hyprland, and brace lines, roughly lines 20-24) and touch no other Nushell logic per the scope filter; rewrite zsh/.zprofile as a neutral comment-only empty login profile naming neither a compositor nor a launch command, keeping the file comment-only. Do not edit any file under .planning/.</action>
  <reversibility rating="reversible">Recoverable via git history per D-04; no migration needed.</reversibility>
  <verify>
    <automated>test ! -e wofi && test ! -e wofi/.config/wofi/config && echo wofi-gone</automated>
    <automated>! grep -qi 'launching\|start-hyprland' nushell/.config/nushell/env.nu && ! grep -qi 'hyprland' zsh/.zprofile && cat zsh/.zprofile</automated>
  </verify>
  <done>wofi/ absent from disk and git index, env.nu launch block deleted with surrounding XDG block intact, .zprofile neutral and comment-only.</done>
  <acceptance_criteria>test ! -e wofi passes; git status shows wofi/ deleted; env.nu retains its XDG Paths block; zsh/.zprofile contains no compositor or autostart mention.</acceptance_criteria>
</task>

<task type="auto">
  <name>Docs remnant sweep (README + AGENTS.md) with repo-wide zero-hits gate</name>
  <files>README.md, AGENTS.md</files>
  <read_first>README.md; AGENTS.md; setup.sh</read_first>
  <action>Per D-12 with Phase 1 D-04 same-commit atomicity, purge every removed-stack mention from README.md at the six anchored sites: line-34 checklist prose becomes 6 toggleable naming nvim zsh nushell alacritty starship keyd with server pre-unchecking alacritty and keyd; line-66 Termux disabled list becomes keyd and alacritty; lines 79-80 GUI lists become Arch alacritty keyd and Debian alacritty; lines 95 and 101 stow examples drop the extra launcher package; line-146 utilities row drops it; mode and checklist prose match the new prompt_mode wording (desktop extras versus headless). In AGENTS.md remove the launcher from the project line-7 desktop primitives, delete the compositor GUI-stack bullet at line 82, fix the stale zprofile auto-exec claim at line 89, correct core/GUI dep lists at lines 100-101 and 104 to Arch adds alacritty keyd and Debian adds alacritty, drop the launcher from the stow-package enumeration at line 116 and the config enumeration at line 120, and rewrite the architecture rows (Input/Launcher table, bounded-contexts list, terminal-layer location/contains/depends/used-by rows, entry-point triggers and responsibilities, package-directory enumeration) so no removed-stack name or compositor dependency remains; keep Zsh-default and Nushell-backup wording everywhere. Unified-single-page checklist prose is Plan 02 work — describe only the 6-row pre-merge state here.</action>
  <verify>
    <automated>! rg -qi 'hyprland|waybar|grim|slurp|wl-copy|wofi' --glob '!.planning/**' --glob '!02.1-01-PLAN.md' README.md AGENTS.md setup.sh nushell/.config/nushell/env.nu zsh/.zprofile && echo sweep-clean</automated>
    <automated>test ! -e wofi && grep -q 'GUI (Arch)' README.md && bash -n setup.sh && bash setup.sh --mode local --shell zsh --yes --dry-run < /dev/null 2>&1 | grep -q 'DRY RUN'</automated>
    <human-check>Skim README.md mode and checklist sections once: Local reads as desktop extras (Alacritty, Keyd), Server as headless, with no removed-stack names.</human-check>
  </verify>
  <done>README.md and AGENTS.md are remnant-free, the scoped zero-hits gate passes, wofi/ is gone, and local/server dry-runs (including --yes non-interactive) preview with zero writes.</done>
  <acceptance_criteria>Scoped rg over README.md, AGENTS.md, setup.sh, env.nu, and zsh/.zprofile returns no match; README.md contains GUI (Arch) with alacritty and keyd only; bash setup.sh --mode local --shell zsh --yes --dry-run piped from /dev/null exits 0.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-prompt→installer | Rewritten prompt_mode/usage text crosses into shell parsing; allow-list validation must hold |
| installer→package-manager | Shrunk gui arrays flow into pacman/apt/pkg commands; only post-removal names may flow |
| repo→operator | wofi/ deletion and doc edits must not orphan references that mislead future installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-01 | Tampering | setup.sh:get_deps + gui_list partition | high | mitigate | Shrink tables and gui_list in the same edit per task 1; gate with dry-run local/server previews showing only post-removal GUI names; block on any removed name in Missing GUI deps output |
| T-02.1-02 | Tampering | setup.sh:prompt_mode + usage() rewrite | medium | mitigate | Keep >&2 routing, read -r, exit-1-on-EOF, and --mode allow-list validation untouched; new strings are display-only and never interpolated into commands |
| T-02.1-SC | Tampering | package installs | low | accept | No new packages are installed by this phase; system packages come from distro managers only; rationale recorded, no registry verification applies |
</threat_model>

<verification>
Automated: bash -n setup.sh; registry/array positive greps; scoped zero-hits rg gate over the five remnant files; wofi absence test; non-interactive dry-run matrix (local + server, with and without --yes) each exiting 0 with DRY RUN preview and zero writes. Human check (end-of-phase, per workflow.human_verify_mode): skim README mode/checklist prose for desktop-extras versus headless wording.
</verification>

<success_criteria>
All four must_haves truths hold; every D-01/D-02/D-03/D-04/D-05/D-07/D-12 item is implemented with no commented-out leftovers; .planning history untouched; plan commits with docs atomically.
</success_criteria>

## Artifacts this phase produces

This plan creates no new symbols — it deletes and shrinks. Net symbol changes: removed stow package paths wofi/.config/wofi/config and wofi/.config/wofi/style.css; narrowed array values ALL_PACKAGES (6), GUI_STOW_PACKAGES (2), TERMUX_DISABLED_PACKAGES (2); narrowed get_deps gui arrays arch (alacritty keyd) and debian (alacritty); narrowed gui_list partition case; rewritten prompt_mode display strings; 6-count audit strings. The plan-review-convergence source-grounding pass must treat the narrowed array values and rewritten strings as intended changes, not drift.

<output>
Create `.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-01-SUMMARY.md` when done
</output>
