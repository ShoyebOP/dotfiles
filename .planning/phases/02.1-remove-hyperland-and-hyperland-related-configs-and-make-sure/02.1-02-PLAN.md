---
phase: 02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified: [setup.sh, README.md]
autonomous: true
requirements: []
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "User sees ONE single-page checklist, shown once before any write, on both the install and the uninstall path — never two pages"
    - "Every remaining installable app is toggleable: tick means install the binary AND stow its config when one exists; untick means never install, never stow — mode and shell only set pre-selection defaults"
    - "The removed six never appear on the unified page, not even as disabled rows"
    - "On Debian with keyd ticked, the user gets a manual-build pointer at https://github.com/rvaiya/keyd; keyd is never routed through apt, and privileged stow runs only when the keyd binary exists"
    - "The five-backend ladder order, cancel-never-cascades contract, Termux disabled-row emulation, --yes and no-TTY fast paths, DRY_RUN previews, strict mode, repo-root guard, and the privileged keyd gate all behave exactly as before"
  artifacts:
    - path: "setup.sh"
      provides: "Unified single-page checklist dispatcher, unified backend rungs, tick-authoritative dep filter, Debian keyd notice"
      contains: "prompt_unified_checklist"
    - path: "README.md"
      provides: "Unified-page checklist prose with documented --yes preset semantics"
      contains: "single-page checklist"
  key_links:
    - from: "setup.sh:prompt_unified_checklist"
      to: "setup.sh:SELECTED_PACKAGES + SELECTED_DEPS"
      via: "single invocation splits ticks back into both downstream arrays (zsh/starship land in both; nvim row lands nvim plus neovim), so run_stow, run_uninstall, preview_selection, and offer_system_package_removal work unchanged"
      pattern: "SELECTED_PACKAGES"
    - from: "setup.sh:filter_deps_by_selection"
      to: "setup.sh:verify_deps/install_deps"
      via: "extended rule drops any system dep matching an unticked stow row (with the nvim-neovim map), closing the gui-bypass hole so untick is authoritative"
      pattern: "filter_deps_by_selection"
    - from: "setup.sh:prompt_debian_keyd_notice"
      to: "setup.sh:install_keyd_privileged"
      via: "Debian notice runs before stow, never invokes sudo itself, and the privileged gate is entered only when the keyd binary exists"
      pattern: "command -v keyd"
---

<objective>
Workstream B for Phase 2.1 (per D-06, D-08, D-09, D-10, D-11, D-13, plus the D-02 filter-logic remainder): merge prompt_checklist and prompt_toolchain_checklist into ONE single-page checklist honoring strict tick semantics, close the gui-bypass hole in the dep filter, add the Debian-only keyd manual-build prompt, wire both install and uninstall paths through the same page, and scope uninstall offers to the post-removal set.

Purpose: Every app the script can install becomes toggleable in one place with tick authoritative over install plus stow, while every Phase 1/2 safety contract (ladder order, cancel semantics, Termux emulation, fast paths, dry-run, privileged gate) survives the merge unchanged.

Output: Extended setup.sh checklist/filter/keyd/uninstall logic plus README unified-page prose — same-commit atomicity per Phase 1 D-04.

Scope-gate notes (visible, recorded choice): spec-less probe fallback skipped — no mapped REQ-IDs; must_haves derive from CONTEXT.md D-01..D-13 and the ROADMAP goal. API Coverage checkpoint skipped — no external API/SDK/service involved. Requires Plan 01 (post-removal tables and registries) — hence wave 2.

Locked --yes semantics (Open Q1 resolved): --yes selects mode/shell/family presets (identical to the no-TTY preset path), NOT all-ON — so server mode keeps GUI rows OFF and Termux keeps disabled rows OFF under --yes. This supersedes the 01-05 toolchain all-select behavior; document it in usage() and README. Locked row-shape rule: derive UNIFIED_ROWS programmatically from post-removal ALL_PACKAGES plus ALL_TOOLCHAIN, merging zsh and starship exactly and mapping the nvim row to neovim (RESEARCH estimates 17 rows; the exact count follows from the arrays — acceptance asserts one page and tick authority, never a magic number). The 7-to-6 change supersedes Phase 1 D-09's row count only; ladder mechanics are unchanged (Open Q3 resolved).
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@setup.sh
@.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-CONTEXT.md
@.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-RESEARCH.md
@.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-PATTERNS.md
@.planning/phases/01-universal-installer-platform-foundations/01-05-SUMMARY.md
@.planning/phases/02-safe-reversible-server-safe-deployment/02-01-SUMMARY.md
</context>

<tasks>
<!-- planner-discipline-allow: hyprland|waybar|grim|slurp|wl-copy|wofi -->
<!-- planner-discipline-allow: hyprland -->
<!-- planner-discipline-allow: waybar -->
<!-- planner-discipline-allow: grim -->
<!-- planner-discipline-allow: slurp -->
<!-- planner-discipline-allow: wl-copy -->
<!-- planner-discipline-allow: wofi -->

<task type="auto">
  <name>Unified single-page checklist dispatcher plus five backend rungs</name>
  <files>setup.sh</files>
  <read_first>setup.sh; .planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-PATTERNS.md; .planning/phases/01-universal-installer-platform-foundations/01-05-SUMMARY.md</read_first>
  <action>Per D-08/D-09/D-10/D-11, build the merged page reusing the proven ladder (never a new TUI framework): add a UNIFIED_ROWS array derived from the post-removal ALL_PACKAGES six plus ALL_TOOLCHAIN thirteen with zsh and starship merged into single rows and the nvim row mapped to neovim, so toolchain-only survivors (stow, git, zoxide, uv, ripgrep, nodejs, npm, make, gcc, fzf) appear once and the removed six appear nowhere, not even as disabled rows; add dispatcher prompt_unified_checklist keeping the exact prompt_checklist skeleton (no-TTY branch first with zero prompts, then gum, whiptail, dialog, fzf, read rungs where return 1 means missing tool and fall through while return 2 means cancel and abort immediately without cascading); add five unified rungs unified_checklist_gum, unified_checklist_whiptail, unified_checklist_dialog, unified_checklist_fzf, unified_checklist_read that render identical rows with identical presets computed once (all rows ON by default; server mode forces the GUI_STOW_PACKAGES rows alacritty and keyd OFF; shell choice forces exactly one of zsh and nushell ON; Termux forces TERMUX_DISABLED_PACKAGES rows alacritty and keyd OFF with the not-available-on-Termux suffix in whiptail, dialog, and read plus strip_termux_disabled warn-and-drop cover for gum and fzf); each rung splits ticks back into both SELECTED_PACKAGES and SELECTED_DEPS preserving ALL_TOOLCHAIN order with the zsh and starship rows landing in both arrays and the nvim row landing as nvim in packages and neovim in deps; keep the safe-parse idiom of xargs -n1 splitting with allow-list matching against UNIFIED_ROWS and no eval, the read-backend number-toggle with comma-to-space normalization and invalid and out-of-range warnings, the small-terminal rows-below-20 fallback to the read rung, and _toolchain_ensure_stow re-enabling stow when missing; implement --yes as mode/shell/family presets with zero prompts and keep the no-TTY preset path identical; replace both main-flow call sites (install branch and the uninstall branch around old lines 1428-1439) with the single dispatcher so each path shows exactly one page; update offered-count audit lines to the unified row count and derive whiptail/dialog/fzf list heights from the UNIFIED_ROWS length instead of the old 7 and 13 clamps; keep DRY_RUN preview-before-mutation, set -Eeuo pipefail, ${1-} guards, and the outside-repo-root guard untouched.</action>
  <verify>
    <automated>bash -n setup.sh && grep -q 'prompt_unified_checklist' setup.sh && grep -q 'UNIFIED_ROWS' setup.sh</automated>
    <automated>bash setup.sh --mode server --shell zsh --dry-run < /dev/null 2>&1 | tee /tmp/p02-server.txt | grep -qc 'Selection' | grep -q '^1$'; grep -q 'Selected packages' /tmp/p02-server.txt || grep -qi 'unified\|selected' /tmp/p02-server.txt</automated>
    <automated>bash setup.sh --mode local --shell zsh --yes --dry-run < /dev/null 2>&1 | grep -q 'DRY RUN' && bash setup.sh --mode server --shell zsh --yes --dry-run < /dev/null 2>&1 | grep -q 'DRY RUN'</automated>
  </verify>
  <done>One dispatcher plus five rungs render the same unified rows with the same presets on both paths, ticks split into both downstream arrays, fast paths stay non-interactive, and syntax plus dry-runs pass.</done>
  <acceptance_criteria>setup.sh contains prompt_unified_checklist and unified_checklist_read; install and uninstall branches each invoke the unified dispatcher exactly once; server no-TTY dry-run shows a single selection section with GUI rows OFF; --yes dry-runs exit 0 with DRY RUN preview and zero writes.</acceptance_criteria>
</task>

<task type="auto">
  <name>Tick-authoritative filter, Debian keyd notice, and uninstall scoping</name>
  <files>setup.sh</files>
  <read_first>setup.sh; .planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-RESEARCH.md</read_first>
  <action>Per D-06/D-09/D-10/D-13 and the D-02 filter remainder, extend filter_deps_by_selection and the in-get_deps filter with one general rule: a system dep whose name matches (or maps to, reusing the verify_deps neovim-to-nvim, ripgrep-to-rg, nodejs-to-node case) an unticked stow row in SELECTED_PACKAGES is dropped from the install set, so post-removal the only bypass-capable names alacritty and keyd obey their rows and mode plus shell govern presets only; update the line-228 comment to describe the tick-authoritative rule. Add function prompt_debian_keyd_notice called on the install path only after the unified checklist: when FAMILY is debian and keyd is ticked and the keyd binary is absent, print a notice that apt has no keyd package and point at building from https://github.com/rvaiya/keyd, honoring the DRY RUN prefix under dry-run, never appending keyd to any apt missing list, never invoking sudo itself, and entering install_keyd_privileged only when the keyd binary exists (never offer automated clone plus make plus install — that automation is deferred). Derive offer_system_package_removal candidates from the unified ticked set mapped to dep names and scoped to the post-removal set only, so removed binaries are never offered per D-13; keep the privileged keyd gate verbatim (preview plus diff -u plus gum-confirm or Type-yes plus conditional adopt plus reload-or-true), the Termux early-returns, the typed-yes uninstall guard, and Mason cleanup on nvim deselect exactly as Phase 2 left them.</action>
  <verify>
    <automated>bash -n setup.sh && grep -q 'prompt_debian_keyd_notice' setup.sh && grep -q 'github.com/rvaiya/keyd' setup.sh</automated>
    <automated>bash -c 'source ./setup.sh; SELECTED_PACKAGES=(nvim zsh starship); SELECTED_DEPS=("${ALL_TOOLCHAIN[@]}"); FAMILY=debian; MODE=local; mapfile -t _d < <(get_deps "$FAMILY" "$MODE"); printf "%s\n" "${_d[@]}" | grep -qv "^alacritty$" && echo untick-authoritative'</automated>
    <automated>bash setup.sh --uninstall --mode server --shell zsh --yes --dry-run < /dev/null 2>&1 | grep -q 'DRY RUN'</automated>
  </verify>
  <done>Unticked stow rows suppress their system deps, Debian keyd tick produces guidance instead of an apt failure, uninstall offers only post-removal names, and every safety gate from Phase 2 still stands.</done>
  <acceptance_criteria>setup.sh contains prompt_debian_keyd_notice and the rvaiya keyd URL; sourced filter demo drops alacritty when its stow row is unticked; keyd never appears in a Debian apt install preview; uninstall dry-run exits 0 with DRY RUN preview.</acceptance_criteria>
</task>

<task type="auto">
  <name>README unified-page prose plus full verification matrix</name>
  <files>README.md, setup.sh</files>
  <read_first>README.md; setup.sh</read_first>
  <action>Per D-08/D-10 and the locked --yes rule, rewrite the README checklist prose (the old two-page and 7-toggleable wording around lines 34 and 66) to describe the single-page checklist shown once before any write on both install and uninstall, the strict tick semantics (tick installs the binary and stows its config when one exists; untick never installs and never stows), mode plus shell as pre-selection defaults only, the five-backend ladder in locked order with cancel never cascading, Termux disabled rows, and --yes meaning mode/shell/family presets identical to the no-TTY path; update the usage() --yes flag line in setup.sh in the same commit to state the preset semantics. Then run the full matrix: bash -n; scoped zero-hits rg over setup.sh, README.md, AGENTS.md, env.nu, and zsh/.zprofile; local and server dry-runs with and without --yes from /dev/null; uninstall dry-run; and a sourced ladder-fallback spot check by hiding gum/dialog from PATH so a lower rung serves the page.</action>
  <verify>
    <automated>bash -n setup.sh && ! rg -qi 'hyprland|waybar|grim|slurp|wl-copy|wofi' --glob '!.planning/**' --glob '!02.1-*-PLAN.md' setup.sh README.md AGENTS.md nushell/.config/nushell/env.nu zsh/.zprofile && echo matrix-sweep-clean</automated>
    <automated>for m in local server; do bash setup.sh --mode $m --shell zsh --dry-run < /dev/null >/dev/null 2>&1 || exit 1; bash setup.sh --mode $m --shell zsh --yes --dry-run < /dev/null >/dev/null 2>&1 || exit 1; done; bash setup.sh --uninstall --mode server --shell zsh --yes --dry-run < /dev/null >/dev/null 2>&1 && echo matrix-ok</automated>
    <automated>grep -qi 'single.page' README.md && grep -q 'presets' README.md && echo docs-ok</automated>
    <human-check>Run one interactive dry-run on a machine with a TUI backend (whiptail or fzf) and confirm exactly one checklist page appears with every remaining app toggleable before any write.</human-check>
  </verify>
  <done>README documents the unified page and --yes presets, the whole dry-run matrix passes non-interactively, the zero-hits gate holds, and ladder fallback is spot-checked.</done>
  <acceptance_criteria>README.md contains single-page wording and presets wording; six-command dry-run matrix (local/server with and without --yes plus uninstall) exits 0; scoped rg returns no match; fallback-backend dry-run still shows the unified page.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tui-stdout→installer | All five backend outputs are untrusted input until allow-list matched; never eval, never command-substitute into commands |
| checklist→package-manager | Unified ticks flow into pacman/apt/pkg install and removal commands; mapping must stay within the post-removal set |
| notice→privileged-stow | Debian keyd notice must never trigger sudo; only the existing confirm gate may authorize /etc writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-03 | Tampering/EoP | unified checklist backends (V5 input validation) | high | mitigate | Keep xargs -n1 split plus allow-list match against UNIFIED_ROWS in every rung; never eval backend stdout; keep read-backend numeric validation with invalid-token warnings; block on any eval or unquoted command substitution of backend output |
| T-02.1-04 | Tampering | prompt_debian_keyd_notice → install_keyd_privileged (V4 access control) | high | mitigate | Notice prints the build URL only and never invokes sudo; privileged stow entered solely when the keyd binary exists and then only through the unchanged preview plus diff plus explicit-confirm plus conditional-adopt gate; block on any sudo reachable from the notice path |
| T-02.1-05 | Denial of service | --yes / no-TTY fast paths (V5) | medium | mitigate | Preserve no-TTY presets with zero prompts and the locked --yes preset rule; gate with the non-interactive CI-style matrix (piped /dev/null, --yes) across local/server and uninstall |
| T-02.1-SC | Tampering | package installs | low | accept | Zero new packages; distro-manager packages only; informational keyd URL never fetched or executed by the script |
</threat_model>

<verification>
Automated: bash -n; symbol presence greps (dispatcher, UNIFIED_ROWS, notice, URL); sourced filter unit demo proving untick authority; six-command non-interactive dry-run matrix; scoped zero-hits gate; ladder-fallback spot check. Human check (end-of-phase, per workflow.human_verify_mode): one interactive dry-run confirming a single toggleable page before any write.
</verification>

<success_criteria>
All five must_haves truths hold; D-06/D-08/D-09/D-10/D-11/D-13 fully implemented with the five-backend ladder, cancel contract, Termux emulation, fast paths, dry-run, and privileged gate behaviorally unchanged; README and usage() document the unified page and --yes presets; uninstall never offers removed binaries.
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan (the plan-review-convergence source-grounding pass must exclude these from drift verification): dispatcher function prompt_unified_checklist; backend rungs unified_checklist_gum, unified_checklist_whiptail, unified_checklist_dialog, unified_checklist_fzf, unified_checklist_read; row array UNIFIED_ROWS; notice function prompt_debian_keyd_notice referencing https://github.com/rvaiya/keyd (pointer only, never fetched); revised usage() --yes text; README single-page checklist prose. SELECTED_PACKAGES and SELECTED_DEPS keep their names and downstream contract — populated from the single page per RESEARCH Pattern 1.

<output>
Create `.planning/phases/02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure/02.1-02-SUMMARY.md` when done
</output>
