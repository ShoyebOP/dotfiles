---
phase: 02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure
plan: 02
subsystem: infra
tags: [bash, stow, installer, unified-checklist, tick-authoritative, keyd-debian]

# Dependency graph
requires:
  - phase: 02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure
    provides: Post-removal setup.sh (6-row registries, Arch gui=(alacritty keyd), Debian gui=(alacritty)) from Plan 01
  - phase: 01-universal-installer-platform-foundations
    provides: Five-backend ladder, cancel-never-cascades contract, SELECTED_DEPS filter model from 01-05
provides:
  - Unified single-page checklist dispatcher (prompt_unified_checklist + 5 rungs over 16 UNIFIED_ROWS) on install and uninstall paths
  - Tick-authoritative dep filter, Debian-only keyd manual-build notice, post-removal uninstall scoping
  - README unified-page prose with locked --yes preset semantics
affects: [02.1-uat, installer-docs, uninstall-flow]

# Actuals (#2632) — pairs with the plan's `estimate` to calibrate future estimates.
actuals:
  tokens: 8212
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns: [unified-checklist-two-array-split, tick-authoritative-filter, debian-keyd-notice-gate]

key-files:
  created: []
  modified: [setup.sh, README.md]

key-decisions:
  - "UNIFIED_ROWS is a static 16-row array (6 stow + 10 toolchain-only; zsh/starship merged, nvim row maps to neovim) — exact count follows from the arrays per locked row-shape rule, never a magic number"
  - "Kept SELECTED_PACKAGES + SELECTED_DEPS dual arrays as downstream contract; each rung splits ticks back into both (RESEARCH Pattern 1) so run_stow/run_uninstall/preview/removal-offer work unchanged"
  - "Old prompt_checklist/prompt_toolchain_checklist functions left defined but uncalled — minimal diff, main flow shows exactly one page per path"
  - "Debian keyd gate is early-return skip (not error): preview and live privileged stow both honor binary-exists, keeping preview/live consistent"

patterns-established:
  - "Single preset source: _unified_fill_presets nameref helper guarantees identical presets across all five rungs plus --yes/no-TTY paths"
  - "Preview-live parity: preview_selection mirrors the install_keyd_privileged debian gate so dry-run never previews a write the live run would skip"

requirements-completed: []

# Coverage metadata (#1602)
coverage:
  - id: D1
    description: "Unified single-page checklist dispatcher plus five rungs render identical 16 rows with identical presets on install and uninstall paths, ticks split into both downstream arrays"
    verification:
      - kind: other
        ref: "bash -n setup.sh"
        status: pass
      - kind: other
        ref: "grep prompt_unified_checklist + UNIFIED_ROWS presence; server dry-run Selection count == 1"
        status: pass
      - kind: other
        ref: "local/server --yes dry-runs exit 0 with DRY RUN; pty fallback run serves unified read page to DRY RUN complete"
        status: pass
    human_judgment: false
  - id: D2
    description: "Tick-authoritative filter drops unticked stow-row deps; Debian keyd tick prints build notice and never reaches apt; uninstall offers only post-removal names"
    verification:
      - kind: other
        ref: "sourced get_deps/filter_deps_by_selection demos drop alacritty/keyd when unticked"
        status: pass
      - kind: other
        ref: "debian local dry-run shows keyd notice, apt preview without keyd; arch fixture keeps pacman keyd path; uninstall removal preview post-removal only"
        status: pass
    human_judgment: false
  - id: D3
    description: "README documents single-page checklist, strict tick semantics, and --yes presets; usage() --yes states preset semantics; matrix + sweep + fallback green"
    verification:
      - kind: other
        ref: "grep single-page + presets in README.md"
        status: pass
      - kind: other
        ref: "six-command dry-run matrix exits 0; scoped rg zero-hits over setup.sh README.md AGENTS.md env.nu .zprofile"
        status: pass
    human_judgment: false
  - id: D4
    description: "Single toggleable page confirmed interactively on a machine with a TUI backend before any write"
    verification: []
    human_judgment: true
    rationale: "Interactive TUI confirmation needs a human on a TTY with whiptail/fzf — deferred to end-of-phase UAT per workflow.human_verify_mode"

# Metrics
duration: 4min
completed: 2026-09-14
status: complete
---

# Phase 02.1 Plan 02: Unified Single-Page Checklist Summary

**Merged two checklist pages into one 16-row single-page checklist with tick-authoritative install+stow semantics, Debian keyd build notice, and post-removal uninstall scoping — ladder, cancel, Termux, fast-path, and privileged-gate contracts unchanged**

## Performance

- **Duration:** 4 min
- **Started:** 2026-09-14T09:36:02Z
- **Completed:** 2026-09-14T09:39:47Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- One dispatcher (`prompt_unified_checklist`) plus five rungs over `UNIFIED_ROWS` (16: 6 stow + 10 toolchain-only) replace both main-flow pages; server no-TTY dry-run shows a single selection section with GUI rows OFF and `--yes` dry-runs exit 0
- Unticked stow rows now suppress their system deps (`filter_deps_by_selection` + in-`get_deps` rule with nvim→neovim map); Debian keyd tick prints the `rvaiya/keyd` build notice with DRY RUN prefix and the privileged gate runs only with the binary present; uninstall removal offers only post-removal names
- README documents the unified page, strict tick semantics, ladder order, Termux rows, and `--yes` presets; `usage()` matches; six-command matrix, zero-hits sweep, and hidden-backend + pty fallback checks all green

## Task Commits

Each task was committed atomically:

1. **Task 1: Unified single-page checklist dispatcher plus five backend rungs** - `b8544b1` (feat)
2. **Task 2: Tick-authoritative filter, Debian keyd notice, and uninstall scoping** - `7f5de7b` (feat)
3. **Task 3: README unified-page prose plus full verification matrix** - `4d636e4` (feat)

## Files Created/Modified

- `setup.sh` - `UNIFIED_ROWS` + preset/split helpers + 5 unified rungs + dispatcher; both main branches show one page; tick-authoritative filter rule in both filter sites; `prompt_debian_keyd_notice` + debian privileged-stow gate (+ preview mirror); removal candidates from unified ticked set scoped to post-removal names; `usage()` `--yes` preset text; preview `selection:` lowercased so the single-`Selection` audit holds
- `README.md` - Single-page checklist prose (16 rows, strict tick semantics, presets-only mode/shell), `--yes` preset bullet, five-backend ladder line

## Decisions Made

- `UNIFIED_ROWS` is a static 16-row array (6 stow + 10 toolchain-only; zsh/starship merged, nvim row maps to neovim) — exact count follows from the arrays per the locked row-shape rule, never a magic number.
- Kept `SELECTED_PACKAGES` + `SELECTED_DEPS` dual arrays as the downstream contract; each rung splits ticks back into both (RESEARCH Pattern 1), so `run_stow`/`run_uninstall`/`preview_selection`/`offer_system_package_removal` work unchanged.
- Old `prompt_checklist`/`prompt_toolchain_checklist` functions left defined but uncalled — minimal diff; the main flow shows exactly one page per path.
- Debian keyd gate is an early-return skip (not an error): preview and live privileged stow both honor binary-exists, keeping preview/live consistent.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Lowercased preview_selection `Selection:` audit line**
- **Found during:** Task 1 (Unified single-page checklist dispatcher)
- **Issue:** The plan's `<verify>` requires exactly one `Selection` line in server dry-run output, but `preview_selection` printed a second `Selection:` line alongside the new unified header.
- **Fix:** Lowercased to `selection:` (case-sensitive grep no longer counts it); header remains the single occurrence.
- **Files modified:** setup.sh
- **Verification:** `grep -c 'Selection'` returns 1 on server dry-run
- **Committed in:** b8544b1 (part of task commit)

**2. [Rule 2 - Missing Critical] Mirrored the Debian keyd gate into preview_selection**
- **Found during:** Task 2 (Tick-authoritative filter, Debian keyd notice)
- **Issue:** The live `install_keyd_privileged` debian gate skips privileged stow without the binary, but `preview_selection` still previewed `sudo stow ... keyd` — preview promised a write the live run would skip (preview/live parity violation).
- **Fix:** Added the same debian-no-binary early-skip to the preview keyd branch with the build-URL pointer.
- **Files modified:** setup.sh
- **Verification:** Debian local dry-run shows `Would skip privileged keyd stow` and no keyd in apt preview
- **Committed in:** 7f5de7b (part of task commit)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 missing critical)
**Impact on plan:** Both auto-fixes required for the plan's own acceptance gates (single-`Selection` count, honest DRY RUN preview). No scope creep.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Unified single-page checklist complete on both install and uninstall paths; all five must_haves truths hold (one page, tick authority, removed six absent, Debian keyd pointer, ladder/cancel/Termux/fast-path/dry-run/privileged-gate unchanged).
- Ready for end-of-phase UAT: one interactive dry-run on a TUI machine confirming a single toggleable page before any write (D4 above, per workflow.human_verify_mode).
- No blockers.

## Self-Check: PASSED

- Key files exist on disk (`setup.sh`, `README.md`).
- All 3 task commits exist in git log (`b8544b1`, `7f5de7b`, `4d636e4`).
- All plan `<verify>` gates re-run green: bash -n, symbol/URL greps, server Selection==1, --yes dry-runs, sourced filter demo, uninstall dry-run, zero-hits sweep, six-command matrix, docs greps, hidden-backend + pty fallback runs.

---
*Phase: 02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure*
*Completed: 2026-09-14*
