# Phase 2.1: remove hyperland and hyperland related configs and make sure anything related to hyperland is not installed in local installation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-14
**Phase:** 2.1-remove-hyperland-and-hyperland-related-configs-and-make-sure
**Areas discussed:** Removal breadth, Wofi package fate, Local mode meaning, Remnants + cleanup, Unified single-page checklist (user-raised)

---

## Removal breadth

| Option | Description | Selected |
|--------|-------------|----------|
| hyprland only | Remove just `hyprland`; keep waybar/grim/slurp/wl-copy as generic Wayland utils | |
| hyprland + Wayland bar/capture | Remove hyprland + waybar + grim + slurp + wl-copy | |
| All except alacritty/keyd | Strip all GUI system-deps tied to Hyprland local mode; only alacritty/keyd remain | ✓ |

**User's choice:** All except alacritty/keyd
**Notes:** Follow-up confirmed wofi goes too ("Remove wofi too" over system-deps-only or you-decide). Debian keyd asymmetry questioned by user ("why does debian not have keyd?") — investigated live: Ubuntu 24.04 apt has no `keyd` (only ekeyd/esekeyd), Arch pacman does. User chose "Keep asymmetry". Follow-up: Debian keyd ticked → "Yes — prompt to manual install" (point at rvaiya/keyd build, proceed to privileged stow only if binary exists). Final: "Hard delete (clean)" over commented-out lines.

## Wofi package fate

| Option | Description | Selected |
|--------|-------------|----------|
| Delete directory entirely | Remove wofi/ tree (config + style.css) — git history recovers if needed | ✓ |
| Keep dir, remove from lists | Keep files on disk but remove from ALL_PACKAGES + GUI_STOW_PACKAGES | |
| Keep dir, disabled row | Visible-but-disabled checklist row like Termux pattern | |

**User's choice:** Delete directory entirely
**Notes:** Follow-up on 6-vs-7 checklist rows was dismissed by user; clean-6 recorded as working default in CONTEXT (agent discretion).

## Local mode meaning

| Option | Description | Selected |
|--------|-------------|----------|
| Rewrite both lines | Local = desktop extras (Alacritty, Keyd); Server = headless — Hyprland gone from text | ✓ |
| Minimal word swap | Only delete the word Hyprland from prompt | |

**User's choice:** Rewrite both lines

## Remnants + cleanup

| Option | Description | Selected |
|--------|-------------|----------|
| Full sweep + offer | Delete all text remnants AND uninstall offers to remove leftover Hyprland binaries via pacman -Rns/apt remove | |
| Text only | Delete text remnants; leave installed Hyprland binaries alone | ✓ (variant) |

**User's choice:** delete all remnants but it should not offer to uninstall those packages (free-text variant of Text only)
**Notes:** Sweep covers nushell env.nu commented block, zsh/.zprofile comments, README GUI sections, AGENTS.md stack lines. Historical .planning/ docs untouched.

## Unified single-page checklist (user-raised mid-discussion)

| Option | Description | Selected |
|--------|-------------|----------|
| Gone + unify rest | 6 Hyprland-related gone entirely; single page lists only remainder | ✓ |
| Keep as opt-in rows | Keep hyprland set as unticked-by-default toggleable rows | |

**User's choice:** Gone + unify rest
**Notes:** User verbatim: "the current install and uninstall setup shows two pages of checklist. but i want all of it to be unified as one single page." + "every app the setup script has the ability to install/uninstall is toggleable and each toggle will mean install the app and if there is a stowable config stow them, if untick never install never syow" [sic]. Mode/shell set pre-selection defaults only; gui extras lose their mode-governed bypass. Five-backend ladder and cancel-never-cascades preserved.

---

## the agent's Discretion

- Checklist clean-6 default (follow-up dismissed; planner may use disabled-row wofi instead if backend implementations require it).
- Exact new prompt_mode strings and unified-page row ordering/grouping.
- Debian keyd manual-prompt exact wording.

## Deferred Ideas

- Debian keyd fully-automated git-build inside setup.sh (Phase 2.1 only prompts + links).
- AUTO-01/AUTO-02 backup + CI remain v2.
