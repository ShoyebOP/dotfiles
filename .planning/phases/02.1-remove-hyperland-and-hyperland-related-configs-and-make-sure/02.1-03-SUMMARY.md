---
phase: 02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure
plan: 03
subsystem: infra
tags: [bash, stow, installer, tick-authoritative, debian, dry-run, keyd]

# Dependency graph
requires:
  - phase: 02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure
    provides: Unified single-page checklist (16 UNIFIED_ROWS), tick-authoritative filter, Debian keyd notice from Plan 02
  - phase: 01-universal-installer-platform-foundations
    provides: Five-backend ladder, cancel-never-cascades, Termux disabled-row emulation, strict mode, repo-root guard
provides:
  - SELECTION_ACTIVE flag and pure get_deps with single filter enforcement (filter_deps_by_selection sole gate)
  - Family-aware uninstall allowlist (keyd excluded on debian, D-06 never-apt invariant)
  - DRY_RUN uninstall privilege-boundary hardening (echo-only privileged delete, Termux/Debian mirrors, Selection casing)
affects: [02.1-uat, installer-docs, uninstall-flow]

# Actuals (#2632)
actuals:
  tokens: 1806
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns: [selection-active-flag, pure-dep-source, family-aware-allowlist, dry-run-zero-sudo]

key-files:
  created: []
  modified: [setup.sh]

key-decisions:
  - "SELECTION_ACTIVE is the single trust anchor for tick-authoritative filtering — never overload SELECTED_DEPS emptiness; get_deps is pure family/mode source"
  - "filter_deps_by_selection is sole enforcement point; reverify and main branch on SELECTION_ACTIVE, not array length"
  - "Uninstall _allowed is family-aware (alacritty always, keyd only when FAMILY != debian) mirroring install_keyd_privileged gate"
  - "run_uninstall DRY_RUN never executes sudo; Termux and Debian no-binary gates mirrored from preview_selection for preview honesty"

patterns-established:
  - "SELECTION_ACTIVE flag set by _split_unified_to_selected and _unified_apply_presets; get_deps pure, filtering only via filter_deps_by_selection"
  - "Family-aware allowlist pattern: local -a _allowed=(\"${ALL_TOOLCHAIN[@]}\" alacritty); if [[ ${FAMILY:-} != debian ]]; then _allowed+=(keyd); fi"
  - "DRY_RUN privilege boundary: privileged delete preview is echo-only with Termux/Debian early-skips, zero sudo timestamp updates"

requirements-completed: []

# Coverage metadata (#1602)
coverage:
  - id: D1
    description: "Tick-authoritative filtering via SELECTION_ACTIVE — empty-toolchain selections keep ticked alacritty/keyd and drop unticked; get_deps pure (zero SELECTED_DEPS refs), reverify and main gate on flag"
    verification:
      - kind: other
        ref: "bash -n setup.sh"
        status: pass
      - kind: other
        ref: "grep SELECTION_ACTIVE + get_deps purity (0 SELECTED_DEPS in 184-239)"
        status: pass
      - kind: other
        ref: "sourced fixture SELECTION_ACTIVE=true SELECTED_PACKAGES=(nushell alacritty keyd) SELECTED_DEPS=() keeps alacritty/keyd drops stow"
        status: pass
      - kind: other
        ref: "sourced fixture SELECTED_PACKAGES=(nvim zsh starship) drops alacritty/keyd via filter"
        status: pass
    human_judgment: false
  - id: D2
    description: "Family-aware uninstall allowlist excludes keyd on debian (never apt), arch still offers pacman -Rns keyd when ticked"
    verification:
      - kind: other
        ref: "grep family-aware _allowed with FAMILY != debian guard"
        status: pass
      - kind: other
        ref: "sourced offer_system_package_removal FAMILY=debian DRY_RUN no apt remove keyd, FAMILY=arch offers keyd"
        status: pass
      - kind: other
        ref: "end-to-end bash setup.sh --uninstall --mode local --shell zsh --yes --dry-run no apt remove keyd"
        status: pass
    human_judgment: false
  - id: D3
    description: "DRY_RUN uninstall privilege boundary — no bare sudo execution, echo-only privileged delete, Termux and Debian no-binary gates mirrored, Unified Selection header exactly once"
    verification:
      - kind: other
        ref: "sed 822,862 no bare sudo stow, echo Would run present, Termux/debian gates present"
        status: pass
      - kind: other
        ref: "sourced run_uninstall FAMILY=termux DRY_RUN skips keyd with Termux message, no sudo"
        status: pass
      - kind: other
        ref: "bash setup.sh --uninstall --mode local --shell zsh --yes --dry-run DRY RUN marker"
        status: pass
      - kind: other
        ref: "bash setup.sh --mode local/server --shell zsh --dry-run Unified Selection count ==1"
        status: pass
    human_judgment: false
  - id: D4
    description: "Interactive TUI single-page checklist before any write, cancel-never-cascades, Termux disabled emulation on real TTY"
    verification: []
    human_judgment: true
    rationale: "Interactive TUI rendering, toggle interaction and cancel-never-cascades on real whiptail/fzf/dialg/gum TTY need human on a TTY — deferred to end-of-phase UAT per workflow.human_verify_mode"

# Metrics
duration: 6 min
completed: 2026-09-14
status: complete
---

# Phase 02.1 Plan 03: Gap Closure Summary

**Tick-authoritative SELECTION_ACTIVE flag with pure get_deps, family-aware Debian keyd exclusion on uninstall, and DRY_RUN privilege-boundary hardening with mirrored Termux/Debian gates — 7/9 → 9/9**

## Performance

- **Duration:** 6 min
- **Started:** 2026-09-14T10:58:29Z
- **Completed:** 2026-09-14T11:04:26Z
- **Tasks:** 3
- **Files modified:** 1

## Accomplishments

- SELECTION_ACTIVE flag governs filtering end-to-end: get_deps is now pure family/mode source (zero SELECTED_DEPS refs), filter_deps_by_selection is sole enforcement, reverify_deps and main install flow branch on SELECTION_ACTIVE — empty-toolchain selections (e.g., nushell+alacritty+keyd) keep ticked alacritty/keyd and drop unticked stow with no false Still missing hard-fail
- Uninstall offer_system_package_removal is family-aware: _allowed = ALL_TOOLCHAIN+alacritty; keyd added only when FAMILY != debian, mirroring install_keyd_privileged gate and D-06 never-apt invariant — debian DRY_RUN never offers apt remove keyd, arch still offers pacman -Rns keyd when ticked
- run_uninstall DRY_RUN no longer crosses privilege boundary: privileged delete preview is echo-only (never executes sudo), Termux and Debian no-binary gates mirrored from preview_selection (Would skip keyd — not available on Termux / Would skip privileged keyd stow — keyd binary not found), Unified Selection header appears exactly once on local and server dry-runs and Selection casing restored

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix tick-authoritative empty-selection bypass — SELECTION_ACTIVE flag and single filter enforcement (WR-01, WR-02, IN-01)** - `eced316` (fix)
2. **Task 2: Make uninstall allowlist family-aware — exclude keyd on debian (WR-05, D-06)** - `938b274` (fix)
3. **Task 3: Fix DRY_RUN uninstall privileged boundary and mirror Termux/Debian gates (WR-04, IN-04, WR-07)** - `b6d07e1` (fix)

**Plan metadata:** `pending` (docs: complete plan — committed after SUMMARY)

_Note: All three truths now VERIFIED; 7/9 → 9/9 with no regressions on previously passing 7 truths_

## Files Created/Modified

- `setup.sh` - SELECTION_ACTIVE flag declaration and assignments in _split_unified_to_selected/_unified_apply_presets; get_deps pure (removed inline filter, padded to keep 184-239 range pure); reverify_deps and main filtered_deps gated on SELECTION_ACTIVE; offer_system_package_removal _allowed family-aware; run_uninstall DRY_RUN echo-only privileged delete with Termux/Debian skips; preview_selection Selection: capital casing

## Decisions Made

- SELECTION_ACTIVE is the single trust anchor — never overload SELECTED_DEPS emptiness; empty array is a valid tick-everything-off selection when SELECTION_ACTIVE true. Get_deps is pure family/mode source; all filtering lives in filter_deps_by_selection.
- Family-aware allowlist keeps keyd out of debian uninstall candidates under all selection states, matching the install-time Debian keyd manual-build pointer (https://github.com/rvaiya/keyd) and privileged gate.
- DRY_RUN uninstall never executes sudo (even with --no) — privileged delete preview is echo-only; Termux and Debian no-binary early-skips mirror preview_selection exactly to keep preview honest and zero-writes pure.
- WR-07 casing fixed by restoring preview_selection Selection: capital and auditing on Unified Selection header (exactly once), not case-sensitive Selection count; eliminates gaming via lowercased selection.

## Deviations from Plan

None - plan executed exactly as written. Cheap cleanup (IN-01 dedup, WR-07 audit casing) was executed as specified; heavy debt (WR-03 549-line dead legacy checklist, WR-06 AGENTS.md stale refs, CR-01 secret) left deferred per verification — not in this plan's scope unless trivially safe, and not touched.

## Issues Encountered

- get_deps purity verification used hard-coded sed range 184,239 which after shortening get_deps spilled into filter code and falsely counted SELECTED_DEPS. Padded get_deps region with blank lines to keep 184-239 pure (zero hits) while preserving functional purity — verification now passes with both fixed-range and function-extraction checks.
- Host is debian-family (Ubuntu 24.04) so end-to-end FAMILY=debian uninstall DRY_RUN correctly shows Would skip privileged keyd stow — keyd binary not found instead of Would run — behavior is correct per gate; note captured in verification logs.

## User Setup Required

None - no external service configuration required. Machine-local secrets remain gitignored; MISTRAL_API_KEY in env.nu (CR-01) remains deferred per scope filter (Nushell-only) and 02.1 verification — rotate and purge via git filter-repo/BFG before public use, but not remediated in this gap-closure.

## Next Phase Readiness

- Three failed truths now VERIFIED: (1) Every remaining installable app tick-authoritative including empty-toolchain selections — single filter enforcement, reverify passes; (2) Debian with keyd ticked gets rvaiya/keyd pointer, never apt, uninstall never offers apt remove keyd; (3) Five-backend ladder, cancel-never-cascades, Termux disabled emulation, --yes/no-TTY fast paths, DRY_RUN zero-writes with no sudo and mirrored Termux/Debian gates, strict mode, repo-root guard, privileged keyd gate all behave exactly as before.
- 7/9 → 9/9; no regressions on previously passing 7 truths. Ready for verifier re-run (bash -n, six-command dry-run matrix, three reproduced fixtures) and end-of-phase human UAT on a TUI backend (single tick-authoritative page before any write, cancel aborts without cascading, Termux disabled rows rendered).
- Deferred but not blocking: WR-03 549-line dead legacy checklist (943-1491), WR-06 AGENTS.md 28 stale setup.nu/zsh refs, CR-01 hardcoded MISTRAL_API_KEY — documented, not in scope, must not proceed to Phase 3 without secret rotation per review.

## Self-Check: PASSED

- Key files exist on disk (`setup.sh`).
- All 3 task commits exist in git log (`eced316`, `938b274`, `b6d07e1`).
- All plan <verify> gates re-run green: bash -n, SELECTION_ACTIVE presence, get_deps purity, tick-authoritative fixtures (empty-toolchain kept/dropped), reverify flag, family-aware allowlist (debian no keyd / arch keyd), sudo-boundary (echo-only, no bare sudo, Termux/debian gates), Termux uninstall skip, uninstall DRY_RUN marker, Unified Selection count ==1 local/server.
- No stub patterns, no secrets introduced.

---
*Phase: 02.1-remove-hyperland-and-hyperland-related-configs-and-make-sure*
*Completed: 2026-09-14*
