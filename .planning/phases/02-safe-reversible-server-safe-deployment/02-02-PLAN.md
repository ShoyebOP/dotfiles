---
phase: 02-safe-reversible-server-safe-deployment
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [setup.sh, zsh/.zshrc, README.md, AGENTS.md, teardown.zsh, teardown.nu]
autonomous: true
requirements: [SHEL-01, DOCS-01]
estimate:
  tokens: 26000
  raw_tokens: 13000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "User gets Zsh provisioned before stow completes — installer ensures `zsh` binary present via common deps, Zinit self-clones on first zsh launch via zsh/.zshrc (installer does NOT clone/pin), and `chsh -s $(which zsh)` is offered only at the very end after explicit `yes` (never auto, --yes does not bypass, --dry-run previews, Termux/chsh-missing skipped, already-zsh skipped)"
    - "User reads README.md, setup.sh usage header, zsh/.zshrc header and sees `Default: Zsh | Backup: Nushell`, primary example `bash setup.sh --mode local` / `--dry-run` and correct `stow --restow nvim zsh starship` vs `nvim nushell starship` vs GUI extras; least-privilege sudoers documented as `systemctl reload keyd`/`keyd reload` not start/stop"
    - "User finds `teardown.zsh`/`teardown.nu` deleted after `bash setup.sh --uninstall` ships and docs contain no dangling teardown references — staged delete atomically fixes docs, recoverable via git history"
  artifacts:
    - path: "setup.sh"
      provides: "Zsh binary ensured via common deps before stow, end-of-run chsh offer with dry-run preview and Termux/--yes/already-zsh guards"
      contains: "offer_chsh"
    - path: "zsh/.zshrc"
      provides: "Header reflects Zsh default, Zinit self-clone block untouched and unpinned"
      contains: "zinit"
    - path: "README.md"
      provides: "Shell-path table Default Zsh|Backup Nushell, quick-start Zsh examples, manual stow core vs backup one-liners, keyd preview+suiders, Zinit self-clone note"
      contains: "Default: Zsh"
    - path: "AGENTS.md"
      provides: "Shell default guidance flipped to Zsh canonical if shell-guidance prose exists"
      contains: "Zsh"
  key_links:
    - from: "setup.sh:get_deps common"
      to: "setup.sh:run_stow zsh"
      via: "zsh binary present via verify→install→re-verify before stow --restow zsh; Zinit not cloned by installer per D-12"
      pattern: "offer_chsh"
    - from: "setup.sh:offer_chsh"
      to: "OS chsh"
      via: "offer only after quarantine_scan+run_stow+post_verify succeed, which zsh != $SHELL, explicit Type yes, --yes does NOT bypass, dry-run previews, Termux skipped"
      pattern: "chsh -s"
    - from: "README.md"
      to: "setup.sh usage()"
      via: "both show Default: Zsh | Backup: Nushell and bash setup.sh --mode local as primary example"
      pattern: "Default: Zsh"
  prohibitions:
    - "No installer git clone of Zinit and no commit pin per D-12"
    - "No auto chsh without explicit Type yes and no --yes bypass of chsh per D-13"
    - "No deletion of Nushell mentions/column — keep backup column per D-14"
    - "No teardown shims — git rm delete, recoverable via history per Phase 1 D-03"
    - "No new persisted mode file per D-11"
---

<!-- planner-region-allow: teardown -->
<!-- planner-region-allow: deferred to Phase 2 -->
<!-- planner-region-allow: git clone.*zinit -->

# Plan 02-02: Zsh self-provision (Zinit) + docs flipped to Zsh default + staged teardown delete

> **As a** user who clones fresh and reads the docs, **I want to** see Zsh as the documented default with correct quick-start and manual stow examples, get Zsh provisioned automatically without manual Zinit cloning, and be offered a safe chsh only at the very end **so that** my default path is unambiguous and my shell change is never surprising.

<objective>
Flip documentation to `Default: Zsh | Backup: Nushell` everywhere (`README.md` shell-path table, quick-start primary example `bash setup.sh --mode local` / `--mode local --dry-run` + server Zsh dry-run, manual `stow --restow nvim zsh starship` core vs `nvim nushell starship` backup vs GUI `nvim zsh starship alacritty wofi`, keyd preview + `diff -u` + least-privilege sudoers `NOPASSWD: /usr/bin/systemctl reload keyd, /usr/bin/keyd reload` per D-09, Zinit self-clone note per D-12, footer intact), update `setup.sh` header `usage()` comments and `zsh/.zshrc` header/inline comments to reflect Zsh default while leaving the `zsh/.zshrc` Zinit self-clone block untouched (no pin, no installer clone), ensure `zsh` binary is present via Phase 1 common deps before `stow --restow zsh`, offer `chsh -s $(which zsh)` once at the very end after `quarantine_scan` + `run_stow` + `post_verify` succeed and only after explicit `Type 'yes'` (never auto, `--yes` does NOT bypass, `--dry-run` prints preview, skip if `which zsh == $SHELL`, skip on Termux and when chsh missing), and staged-delete `teardown.zsh`/`teardown.nu` and atomically fix docs per D-15 (no shims).

Purpose: Completes the user-visible canon flip and shell-provision polish that the tracer 02-01 did not cover. After this plan, docs say Zsh default throughout, installer provisions Zsh before stow, chsh is safely offered, and the old teardowns are gone without dangling pointers.
Output: Docs read Zsh-default, `zsh/.zshrc` still self-clones Zinit, `setup.sh` ends with a safe chsh offer, teardowns deleted, all `bash -n` clean.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-safe-reversible-server-safe-deployment/02-CONTEXT.md
@.planning/phases/02-safe-reversible-server-safe-deployment/02-RESEARCH.md
@.planning/phases/02-safe-reversible-server-safe-deployment/02-PATTERNS.md
@setup.sh
@zsh/.zshrc
@README.md
@AGENTS.md
@teardown.zsh
@teardown.nu
</context>

<tasks>

<task type="auto">
  <name>Zsh provision already via deps + Zinit self-clone untouched + end-of-run chsh offer</name>
  <files>setup.sh, zsh/.zshrc</files>
  <read_first>setup.sh (post-01 tracer: run_uninstall, install_keyd_privileged, get_deps arch/debian/termux common includes zsh, SELECTED_DEPS/ALL_TOOLCHAIN, FAMILY, DRY_RUN, YES, verify_deps→install_deps→reverify_deps, quarantine_scan, run_stow, post_verify, main flow with checklist before any write, strict header set -Eeuo pipefail; shopt -s inherit_errexit, SCRIPT_DIR, ${1-} guards, outside-repo-root guard); zsh/.zshrc lines 1-6 header + 134-164 Zinit self-clone block via git clone https://github.com/zdharma-continuum/zinit and source; 02-CONTEXT.md D-12 D-13; 02-RESEARCH.md Pattern 4 Zsh Self-Provision + Alternatives Considered --yes does NOT bypass chsh; 02-PATTERNS.md zsh/.zshrc analog + chsh end-of-run offer pattern</read_first>
  <action>Wire SHEL-01 per D-12 and D-13 reusing Phase 1 deps tables and without duplicating shell-runtime cloning, keeping Bash strict invariants and Termux/--yes/dry-run contracts.

On zsh/.zshrc leave the Zinit self-clone block lines 138-164 untouched: do not move cloning into setup.sh, do not add commit pinning, do not edit the git clone URL or chmod/chown lines. Only edit the file header comment lines 1-6 to clarify Zsh as default, e.g. change header to note ZSH Configuration File (.zshrc) — default shell, Nushell is backup per D-14 docs header rule, and add or keep an inline comment near the Zinit block stating Zinit clones itself on first zsh launch via this block — no installer clone, no commit pin per D-12 if not already present. No functional change to the bootstrap.

On setup.sh confirm zsh binary is ensured before stow via the existing common deps: get_deps common already lists zsh for arch/debian/termux and verify_deps maps neovim→nvim etc. but includes zsh 1:1, so no new dep table entry is needed — add a comment near get_deps common arrays stating zsh in common ensures Zsh provision before stow per D-12 and Phase 1 make+gcc+fzf+zsh decision. Ensure main still wires verify→install→re-verify before quarantine_scan/run_stow/post_verify so zsh is installed before stow --restow zsh.

Add offer_chsh function defined before main, gated to run only on install path (not uninstall) and only after quarantine_scan plus run_stow plus post_verify have all succeeded and before the final echo Setup complete. Implement guards in order: resolve zsh_path via command -v zsh, if not found warn Warning: zsh not found — cannot offer chsh. and return 0; if SHELL is set and zsh_path equals SHELL skip with Default shell already zsh (…) — skipping chsh offer. and return 0; if FAMILY is termux print Termux detected — chsh not applicable; skipping default shell change. and return 0; if command -v chsh missing print chsh not found — skipping. Run manually: chsh -s zsh_path to stderr and return 0; if DRY_RUN is true print [DRY RUN] Would run: chsh -s zsh_path and return 0 before any prompt; otherwise prompt with printf Change default shell to zsh? Type 'yes' to run chsh -s %s: zsh_path to stderr then read -r reply and require exact case-sensitive yes, on mismatch print Skipping chsh (expected yes). Manually run: chsh -s zsh_path and return 0 (do not error); on yes run chsh -s zsh_path and on failure warn Warning: chsh failed (check /etc/shells contains … , or use sudo chsh). and return 1 but wrapped with || true at call site so installer never kills session. Ensure --yes does NOT bypass this prompt per D-13: do not check YES inside offer_chsh except for the early DRY_RUN preview; the prompt always requires typed yes even when YES is true. Insert call after post_verify success as offer_chsh || true and before final success echo.

Add DRY_RUN preview for chsh in the DRY_RUN fast-path block that prints === Preview ===: also print [DRY RUN] Would run: chsh -s $(which zsh) when zsh binary exists and SHELL differs, mirroring the live offer.

Keep set -Eeuo guards: split local zsh_path assignment from command substitution to avoid masking, quote all expansions, and add || true around keyd reload and chsh at call sites.</action>
  <verify>
    <automated>bash -n setup.sh && bash -n zsh/.zshrc 2>&1 | head -5 || echo "zsh lint optional"</automated>
    <automated>grep -q "offer_chsh" setup.sh && echo "offer_chsh present"</automated>
    <automated>grep -q "Zinit.*self-clones\|self-clone" setup.sh || grep -q "Zinit clones itself" zsh/.zshrc && echo "self-clone comment present"</automated>
    <automated>bash setup.sh --dry-run --mode server --shell zsh --yes 2>&1 | grep -q "Would run: chsh" || echo "dry-run chsh preview when not already zsh"</automated>
    <automated>grep -q "Change default shell to zsh" setup.sh && ! grep -q "YES.*chsh\|--yes.*chsh.*bypass" setup.sh && echo "chsh not --yes bypassed"</automated>
    <automated>grep -q "TERMUX.*chsh\|termux.*chsh" setup.sh && echo "termux gate present"</automated>
  </verify>
  <done>Zsh binary ensured before stow via common deps, Zinit self-clone left untouched with header clarified, chsh offered only at very end after explicit yes, never via --yes, dry-run previews, Termux and already-zsh skipped, installer does not clone Zinit</done>
  <acceptance_criteria>File setup.sh contains string offer_chsh; File setup.sh contains string Change default shell to zsh? Type 'yes'; File zsh/.zshrc contains string zinit or Zinit; Command bash -n setup.sh exits 0; Output of bash setup.sh --dry-run --mode server --shell zsh --yes contains Would run or already zsh hint without prompting</acceptance_criteria>
</task>

<task type="auto">
  <name>Docs full flip keep backup column + staged delete teardowns</name>
  <files>README.md, AGENTS.md, setup.sh, teardown.zsh, teardown.nu, zsh/.zshrc</files>
  <read_first>README.md (lines 7-18 shell-path table, 22-59 Unified Bash Installer intro + quick-start + OPTIONS - --yes reserved --uninstall deferred bulleted, 82-99 Manual stow core vs backup one-liners, 101-116 keyd Setup Phase 2 placeholder + sudoers start|stop snippet, 117-122 Zsh Plugin Manager git clone comment, 154-156 footer); setup.sh lines 26-48 usage heredoc OPTIONS and EXAMPLES; zsh/.zshrc lines 1-6 header; AGENTS.md lines 11-19 Constraints shell default block; teardown.zsh + teardown.nu (files to delete, 185/155 lines, teardown yes guard, rm -rf delete_stow_directory banned); 02-CONTEXT.md D-09 D-14 D-15; 02-PATTERNS.md README.md analog + AGENTS.md analog + teardown DELETE contract</read_first>
  <action>Execute DOCS-01 full flip per D-14 and least-privilege sudoers per D-09 and staged delete per D-15 atomically in one commit, keeping Nushell backup column and Phase 1 spine intact, prose only with no code fences in this action.

On README.md keep the Feature Zsh (Default) Nushell (Backup) pipe-table shape and the Default: Zsh Backup: Nushell callout exactly. Update OPTIONS bullet for --yes from reserved for Phase 2 … to Assume yes for prompts (CI bypass for --uninstall and privileged flows). Rewrite --uninstall, --remove bullet from deferred to Phase 2; use teardown scripts for now to cleanly unstows selected configs via stow -D plus privileged keyd cleanup plus Mason artefacts when nvim deselected plus offers system package removal; typed yes required (bypass with --yes). Keep the teardown script names in this commit only until teardown files are staged-deleted in the same commit; then in the same edit remove any leftover Use teardown scripts for now hint after the files are deleted so no dangling pointer remains.

Update Examples block to primary example bash setup.sh --mode local and bash setup.sh --mode local --dry-run and bash setup.sh --mode server --shell zsh --dry-run per D-14, keeping non-interactive one-liner order --mode before --shell and --dry-run trailing. Update Manual Installation stow one-liners to stow --dir=. --target="$HOME" --restow nvim zsh starship for core Zsh default versus nvim nushell starship for backup, and full local GUI to nvim zsh starship alacritty wofi; keep explicit --dir and --target flags.

Rewrite keyd Setup section from future prose Phase 2 will handle … to installed behavior: installer previews with stow --no --verbose -t / keyd plus diff -u if /etc/keyd/default.conf is a regular file, requires gum confirm / Type yes before sudo stow --adopt -t / keyd (plain sudo stow -t / keyd otherwise), then sudo keyd reload or sudo systemctl reload keyd or true. Replace sudoers snippet from NOPASSWD: /usr/bin/systemctl start keyd, /usr/bin/systemctl stop keyd to NOPASSWD: /usr/bin/systemctl reload keyd, /usr/bin/keyd reload per D-09 least-privilege. Never show sudo stow --adopt -t / keyd in docs without the confirmation gate note.

Update Zsh Plugin Manager section comment from will be cloned automatically on first shell start via bash setup.sh in Phase 2 to Zinit clones itself on first zsh launch via zsh/.zshrc — no installer clone, no commit pin per D-12, keep the git clone line as manual fallback, not as installer step.

Retain footer one-liner Installer: bash setup.sh --mode <local|server> --shell <zsh|nushell> [--dry-run] — Zsh default, Nushell backup, and retain Zsh (Default) Nushell (Backup) Tech Stack table without deleting the Nushell column.

On setup.sh update the usage heredoc header comment to state Unified Dotfiles Installer (Bash) — Zsh default, Nushell backup and ensure OPTIONS examples list bash setup.sh --mode local and --dry-run variants matching README per D-14.

On zsh/.zshrc ensure header reflects Zsh default as in prior task; do not duplicate edits.

On AGENTS.md if shell-guidance prose beyond the GSD project block exists that says Nushell vs Zsh default, flip it to Zsh default | Nushell backup mirroring README D-14; do not edit the <!-- GSD:project-start --> wrapper beyond human prose inside; keep the no package.json admonition.

Staged delete: run git rm teardown.zsh teardown.nu in the same commit that fixes docs, remove their mentions from README bullet and any AGENTS or R and D notes, no shims, no wrappers, recoverable via git history per Phase 1 D-03. After delete ensure grep -r teardown README.md AGENTS.md setup.sh returns zero hits except possibly historical CHANGELOG; verify with git status clean except .stow-conflicts.

Keep all edits 4-space indented where shell, LF, and existing pipe-table casing Zsh (Default) / Nushell (Backup) exactly.</action>
  <verify>
    <automated>grep -q "Default: Zsh" README.md && grep -q "Backup: Nushell" README.md && echo "zsh default backup present"</automated>
    <automated>grep -q "bash setup.sh --mode local" README.md && echo "primary example flipped"</automated>
    <automated>grep -q "stow --dir=. --target=\"\$HOME\" --restow nvim zsh starship" README.md && grep -q "stow --dir=. --target=\"\$HOME\" --restow nvim nushell starship" README.md && echo "manual stow both variants present"</automated>
    <automated>grep -q "reload keyd" README.md && ! grep -q "start keyd.*stop keyd" README.md && echo "least-privilege sudoers fixed"</automated>
    <automated>grep -q "Zinit clones itself" README.md && echo "zinit note updated"</automated>
    <automated>test ! -f teardown.zsh && test ! -f teardown.nu && echo "teardowns deleted"</automated>
    <automated>! grep -rq "teardown" README.md AGENTS.md 2>/dev/null && echo "no dangling teardown mentions"</automated>
    <automated>bash -n setup.sh && echo "setup.sh lint ok"</automated>
  </verify>
  <done>README, setup.sh usage, zsh/.zshrc header, AGENTS all read Default Zsh | Backup Nushell with correct stow examples and least-privilege sudoers; teardowns git-rm deleted and docs contain zero dangling teardown mentions; no Nushell column deleted; bash -n passes</done>
  <acceptance_criteria>File README.md contains string Default: Zsh and Backup: Nushell; File README.md contains string stow --dir=. --target="$HOME" --restow nvim zsh starship; File README.md contains string systemctl reload keyd; File teardown.zsh does not exist; File teardown.nu does not exist; Command bash -n setup.sh exits 0</acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Verify shell default canon and idempotent uninstall preview in docs/tests</name>
  <files>setup.sh, README.md</files>
  <read_first>setup.sh (full file: pre-scan --help wins, parse_args with UNINSTALL, main repo-root guards, FAMILY detection, mode/shell/checklist before any write, verify→install→re-verify, quarantine, run_stow delegating keyd, offer_chsh, run_uninstall, offer_system_package_removal, DRY_RUN preview blocks); README.md post-flip; 02-CONTEXT.md D-14 D-15 scope; 02-RESEARCH.md No new npm packages required block</read_first>
  <action>Final polish per D-14 and D-15 verification: ensure no dangling pointers and canon consistency between code and docs, without inventing new files.

Verify setup.sh OPTIONS parsed after flip still show --mode, --shell, --dry-run, --yes, --help, --uninstall/--remove with --help wins anywhere semantics intact; ensure bash setup.sh --help exits 0 anywhere even when combined with --uninstall per strict header contract. Ensure bash setup.sh --dry-run --mode server --shell zsh --yes and bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes both emit [DRY RUN] Would run: prefixes and zero writes, and that second dry-run uninstall is idempotent.

Grep the repo for remaining Nushell-primary phrasing outside README backup column: ensure no file still says Nushell default or Default: Nushell; allow Backup: Nushell only. Grep for remaining deferred to Phase 2 teardown hints; ensure zero hits after staged delete.

Ensure zsh/.zshrc still contains the Zinit self-clone git clone line as manual fallback, and ensure setup.sh does not contain git clone.*zinit outside of zsh/.zshrc (no installer clone per D-12).

Leave nvim/.config/nvim/README.md untouched if absent (it was deleted pre-Phase-1 per D-14); do not recreate it.

Add no new mode file; ensure grep -R "dotfiles/mode" and grep -R "\.dotfiles-mode" outside of the legacy rm -f lines returns zero hits, confirming D-11 no persistence.</action>
  <verify>
    <automated>bash setup.sh --help 2>&1 | grep -q "Usage:" && bash setup.sh --uninstall --help 2>&1 | grep -q "Usage:" && echo "help wins anywhere"</automated>
    <automated>bash setup.sh --dry-run --mode server --shell zsh --yes 2>&1 | grep -q "\[DRY RUN\]" && bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes 2>&1 | grep -q "\[DRY RUN\]" && echo "both dry-runs preview"</automated>
    <automated>! grep -rq "Default: Nushell" README.md setup.sh zsh/.zshrc 2>/dev/null && echo "no nushell default"</automated>
    <automated>! grep -rq "deferred to Phase 2" README.md setup.sh 2>/dev/null && echo "no deferred hint"</automated>
    <automated>grep -q "git clone.*zinit" zsh/.zshrc && ! grep -q "git clone.*zinit" setup.sh && echo "zinit only in zshrc"</automated>
    <automated>! grep -R "dotfiles/mode" setup.sh | grep -v "rm -f" && ! grep -R "\.dotfiles-mode" setup.sh | grep -v "rm -f" && echo "no mode file writes"</automated>
  </verify>
  <done>Help wins anywhere still exits 0, both install and uninstall dry-runs preview with zero writes and idempotent second run, no Nushell-default phrasing remains, no deferred teardown hint remains, Zinit clone only in zshrc, no new mode file writes</done>
  <acceptance_criteria>Output of bash setup.sh --help contains Usage:; Output of bash setup.sh --dry-run --mode server --shell zsh --yes contains [DRY RUN]; File setup.sh does not contain line git clone.*zinit outside comment; File zsh/.zshrc contains string zinit</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → setup.sh parse_args | Untrusted input; --help must win before any write, unknown flags abort nonzero |
| setup.sh → HOME via stow --restow zsh | Symlink deployment; must ensure zsh binary before stow, idempotent |
| setup.sh → OS user account via chsh | Changes /etc/passwd login shell; must never auto-run, requires explicit yes |
| Docs → user expectations (README.md, AGENTS.md) | Docs must not promise pre-Phase-2 behavior (--adopt without gate, Nushell default) |
| teardown deletion → git history | Reversibility via git history, not shims |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Spoofing | setup.sh usage/docs mismatch | medium | mitigate | Keep README.md quick-start and setup.sh usage() heredoc in sync: both show Default Zsh | Backup Nushell and bash setup.sh --mode local as primary, verified by grep |
| T-02-09 | Tampering | setup.sh installer cloning Zinit | medium | mitigate | Never run git clone for Zinit in setup.sh per D-12; leave self-clone in zsh/.zshrc only, commented as no installer pin |
| T-02-10 | Elevation of Privilege | chsh -s $(which zsh) | high | mitigate | Offer only after quarantine_scan+run_stow+post_verify succeed, require exact Type 'yes', --yes does NOT bypass, --dry-run prints [DRY RUN] Would run: chsh, Termux skipped, already-zsh skipped, wrapped with \|\| true |
| T-02-11 | Repudiation | teardown staged delete without doc fix | low | mitigate | git rm teardown.zsh teardown.nu atomically in same commit that rewrites README --uninstall bullet and keyd sudoers; no shim, recoverable via git history |
| T-02-12 | Denial of Service | chsh failure kills installer | medium | mitigate | chsh errors warn but do not abort deployment; offer_chsh called as offer_chsh \|\| true, and keyd reload similarly \|\| true |
| T-02-13 | Information Disclosure | docs still advertise blank privileged --adopt or start/stop sudoers | medium | mitigate | Rewrite keyd section to preview+diff+adopt gate and replace sudoers with least-privilege reload keyd / keyd reload only per D-09 |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package manager installs in this plan; host tools only (bash, stow, chsh, git); package legitimacy audit N/A |
</threat_model>

<verification>
- `bash -n setup.sh && bash -n zsh/.zshrc` (zsh lint optional) exits 0; `bash setup.sh --help` and `bash setup.sh --uninstall --help` exit 0 with Usage: before any write
- `bash setup.sh --dry-run --mode server --shell zsh --yes` prints `[DRY RUN] Would run: chsh -s` when not already zsh, or skips with already zsh message; `--yes` does NOT bypass chsh prompt in live run
- `grep -q "git clone.*zinit" zsh/.zshrc` succeeds and `! grep -q "git clone.*zinit" setup.sh` succeeds (no installer clone)
- `grep -q "Default: Zsh" README.md && grep -q "Backup: Nushell" README.md && grep -q "stow --dir=. --target=\"\$HOME\" --restow nvim zsh starship" README.md && grep -q "systemctl reload keyd" README.md` all succeed; `! grep -q "teardown" README.md` and `test ! -f teardown.zsh && test ! -f teardown.nu` succeed after staged delete
- Second `bash setup.sh --dry-run --mode local --dry-run` and `bash setup.sh --dry-run --uninstall` remain idempotent with zero writes
</verification>

<success_criteria>
- Zsh provisioned before stow completes (zsh binary ensured via common deps, no installer Zinit clone, self-clone on first zsh launch intact)
- chsh offered only at very end after explicit yes, never auto, never via --yes, Termux skipped, dry-run previews
- Docs read Default: Zsh | Backup: Nushell throughout with correct stow one-liners and least-privilege keyd sudoers
- Teardowns deleted and no dangling teardown mentions remain; Nushell backup column preserved
</success_criteria>

<output>
Create `.planning/phases/02-safe-reversible-server-safe-deployment/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Exported As |
|--------|------|------|-------------|
| get_deps common zsh | dep table entry | setup.sh | `common=(... zsh)` before stow |
| offer_chsh | function | setup.sh | `offer_chsh()` called after post_verify, explicit yes, Termux/--yes/already-zsh guards, dry-run preview |
| zsh/.zshrc header | comment | zsh/.zshrc | Header reflects Zsh default, self-clone block untouched |
| README.md Zsh default flip | docs | README.md | Table, quick-start, manual stow, keyd preview+suiders, Zinit note, footer |
| AGENTS.md Zsh canon | docs | AGENTS.md | Shell default prose flipped if present |
| setup.sh usage Zsh default | CLI help | setup.sh | `usage()` shows bash setup.sh --mode local as primary, Zsh default note |
| teardown.zsh / teardown.nu deletion | file removal | (deleted) | `git rm` atomically with docs fix |
