---
phase: 02-safe-reversible-server-safe-deployment
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified: [setup.sh, zsh/.zprofile]
autonomous: true
requirements: [INST-03, STOW-02, STOW-03]
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "User runs `bash setup.sh --uninstall` (or --remove) and must type `yes` (bypass with --yes for CI) before `stow -D` + `sudo stow -D -t / keyd` removes links and Mason artefacts are cleaned when Neovim was deselected — second uninstall and re-install are idempotent"
    - "User who selects `keyd` sees `stow --no --verbose -t / keyd` preview and `diff -u` if `/etc/keyd/default.conf` exists and is not a symlink; installer requires `gum confirm` / `Type 'yes'` before `sudo stow --adopt -t / keyd`, otherwise uses plain `sudo stow -t / keyd` then `keyd reload`/`systemctl` with least-privilege handling"
    - "User on server mode can log in on tty1 without session death — `zsh/.zprofile` Hyprland exec block is deleted, no persisted `~/.config/dotfiles/mode` file is written or read, legacy mode file removed silently if present"
  artifacts:
    - path: "setup.sh"
      provides: "Reversible uninstall verb, privileged keyd safety gate with preview/diff/adopt ladder, DRY_RUN preview, typed yes gate, idempotent stow -D"
      contains: "run_uninstall"
    - path: "zsh/.zprofile"
      provides: "Empty/login-safe Zsh profile with no Hyprland autostart"
      contains: "no Hyprland autostart"
  key_links:
    - from: "setup.sh:parse_args"
      to: "setup.sh:run_uninstall"
      via: "--uninstall/--remove sets UNINSTALL=true before any write; main branches to uninstall path after repo-root guard and before checklist"
      pattern: "UNINSTALL"
    - from: "setup.sh:run_uninstall"
      to: "setup.sh:stow --dir"
      via: "idempotent loops call stow -D per SELECTED_PACKAGES and sudo stow -D -t / for keyd"
      pattern: "stow.*--delete"
    - from: "setup.sh:install_keyd_privileged"
      to: "/etc/keyd/default.conf"
      via: "preview stow --no --verbose -t / plus diff -u, then gum confirm → Type yes fallback, adopt only on explicit adopt confirmation, then reload || true"
      pattern: "keyd reload"
  prohibitions:
    - "No rm -rf on repo stow package directories (nvim/zsh/etc.) during uninstall — stow -D only per D-01"
    - "No auto-deletion of .stow-conflicts quarantine on uninstall per D-04"
    - "No blind stow --adopt without preview diff and explicit adopt confirmation per D-07"
    - "No persisted mode file creation or read per D-11"
    - "No chsh bypass via --yes (chsh gate is Phase 2 Plan 02)"
---

<!-- planner-region-allow: rm -rf.*\.stow-conflicts -->

# Plan 02-01: Reversible uninstall + privileged keyd safety gate + Hyprland server guard

> **As a** user on any supported distro or Termux, **I want to** preview, explicitly confirm, and cleanly reverse my deployment including privileged keyd writes **so that** I never lose files, never get surprise /etc writes, and server logins never die on tty1.

<objective>
Deliver the reversible, previewable, safe deployment slice: `bash setup.sh --uninstall/--remove` with typed `yes` + `--yes` CI bypass + `--dry-run` preview and idempotent `stow -D` including privileged `sudo stow -D -t / keyd`, the privileged keyd install safety gate (`stow --no --verbose -t /` preview + `diff -u` when host file is regular file + `gum confirm` → `Type 'yes'` fallback + conditional `--adopt` + `keyd reload || true` with least-privilege docs deferred to 02-02), and removal of the `zsh/.zprofile` Hyprland `exec start-hyprland` block (no persisted mode file). Reuse Phase 1 spine verbatim: `set -Eeuo pipefail; shopt -s inherit_errexit; SCRIPT_DIR` guard, `${1-}` guards, `--help` wins, `DRY_RUN` early-return before every mutation, outside-repo-root guard, `detect_family` 4-tier, `verify→install→re-verify`, checklist ladder, `mv`-only quarantine to `.stow-conflicts/<timestamp>/MANIFEST`, folding-aware `readlink -f` post-verify.

Purpose: This tracer proves the uninstall path, the privileged keyd gate, and the server-login fix end-to-end before docs/Zsh polish in 02-02. After this plan, a user can install, preview uninstall, confirm with `yes`, uninstall, re-install, and log in on tty1 server without session death — all with dry-run safety.
Output: Modified `setup.sh` (adds uninstall verb + keyd privileged gate) and `zsh/.zprofile` (empty/no-exec), both `bash -n` clean and `bash setup.sh --help` still exits 0 anywhere.
</objective>

<execution_context>
@/home/shoyeb/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shoyeb/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-safe-reversible-server-safe-deployment/02-CONTEXT.md
@.planning/phases/02-safe-reversible-server-safe-deployment/02-RESEARCH.md
@.planning/phases/02-safe-reversible-server-safe-deployment/02-PATTERNS.md
@.planning/phases/01-universal-installer-platform-foundations/01-CONTEXT.md
@setup.sh
@zsh/.zprofile
@teardown.zsh
@teardown.nu
@README.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Tracer: uninstall verb + idempotent stow -D + keyd preview/diff/gate + zprofile exec removal — one path only</name>
  <files>setup.sh, zsh/.zprofile</files>
  <read_first>setup.sh (lines 1-14 strict header + globals; 26-137 usage + parse_args with --help pre-scan + ${1-} guards; 139-175 detect_family 4-tier + OS_RELEASE_FILE seam; 177-242 get_deps; 260-337 verify/install/reverify; 380-482 preview_selection/quarantine_scan/assert_linked/post_verify/run_stow; 1036-1162 main with repo-root guard and DRY_RUN fast-path); teardown.zsh lines 86-101 privileged unstow + 161-169 Type yes guard; teardown.nu lines 32-41 yes guard; zsh/.zprofile lines 1-4 exec block to delete; 02-CONTEXT.md D-01 D-03 D-04 D-06 D-07 D-08 D-10 D-11; 02-RESEARCH.md Patterns 1-3 + Standard Stack; 02-PATTERNS.md Strict-mode header + Arg-parsing + DRY_RUN early-return + Typed yes guard + Stow deploy + Quarantine immutability sections</read_first>
  <action>Build the thinnest production-quality end-to-end path per D-01 D-03 D-04 D-06 D-07 D-08 D-10 D-11, reusing Phase 1 spine verbatim and never adding code fences inside this action.

On setup.sh, add global UNINSTALL=false alongside DRY_RUN/YESS/ FAMILY/SCRIPT_DIR/OS_RELEASE_FILE at top (UPPER_SNAKE, default false). Extend usage heredoc to list OPTIONS `--uninstall, --remove` as cleanly unstows selected packages via stow -D plus privileged keyd cleanup and Mason when nvim deselected, typed yes required with --yes bypass for CI, and note --dry-run preview before every mutation. In parse_args keep the for-arg --help pre-scan before the while loop so --help wins anywhere, then replace the deferred 106-111 branch that echoed Note deferred with UNINSTALL=true; shift and keep --remove as alias, preserve --yes and --dry-run boolean toggles, keep unknown --* abort with usage nonzero before any prompt or write, keep ${1-} and arity guards under set -u.

In main keep parse_args "$@" as the first call so --help never triggers unbound variable, keep both repo-root guards [[ -f "$SCRIPT_DIR/setup.sh" ]] and [[ -f ./setup.sh ]] before any prompt or write and reuse the same guards for the uninstall branch, keep DRY_RUN banner === DRY RUN MODE printed once at startup. After FAMILY detection and mode/shell resolution and before checklist/preview, branch: if UNINSTALL is true then collect SELECTED_PACKAGES via the same prompt_checklist path used for install (mode → shell → 7-package checklist before any write, server pre-unchecks GUI but re-check works, Termux disabled rows stay unselectable) so user selects which stow packages to remove; reuse SELECTED_PACKAGES filtering via strip_termux_disabled. Do not special-case a separate mode file; per D-11 never write or read ~/.config/dotfiles/mode or repo-root .dotfiles-mode.

Implement run_uninstall function called from main when UNINSTALL is true. Inside run_uninstall implement DRY_RUN early-return before every filesystem mutation per Phase 1 pattern: first loop over SELECTED_PACKAGES skipping keyd, printing [DRY RUN] Would run: stow --dir="$SCRIPT_DIR" --target="$HOME" --delete $pkg and running stow --dir="$SCRIPT_DIR" --target="$HOME" --no --verbose --delete "$pkg" piped through sed and || true so diff exit 1 never kills set -e; then if keyd is selected preview sudo stow --dir="$SCRIPT_DIR" --target=/ --no --verbose --delete keyd similarly; if DRY_RUN is true return 0 immediately without touching filesystem. For live run implement typed yes gate per D-03: if YES is not true then try gum confirm primary with header Run uninstall (stow -D) for: ${SELECTED_PACKAGES[*]} ?; on gum confirm failure return 1 with Uninstall cancelled; on gum missing fallback to printf Type 'yes' to confirm: to stderr then read -r reply and require exact case-sensitive yes else return 1 with Uninstall cancelled (expected yes). Do not accept y/n, do not bypass with --yes for chsh (chsh gate lives in 02-02) but do bypass uninstall gate when --yes is true per D-03.

After confirmation perform idempotent unstow per D-04: for each pkg in SELECTED_PACKAGES skipping keyd, warn and continue if [[ ! -d "$SCRIPT_DIR/$pkg" ]]; attempt stow --dir="$SCRIPT_DIR" --target="$HOME" -D "$pkg" and on failure warn Warning stow -D returned non-zero (already unstowed or not owned — continuing) but continue; for privileged unstow if keyd selected run sudo stow --dir="$SCRIPT_DIR" --target=/ -D keyd or warn similarly, then attempt sudo keyd reload or sudo systemctl reload keyd or true so failure never kills installer. Leave .stow-conflicts/<timestamp>/ intact — never delete — and keep gitignored entry.

Implement privileged keyd install gate per D-06 D-07 D-08 D-09 as a separate function install_keyd_privileged called from run_stow path when SELECTED_PACKAGES contains keyd and FAMILY is not termux. At start if FAMILY is termux warn keyd not available on Termux and return 0 with no sudo writes. Otherwise print === Privileged keyd preview (no writes) === then [PREVIEW] stow --dir="$SCRIPT_DIR" --target=/ --no --verbose keyd and run stow --dir="$SCRIPT_DIR" --target=/ --no --verbose keyd piped to sed || true. Detect conflict: if /etc/keyd/default.conf exists as regular file and not a symlink then set has_regular_conflict true, print Conflict: /etc/keyd/default.conf exists as a regular file — showing diff: and run diff -u /etc/keyd/default.conf $SCRIPT_DIR/keyd/etc/keyd/default.conf piped to sed || true. If DRY_RUN is true print [DRY RUN] Would run: sudo stow --dir="$SCRIPT_DIR" --target=/ keyd plus conflict note and [DRY RUN] Would run: sudo keyd reload || sudo systemctl reload keyd || true then return 0 before any mutation. For live run run confirmation ladder per D-08: if YES is true then confirmed true else if gum exists then gum confirm Write privileged keyd config to /etc/keyd/default.conf ? else read -r reply with prompt Type 'yes' to confirm privileged keyd install: and require exact yes. On confirmed false return 1. Then decide adopt: only when has_regular_conflict is true, ask second adopt confirmation via gum confirm Conflict file exists. Adopt host file into repo? or read -r adopt_reply with prompt Type 'adopt' to move host file into repo (stow --adopt), or Enter for plain stow: and require exact adopt string, never force --adopt. If use_adopt true run sudo stow --dir="$SCRIPT_DIR" --target=/ --adopt keyd else sudo stow --dir="$SCRIPT_DIR" --target=/ keyd. After successful stow run sudo keyd reload or sudo systemctl reload keyd or true. Throughout never move host file into repo without explicit adopt.

Modify run_stow to delegate keyd handling to install_keyd_privileged instead of the Phase 1 skip notice; remove the four Phase 1 // Phase 2 — no /etc writes notices at setup.sh 385/404/460/478.

Modify zsh/.zprofile per D-10: delete the entire conditional exec start-hyprland block (lines 1-4) and replace with comment-only file stating no Hyprland autostart removed Phase 2 D-10 and that Zsh login profile is intentionally empty and Hyprland is launched manually via display manager or Hyprland command. Do not add mode file reads, command -v Hyprland guards, or || true wrappers. File must be non-executable guard: no exec anywhere.

Ensure strict-mode invariants hold: every new function quotes expansions, uses ${1-} / ${var:-} guards, splits local var assignment from fallible command substitution so set -e is not masked, and pipes preview output through || true.</action>
  <verify>
    <automated>bash -n setup.sh</automated>
    <automated>bash setup.sh --help 2>&1 | grep -q "Usage:" && bash setup.sh --help --uninstall; test $? -eq 0 && echo "help wins over uninstall"</automated>
    <automated>bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes 2>&1 | grep -q "\[DRY RUN\] Would run: stow" && echo "dry-run preview ok"</automated>
    <automated>grep -q "UNINSTALL" setup.sh && grep -q "run_uninstall" setup.sh && grep -q "install_keyd_privileged" setup.sh && echo "symbols present"</automated>
    <automated>test ! -s zsh/.zprofile || ! grep -q "exec start-hyprland" zsh/.zprofile && echo "zprofile exec removed"</automated>
    <automated>bash setup.sh --dry-run --mode local --shell zsh --yes 2>&1 | grep -q "Privileged keyd preview" || echo "preview path exists (keyd selected) or skipped"</automated>
  </verify>
  <done>Valid credentials never applied; dry-run touches zero files, second dry-run idempotent, exec block absent via readlink-agnostic grep, privileged preview prints before any /etc write</done>
  <acceptance_criteria>File zsh/.zprofile does not contain string exec start-hyprland; File setup.sh contains string UNINSTALL= and run_uninstall and install_keyd_privileged; Command bash -n setup.sh exits 0; Output of bash setup.sh --help contains Usage:; Output of bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes contains [DRY RUN] Would run: stow</acceptance_criteria>
</task>

<task type="auto">
  <name>Mason artefacts + legacy mode file + quarantine immutability</name>
  <files>setup.sh</files>
  <read_first>setup.sh (post-01 tracer: run_uninstall already exists, plus SELECTED_PACKAGES/ALL_PACKAGES/TERMUX_DISABLED_PACKAGES, FAMILY detection, DRY_RUN/YES globals); 02-CONTEXT.md D-02 D-04; 02-RESEARCH.md Pattern 1 uninstall_idempotent; 02-PATTERNS.md Quarantine MANIFEST + never-delete contract + Mason artefact cleanup shape; .gitignore line 14 .stow-conflicts/</read_first>
  <action>Extend run_uninstall to cover artefact and legacy state per D-02 and D-04 without touching repo package directories and without deleting quarantine.

Add Mason cleanup that only triggers when nvim is not in SELECTED_PACKAGES after filtering: test if printf '%s\n' "${SELECTED_PACKAGES[@]}" | grep -qx nvim fails, then if [[ -d "$HOME/.local/share/nvim/mason" ]] print Removing Mason artefacts: ~/.local/share/nvim/mason (nvim deselected) to stderr; if DRY_RUN is true print [DRY RUN] Would run: rm -rf ~/.local/share/nvim/mason and return without deleting, otherwise run rm -rf "$HOME/.local/share/nvim/mason" and attempt rmdir "$HOME/.local/share/nvim" 2>/dev/null || true to prune empty parent but never fail. Do not touch site/state/cache unless mason was removed, and never follow MasonInstallAll re-deployment path here (Mason auto-install lives in Phase 4).

Add legacy mode-file cleanup with no error if absent per D-04 and D-11: after Mason, run rm -f "$HOME/.config/dotfiles/mode" 2>/dev/null || true and rm -f "$SCRIPT_DIR/.dotfiles-mode" 2>/dev/null || true (cover both legacy locations the intermediate discussion considered). Do not create, write, or read any mode file elsewhere in setup.sh; main must not contain mkdir -p ~/.config/dotfiles or echo "$MODE" > … branches.

Enforce quarantine immutability: ensure run_uninstall never runs rm -rf on .stow-conflicts and never moves or deletes manifest files; add comment above uninstall function stating .stow-conflicts/<timestamp>/ is never auto-deleted on uninstall — remains as safety backup per D-04. Keep existing quarantine_scan behavior untouched for install path.

Ensure idempotent second uninstall: keep the test -L aware warn path already in tracer; add a second-run smoke check in verify that running uninstall preview twice yields same preview output without error.</action>
  <verify>
    <automated>grep -q "Removing Mason artefacts" setup.sh && grep -q "rm -rf.*mason" setup.sh && echo "mason cleanup present"</automated>
    <automated>grep -q "rm -f.*\.config/dotfiles/mode" setup.sh && grep -q "\.dotfiles-mode" setup.sh && echo "legacy mode cleanup present"</automated>
    <automated>grep -q "\.stow-conflicts" setup.sh && ! grep -q "rm -rf.*\.stow-conflicts" setup.sh && echo "quarantine never deleted"</automated>
    <automated>bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes 2>&1 | grep -q "Would run: rm -rf" || echo "dry-run mason preview when nvim deselected"</automated>
  </verify>
  <done>Mason removal only when nvim deselected, legacy file removal silent on absent, quarantine never auto-deleted, second dry-run uninstall still prints [DRY RUN] without error</done>
  <acceptance_criteria>File setup.sh contains string Removing Mason artefacts; File setup.sh contains string rm -f and fragment .config/dotfiles/mode; Command bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes 2>&1 contains Would run: stow or Mason note without touching filesystem</acceptance_criteria>
</task>

<task type="auto">
  <name>System packages removal offer — all toolchain after same yes gate, Termux-aware, dry-run preview</name>
  <files>setup.sh</files>
  <read_first>setup.sh (get_deps per-family tables common/gui, filter_deps_by_selection, install_deps per-family sudo pacman vs apt vs pkg no-sudo, SELECTED_DEPS/ALL_TOOLCHAIN, FAMILY, DRY_RUN, YES, verify_deps); 02-CONTEXT.md D-05 verbatim twice offer to remove all installed deps; 02-RESEARCH.md System Architecture Diagram uninstall section steps 6-7; 02-PATTERNS.md Deps table + Termux guard for system-deps removal offer</read_first>
  <action>Implement offer_system_package_removal per D-05 inside run_uninstall, reusing the same family tables and SELECTED_DEPS filtering that Phase 1 install used, gated by the same yes ladder and DRY_RUN preview, never auto-removing.

Define offer_system_package_removal function. At top return 0 early if FAMILY is empty and detect_family failed (no manager). Determine candidate deps: if SELECTED_DEPS is non-empty use its contents as the candidate list else fall back to ALL_TOOLCHAIN (cover non-interactive uninstall where no toolchain checklist ran). Filter candidates through filter_deps_by_selection when SELECTED_DEPS is active so only user-selected toolchain is considered. Build display list by mapping install names to probe names already known (stow→stow, neovim→nvim, ripgrep→rg, nodejs→node, others 1:1) but removal uses install names (pacman/apt/pkg names) exactly as get_deps emitted: stow neovim starship git zoxide uv ripgrep nodejs npm make gcc fzf zsh. Do not invent new names.

Preview phase before any mutation: if DRY_RUN is true then for each candidate print [DRY RUN] Would run: sudo pacman -Rns <pkg> on arch, sudo apt remove -y <pkg> on debian, pkg uninstall <pkg> on termux (no sudo) and return 0 without prompting or removing. For live run, prompt once per D-05 with a single yes gate covering the whole list: print "" then === System packages installed via setup.sh === then list candidates, then ask confirmation. Reuse the same ladder as uninstall: if YES is true then confirmed true else if gum exists then gum confirm "Remove system packages listed above via <manager>?" else printf Offer to remove system packages via <manager> for: <list>. Type 'yes' to confirm: and require exact yes. On confirmed false print Skipping system package removal. and return 0 (not an error). On confirmed true iterate candidates and run per-package removal with per-family commands: arch sudo pacman -Rns --noconfirm <pkg>, debian sudo apt remove -y <pkg>, termux pkg uninstall -y <pkg> (no sudo). Wrap each in if ! <cmd>; then warn Warning: failed to remove '<pkg>' via … Try manually: … and continue — never abort uninstall on single package failure and never kills session. For arch/debian ensure sudo prefix; for termux ensure no sudo.

Wire offer_system_package_removal as the last step of run_uninstall after privileged unstow, Mason cleanup, and legacy mode cleanup, before the final echo Uninstall complete. Ensure DRY_RUN early-return at top of run_uninstall already covered the preview loop above so live removal never runs in dry-run. Keep SELECTED_DEPS filtering verbatim from Phase 1 (ALL_TOOLCHAIN order preserved). Add comment noting D-05 verbatim requirement it should offer to remove all packages not only stow config related ones.</action>
  <verify>
    <automated>grep -q "offer_system_package_removal" setup.sh && echo "offer function present"</automated>
    <automated>bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes 2>&1 | grep -q "Would run:.*pacman -Rns\|Would run:.*apt remove\|Would run:.*pkg uninstall" || echo "dry-run package preview present when toolchain selected"</automated>
    <automated>grep -q "pacman -Rns" setup.sh && grep -q "apt remove -y" setup.sh && grep -q "pkg uninstall" setup.sh && echo "all three managers covered"</automated>
    <automated>! grep -q "rm -rf.*\.stow-conflicts" setup.sh && echo "no quarantine deletion"</automated>
  </verify>
  <done>Offer lists all SELECTED_DEPS toolchain names, previews with [DRY RUN] Would run: sudo pacman -Rns / apt remove -y / pkg uninstall, requires same yes gate, never auto-removes, Termux uses no sudo, single package failure does not abort uninstall</done>
  <acceptance_criteria>File setup.sh contains string offer_system_package_removal; File setup.sh contains string pacman -Rns and apt remove -y and pkg uninstall; Command bash -n setup.sh exits 0; Output of bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes contains Would run: stow and offer hint or package list preview</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → setup.sh parse_args | Untrusted user input, TTY vs no-TTY, --help must win before any write |
| setup.sh → HOME symlinks (stow --dir/--target/--delete) | Deletes only links stow owns; must not follow or delete quarantine |
| setup.sh → /etc/keyd via sudo stow -t / | Privileged filesystem write; requires preview + diff + explicit confirmation before --adopt |
| setup.sh → OS package manager (pacman/apt/pkg) | Privileged package removal; must be explicit yes gated and dry-run previewable |
| setup.sh → ~/.local/share/nvim/mason | User data deletion gated on nvim deselection only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | parse_args --help/unknown handling | medium | mitigate | Pre-scan for --help before while loop, `${1-}` guards under `set -u`, unknown abort with usage nonzero before any prompt or write |
| T-02-02 | Tampering | stow -D deletes wrong target | high | mitigate | Use `stow --dir="$SCRIPT_DIR" --target="$HOME" -D` only, never rm -rf on repo dirs; check `[[ -d "$SCRIPT_DIR/$pkg" ]]` before -D; warn on non-zero instead of abort; leave .stow-conflicts intact |
| T-02-03 | Tampering | sudo stow --adopt -t / keyd | high | mitigate | Preview `stow --no --verbose -t / keyd` + `diff -u` when host file is regular file, gum confirm → Type yes fallback, adopt only on second explicit adopt confirmation (exact `adopt` string), otherwise plain sudo stow |
| T-02-04 | Elevation of Privilege | sudo keyd reload / systemctl reload | medium | mitigate | Reload with `|| true` so failure never kills session; do not broaden sudoers beyond `reload` (least-privilege docs in 02-02) |
| T-02-05 | Denial of Service | zsh/.zprofile exec on tty1 | high | mitigate | Delete exec block per D-10, no mode file read/write per D-11, login is no-op; reversible via git history |
| T-02-06 | Information Disclosure | .stow-conflicts quarantine deletion | medium | mitigate | Never auto-delete .stow-conflicts/<timestamp>/MANIFEST on uninstall per D-04; rm -rf only targets mason artefacts when nvim deselected |
| T-02-07 | Repudiation | uninstall without typed confirmation | medium | mitigate | Typed `yes` exact case-sensitive, --yes bypass only for CI, --dry-run prints [DRY RUN] Would run: stow ... and sudo stow --no --verbose without filesystem touch |
| T-02-SC | Tampering | Host package manager installs | high | mitigate | Reuse per-family install names to binary probe map, dry-run preview `[DRY RUN] Would run: pacman -Rns/apt remove/pkg uninstall` before any mutation, per-package graceful failure with manual retry hint |
</threat_model>

<verification>
- `bash -n setup.sh` exits 0 (strict header still holds)
- `bash setup.sh --help` and `bash setup.sh --dry-run --help` exit 0 with Usage: before any write, and `bash setup.sh --uninstall --help` still exits 0 with help wins
- `bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes` prints `[DRY RUN] Would run: stow --dir="..." --target="$HOME" --delete` per SELECTED_PACKAGES, prints `sudo stow --dir="..." --target=/ --no --verbose --delete keyd` preview, prints `diff -u` if host keyd regular file exists, touches zero files on disk (idempotent second dry-run identical)
- Privileged live path without --dry-run requires gum confirm or `Type 'yes'` exact before any sudo stow; with --yes it skips prompt but still never auto-adopts (adopt requires second explicit `adopt` typed or gum confirm)
- `zsh/.zprofile` contains no `exec start-hyprland`, no `DISPLAY` check, no `tty` check; login on tty1 is safe; `grep -R "dotfiles/mode" setup.sh` finds no write/read except the legacy rm -f cleanup
- Re-install after uninstall (`bash setup.sh --mode server --shell zsh --dry-run`) is idempotent — second run prints no install needed or same preview without error
</verification>

<success_criteria>
- Uninstall is reversible via `stow --restow` after `stow -D` because repo package dirs were never deleted
- Dry-run previews every pending stow -D and privileged keyd write before any filesystem touch
- Second uninstall and re-install are safe no-ops (idempotent)
- Privileged keyd never force-adopts without explicit adopt confirmation; diff shown when conflict regular file exists
- Server tty1 login no longer execs Hyprland; no persisted mode file exists
</success_criteria>

<output>
Create `.planning/phases/02-safe-reversible-server-safe-deployment/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Exported As |
|--------|------|------|-------------|
| UNINSTALL | global bool | setup.sh | `UNINSTALL=false` toggled by --uninstall/--remove |
| parse_args --uninstall/--remove branch | CLI flag | setup.sh | `--uninstall` / `--remove` alias sets UNINSTALL |
| run_uninstall | function | setup.sh | `run_uninstall()` handles dry-run preview → yes gate → idempotent stow -D loops → privileged -D → Mason/legacy/system offer |
| install_keyd_privileged | function | setup.sh | `install_keyd_privileged()` handles preview + diff -u + gum/Type yes + conditional --adopt + reload \|\| true |
| offer_system_package_removal | function | setup.sh | `offer_system_package_removal()` offers pacman -Rns/apt remove/pkg uninstall for SELECTED_DEPS after same yes gate |
| zsh/.zprofile cleanup | file | zsh/.zprofile | Empty/comment-only profile, no Hyprland exec |
