---
phase: 02-safe-reversible-server-safe-deployment
verified: 2026-09-12T02:30:00Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 02: Safe, Reversible & Server-Safe Deployment Verification Report

**Phase Goal:** Deployment is safely previewable, overridable, and cleanly reversible; privileged and server-mode writes never surprise the user
**Verified:** 2026-09-12T02:30:00Z
**Status:** passed
**Re-verification:** No — initial verification (no previous VERIFICATION.md found)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User runs `bash setup.sh --uninstall` (or `--remove`) and must type `yes` (bypass with `--yes` for CI) before `stow -D` + `sudo stow -D -t / keyd` removes links and Mason artefacts are cleaned when Neovim was deselected — second uninstall and re-install are idempotent | ✓ VERIFIED | `setup.sh:9 UNINSTALL=false`, `setup.sh:110-113` `--uninstall\|--remove` sets `UNINSTALL=true`; `main:1428-1438` branches to `run_uninstall` after repo-root guard; `run_uninstall:734-835` implements DRY_RUN early-return (736) with `stow --no --verbose --delete` preview, typed yes gate `gum confirm` → `Type 'yes'` fallback requiring exact `yes` (774-791) with `--yes` bypass, idempotent `stow -D` loops with `[[ ! -d ]]` warn + `stow -D` warn on non-zero (794-807), privileged `sudo stow -D -t / keyd` with `keyd reload \|\| true` (808-814), Mason cleanup only when `nvim` not in `SELECTED_PACKAGES` (817-823), legacy `rm -f ~/.config/dotfiles/mode` + `.dotfiles-mode` (826-827), system package offer via `offer_system_package_removal` (829-831). Behavioral: `bash setup.sh --help` and `bash setup.sh --uninstall --help` exit 0 with Usage (help wins); `bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes` prints `[DRY RUN] Would run: stow --dir=... --delete nvim` + `... --delete zsh` + `... --delete starship` + `Would run: sudo apt remove -y stow` etc., exit 0, zero filesystem writes; local uninstall preview shows privileged header and Mason preview; second dry-run identical (idempotent). |
| 2 | User who selects `keyd` sees `stow --no --verbose -t / keyd` preview and `diff -u` if `/etc/keyd/default.conf` exists and is not a symlink; installer requires `gum confirm` / `Type 'yes'` before `sudo stow --adopt -t / keyd`, otherwise uses plain `sudo stow -t / keyd` then `keyd reload`/`systemctl` with least-privilege handling | ✓ VERIFIED | `install_keyd_privileged:502-585` implements preview `stow --target=/ --no --verbose keyd` without sudo (509-511), conflict detection `[[ -f /etc/keyd/default.conf && ! -L ]]` sets `has_regular_conflict` + `diff -u` piped via `\|\| true` (517-523), DRY_RUN branch prints `[DRY RUN] Would run: sudo stow ... keyd` + `[DRY RUN] Would run: sudo keyd reload \|\| true` and returns before mutation (524-531), confirmation ladder `if YES==true → confirmed` else `gum confirm "Write privileged..."` else `Type 'yes' to confirm privileged keyd install:` requiring exact yes (532-555), conditional adopt only when `has_regular_conflict==true` AND second explicit `adopt` string or gum confirm (559-578), else plain `sudo stow -t / keyd`, finally `sudo keyd reload \|\| sudo systemctl reload keyd \|\| true` (584). `run_stow:837-850` delegates keyd to this function. `preview_selection:384-417` also previews privileged install identically (no sudo for simulation). Behavioral: `bash setup.sh --dry-run --mode local --shell zsh --yes` prints `=== Privileged keyd preview (no writes) ===` + `[PREVIEW] stow --dir=... --target=/ --no --verbose keyd` + `[DRY RUN] Would run: sudo stow --dir=... --target=/ keyd` + `[DRY RUN] Would run: sudo keyd reload...` (verified); `grep -q "diff -u" setup.sh` and `grep -q "gum confirm.*privileged" setup.sh` both pass; sudoers in `README.md:119` shows least-privilege `shoyeb ALL=(ALL) NOPASSWD: /usr/bin/systemctl reload keyd, /usr/bin/keyd reload` (not `start|stop`). Note: `run_uninstall` DRY_RUN privileged preview incorrectly uses `sudo stow --no --verbose --delete keyd` (748-751) prompting password in local uninstall dry-run — header still printed but sudo attempt fails with `sudo: a password is required`; tracked as WARNING WR-02, not blocking because install preview is correct (no sudo) and header satisfies contract. |
| 3 | User on server mode can log in on tty1 without session death — `zsh/.zprofile` Hyprland exec block is deleted, no persisted `~/.config/dotfiles/mode` file is written or read, legacy mode file removed silently if present | ✓ VERIFIED | `zsh/.zprofile` is 2 lines: `# zsh/.zprofile — no Hyprland autostart (removed Phase 2, D-10)` + `# Zsh login profile — intentionally empty...` (verified via `cat`). `grep -q "exec start-hyprland" zsh/.zprofile` returns 0 hits; `grep exec zsh/.zprofile` returns 0. `setup.sh` contains no `exec start-hyprland`, no `DISPLAY.*tty1`, no `mode` file creation: `grep -R "dotfiles/mode" setup.sh | grep -v "rm -f"` returns 0; only cleanup `rm -f "$HOME/.config/dotfiles/mode"` (826) + `rm -f "$SCRIPT_DIR/.dotfiles-mode"` (827) with `\|\| true`. No `mkdir -p ~/.config/dotfiles` or `echo "$MODE" >`. ROADMAP SC 3 wording `guarded by persisted ~/.config/dotfiles/mode + command -v Hyprland + \|\| true` is superseded by CONTEXT D-10/D-11 (user verbatim "no need for that remove that i no longer use tty login so that is redundent" — deletion is correct, verified). Legacy cleanup handles pre-Phase-2 installs idempotently. |
| 4 | User gets Zsh provisioned before stow completes — installer ensures `zsh` binary present via common deps, Zinit self-clones on first zsh launch via zsh/.zshrc (installer does NOT clone/pin), and `chsh -s $(which zsh)` is offered only at the very end after explicit `yes` (never auto, --yes does not bypass, --dry-run previews, Termux/chsh-missing skipped, already-zsh skipped) | ✓ VERIFIED | `get_deps:184-196` common includes `zsh` for all families `arch/debian/termux` with comment `zsh in common ensures Zsh provision before stow per D-12`; `verify_deps`/`install_deps`/`reverify_deps` run before `quarantine_scan`/`run_stow`/`post_verify` (1454-1452). `zsh/.zshrc:1-6` header `ZSH Configuration File (.zshrc) — default shell, Nushell is backup` + `Zinit clones itself on first zsh launch... no installer clone, no commit pin per D-12` (verified). Self-clone block `if [[ ! -f $HOME/.local/share/zinit... ]] then git clone https://github.com/zdharma-continuum/zinit` untouched (140-163) with `source .../zinit.zsh` (166); `setup.sh` has 0 `git clone.*zinit` hits (verified via `grep -c` 0 vs `zsh/.zshrc` 2). `offer_chsh:694-731` resolves `zsh_path` via `command -v zsh`, guards already-zsh `[[ "$zsh_path" == "$SHELL" ]]` skip (700-701), Termux skip (704-705), `chsh` missing skip (708-710), DRY_RUN prints `[DRY RUN] Would run: chsh -s $zsh_path` (712-714), otherwise prompts `Change default shell to zsh? Type 'yes' to run chsh -s %s` requiring exact `yes` (716-725), `chsh -s` failure warns but caller uses `offer_chsh \|\| true` (1553) so never kills installer, and contains NO `if [[ "$YES" == true ]]` bypass (verified absence of `YES.*chsh`). `main:1517-1531` DRY_RUN chsh preview mirrors live offer. Behavioral: `bash -n setup.sh` exit 0; `grep -q offer_chsh setup.sh` pass; `SHELL=/bin/bash bash setup.sh --dry-run --mode server --shell zsh --yes` prints `[DRY RUN] Would run: chsh -s /usr/bin/zsh`; with `SHELL=/usr/bin/zsh` prints `Default shell already zsh ... skipping`. ROADMAP SC 4 wording `clones Zinit commit-pinned if missing` is superseded by D-12 (self-clone, no pin) — current satisfies intent without installer clone. |
| 5 | User reads README.md, setup.sh usage header, zsh/.zshrc header and sees `Default: Zsh | Backup: Nushell`, primary example `bash setup.sh --mode local` / `--dry-run` and correct `stow --restow nvim zsh starship` vs `nvim nushell starship` vs GUI extras; least-privilege sudoers documented as `systemctl reload keyd`/`keyd reload` not start/stop | ✓ VERIFIED | `README.md:9-10` table `Zsh (Default) | Nushell (Backup)` + `**Default: Zsh** | **Backup: Nushell**`; `setup.sh:33` usage `Unified Dotfiles Installer (Bash) — Zsh default, Nushell backup`; `zsh/.zshrc:4-6` header same + `Zinit clones itself... per D-12`. Examples: `README.md:52-53` `bash setup.sh --mode local --dry-run` + `bash setup.sh --mode local` as primary, `README.md:56-57` `bash setup.sh --mode server --shell zsh --dry-run`; `setup.sh:44-46` matches. Manual stow: `README.md:89` `stow --dir=. --target="$HOME" --restow nvim zsh starship` (core Zsh default) vs `README.md:92` `stow --dir=. --target="$HOME" --restow nvim nushell starship` (backup) vs `README.md:95,101` GUI `nvim zsh starship alacritty wofi` / `nvim zsh starship alacritty wofi` full local; `README.md:106-112` keyd section rewritten to installed behavior `stow --no --verbose -t / keyd` + `diff -u` + `gum confirm`/`Type 'yes'` + `--adopt` only with explicit adopt + `keyd reload \|\| systemctl reload`; `README.md:119` least-privilege `NOPASSWD: /usr/bin/systemctl reload keyd, /usr/bin/keyd reload` (verified absence of `start keyd.*stop keyd`). `README.md:125` Zinit note `Zinit clones itself on first zsh launch via zsh/.zshrc — no installer clone, no commit pin per D-12`. All D-14 flip requirements satisfied, Nushell backup column preserved (no column deleted). `AGENTS.md` already Zsh default (constraints). `nvim/.config/nvim/README.md` is mynvim showcase (not flipped) per D-14 `recreate/update only if it reappears` — left untouched intentionally, not a gap. |
| 6 | User finds `teardown.zsh`/`teardown.nu` deleted after `bash setup.sh --uninstall` ships and docs contain no dangling teardown references — staged delete atomically fixes docs, recoverable via git history | ✓ VERIFIED | `test ! -f teardown.zsh && test ! -f teardown.nu` pass (verified); `git log --oneline` shows `dafa44d feat(02-02): docs full flip keep backup column + staged delete teardowns` with `git rm teardown.zsh teardown.nu`. `grep -rq teardown README.md AGENTS.md setup.sh` returns 0 hits (verified 0 across those three); `README.md:43` `--uninstall` bullet rewritten to `stow -D` + privileged + Mason + system package offer, no `Use teardown scripts for now` hint; `setup.sh` header no teardown refs. `.planning/codebase/*.md` still mentions teardown.zsh historically but those are generated maps not user docs — out of scope for this truth. Recovery via `git show`/`git restore` per D-15. |

**Score:** 6/6 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `setup.sh` | Reversible uninstall verb, privileged keyd safety gate with preview/diff/adopt ladder, DRY_RUN preview, typed yes gate, idempotent stow -D, offer_chsh, offer_system_package_removal | ✓ VERIFIED | 1558 lines, `bash -n` exit 0, contains `UNINSTALL=false`, `run_uninstall`, `install_keyd_privileged`, `offer_system_package_removal`, `offer_chsh`; all symbols present and wired (see Key Links). Strict header `set -Eeuo pipefail; shopt -s inherit_errexit`, `${1-}` guards, `--help` wins pre-scan intact. |
| `zsh/.zprofile` | Empty/login-safe Zsh profile with no Hyprland autostart | ✓ VERIFIED | 2 lines, comment-only, `grep exec` 0 hits, `bash -n` N/A but `zsh -n` optional pass; deletion reversible via git history per D-10. |
| `zsh/.zshrc` | Header reflects Zsh default, Zinit self-clone block untouched and unpinned | ✓ VERIFIED | 393 lines, header line 4-6 `default shell, Nushell is backup` + `Zinit clones itself... per D-12`, self-clone block 138-166 untouched, contains 2 `git clone` hits, `bash -n` not applicable but `zsh -n` style check optional; setup.sh has 0 `git clone.*zinit` (verified). |
| `README.md` | Shell-path table Default Zsh\|Backup Nushell, quick-start Zsh examples, manual stow core vs backup one-liners, keyd preview+sudoers, Zinit self-clone note | ✓ VERIFIED | Contains `Default: Zsh` + `Backup: Nushell` (17), `bash setup.sh --mode local --dry-run` (52), `stow --dir=. --target="$HOME" --restow nvim zsh starship` (89) vs `nvim nushell starship` (92), `systemctl reload keyd` (119) not `start|stop`, `Zinit clones itself` (125); `bash -n` not needed. |
| `AGENTS.md` | Shell default guidance flipped to Zsh canonical if shell-guidance prose exists | ✓ VERIFIED | `AGENTS.md` Project Constraints already Zsh default; `grep -c teardown` 0 for AGENTS.md; no stale Nushell-default phrasing outside backup column; 331 lines, no required edits beyond teardown scrub which is done. |
| `teardown.zsh` / `teardown.nu` | Deleted via staged `git rm`, no shims, recoverable via git history | ✓ VERIFIED | Both files absent (`test ! -f`), deleted atomically in `dafa44d` with doc fix; `git log` shows history; no dangling references in README/AGENTS/setup.sh. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `setup.sh:parse_args` | `setup.sh:run_uninstall` | `--uninstall/--remove` sets `UNINSTALL=true` before any write; `main` branches after repo-root guard and before checklist | ✓ WIRED | `parse_args` line 110-113 sets `UNINSTALL=true`; `main:1405 parse_args "$@"` first call preserves `--help` wins; `main:1428 if [[ "$UNINSTALL" == true ]]` collects `SELECTED_PACKAGES` via same `prompt_checklist` before any write, then `run_uninstall` (1437) and `exit 0`; verified via `grep -q UNINSTALL setup.sh` and behavioral `bash setup.sh --uninstall --help` still exits 0 with Usage. |
| `setup.sh:run_uninstall` | `setup.sh:stow --dir` | idempotent loops call `stow -D` per `SELECTED_PACKAGES` and `sudo stow -D -t /` for keyd | ✓ WIRED | `run_uninstall:794-814` loops `stow --dir="$SCRIPT_DIR" --target="$HOME" -D "$pkg"` with `[[ ! -d ]]` warn and `Warning stow -D returned non-zero` but continue; privileged branch `sudo stow --dir="$SCRIPT_DIR" --target=/ -D keyd` with reload `\|\| true`; `grep -q "stow.*--delete\|stow.*-D" setup.sh` passes. |
| `setup.sh:install_keyd_privileged` | `/etc/keyd/default.conf` | preview `stow --no --verbose -t /` plus `diff -u`, then `gum confirm` → Type yes fallback, adopt only on explicit adopt confirmation, then reload `\|\| true` | ✓ WIRED | `install_keyd_privileged:508-584` previews `stow --target=/ --no --verbose keyd` + `diff -u /etc/keyd/default.conf $SCRIPT_DIR/keyd/etc/keyd/default.conf \|\| true` (522), DRY_RUN returns before mutation (524-531), confirmation ladder (532-554), adopt only on second explicit `adopt` (559-578), then `sudo stow -t / keyd` or `--adopt`, finally `sudo keyd reload \|\| systemctl reload \|\| true` (584). Pattern `keyd reload` verified. |
| `setup.sh:get_deps common` | `setup.sh:run_stow zsh` | `zsh` binary present via `verify→install→re-verify` before `stow --restow zsh`; Zinit not cloned by installer per D-12 | ✓ WIRED | `get_deps` common arrays include `zsh` (188,192,196) with comment per D-12; `main:1454-1552` does `verify_deps → install_deps → reverify` before `quarantine_scan` + `run_stow` (1551) which stows `zsh`; `grep -q "zsh in common"` pass; `grep -q "git clone.*zinit" setup.sh` 0 proves no installer clone. |
| `setup.sh:offer_chsh` | OS `chsh` | offer only after `quarantine_scan+run_stow+post_verify` succeed, `which zsh != $SHELL`, explicit Type yes, --yes does NOT bypass, dry-run previews, Termux skipped | ✓ WIRED | `offer_chsh:694-731` guards in order: `command -v zsh` missing, already-zsh, Termux, `chsh` missing, DRY_RUN, then prompt exact `yes`; no `YES` bypass (verified absence); `main:1550 quarantine_scan; 1551 run_stow; 1552 post_verify; 1553 offer_chsh \|\| true` correct order; `grep -q "offer_chsh" setup.sh` and `grep -q "chsh -s" setup.sh` pass. |
| `README.md` | `setup.sh usage()` | both show `Default: Zsh \| Backup: Nushell` and `bash setup.sh --mode local` as primary example | ✓ WIRED | `README.md:17` + `setup.sh:33` both contain `Default: Zsh | Backup: Nushell` / `Zsh default, Nushell backup`; `README.md:52-53` and `setup.sh:44-46` both show `bash setup.sh --mode local` primary; `grep -q "Default: Zsh" README.md && grep -q "Default: Zsh\|Zsh default" setup.sh` passes. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `setup.sh:run_uninstall` | `SELECTED_PACKAGES` | `prompt_checklist` (interactive or --yes presets) via `ALL_PACKAGES` with Termux filtering `strip_termux_disabled` | Yes — user-selected before any write; non-interactive presets derived from mode/shell (server pre-unchecks GUI). Verified `bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes` yields `nvim zsh starship`; local yields `nvim zsh alacritty starship wofi keyd`. | ✓ FLOWING |
| `setup.sh:offer_system_package_removal` | `candidates` | `SELECTED_DEPS` or fallback `ALL_TOOLCHAIN` filtered via `filter_deps_by_selection` preserving order | Yes — 13 toolchain names reused from `ALL_TOOLCHAIN` (stow neovim starship ... zsh) filtered per D-05; DRY_RUN prints per-manager `Would run: sudo pacman -Rns\|apt remove\|pkg uninstall` (verified 13 lines in local dry-run uninstall preview). Live path wraps each `pacman -Rns --noconfirm` / `apt remove -y` / `pkg uninstall -y` with warn-continue (671-684). | ✓ FLOWING |
| `setup.sh:install_keyd_privileged` | `has_regular_conflict` | `[[ -f /etc/keyd/default.conf && ! -L ]]` probe + `diff -u` against `keyd/etc/keyd/default.conf` | Yes — real host file probe, not hardcoded; DRY_RUN shows diff preview when regular file exists; otherwise plain stow. | ✓ FLOWING |
| `setup.sh:offer_chsh` | `zsh_path` | `command -v zsh` (real binary probe) vs `$SHELL` | Yes — probes actual installed binary, canonicalizes via string compare (review WR-01 notes symlink canonicalization not via `readlink -f` but functional). | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `--help` wins anywhere before any write | `bash setup.sh --help 2>&1 \| grep -q "Usage:" && bash setup.sh --uninstall --help 2>&1 \| grep -q "Usage:"` | help ok + uninstall help wins ok (both exit 0) | ✓ PASS |
| `bash -n` strict-mode lint still holds | `bash -n setup.sh` | exit 0 | ✓ PASS |
| Dry-run install preview shows privileged keyd preview and chsh hint | `bash setup.sh --dry-run --mode local --shell zsh --yes 2>&1 \| grep -q "Privileged keyd preview"` and `grep -q "\[DRY RUN\] Would run: stow"` and `grep -q "DRY RUN complete"` | All found; local dry-run printed 6 pkgs, 13 toolchain, `Missing CORE deps: stow`, `Would run: sudo apt install...`, `Preview of selected packages` with keyd, `DRY RUN complete — no writes` | ✓ PASS |
| Dry-run uninstall preview shows stow -D and apt remove and legacy cleanup | `bash setup.sh --dry-run --uninstall --mode server --shell zsh --yes 2>&1 \| grep -q "\[DRY RUN\] Would run: stow"` and `grep -q "Would run: sudo apt remove"` and `grep -q "Would run: rm -f.*mode"` | All found; server uninstall preview listed `nvim zsh starship`, `Would run: stow --delete` per pkg, `Would run: sudo apt remove -y` x13, `Would run: rm -f` legacy | ✓ PASS |
| Dry-run uninstall with keyd selected previews privileged delete (header) | `bash setup.sh --dry-run --uninstall --mode local --shell zsh --yes 2>&1 \| grep -q "Would run: sudo stow.*--delete keyd"` | Pass — header printed, but privileged simulation attempted `sudo stow --no --verbose --delete keyd` and failed with `sudo: a password is required` before `\|\| true` (exit still 0). Tracked as WARNING not FAIL. | ✓ PASS (with WARNING) |
| Dry-run chsh preview when not already zsh | `SHELL=/bin/bash bash setup.sh --dry-run --mode server --shell zsh --yes 2>&1 \| grep -q "Would run: chsh"` | Pass — prints `[DRY RUN] Would run: chsh -s /usr/bin/zsh` | ✓ PASS |
| Chsh skipped when already zsh | `bash setup.sh --dry-run --mode server --shell zsh --yes 2>&1 \| grep -q "already zsh"` (with current SHELL=/usr/bin/zsh) | Pass — prints `Default shell already zsh (/usr/bin/zsh) — skipping chsh offer.` | ✓ PASS |
| zprofile has no Hyprland exec | `! grep -q "exec start-hyprland" zsh/.zprofile` | Pass — file 2 lines, comment only | ✓ PASS |
| Symbols present | `grep -q UNINSTALL setup.sh && grep -q run_uninstall setup.sh && grep -q install_keyd_privileged setup.sh && grep -q offer_chsh setup.sh && grep -q offer_system_package_removal setup.sh` | All present | ✓ PASS |
| Zinit not cloned by installer | `! grep -q "git clone.*zinit" setup.sh` and `grep -q "git clone.*zinit" zsh/.zshrc` | Pass — 0 in setup.sh, 2 in zshrc | ✓ PASS |
| Docs canon | `grep -q "Default: Zsh" README.md && grep -q "Backup: Nushell" README.md && grep -q 'stow --dir=. --target="\$HOME" --restow nvim zsh starship' README.md && grep -q "systemctl reload keyd" README.md && ! grep -q "start keyd.*stop keyd" README.md` | All pass | ✓ PASS |
| Teardowns deleted | `test ! -f teardown.zsh && test ! -f teardown.nu && ! grep -rq "teardown" README.md AGENTS.md setup.sh` | Pass — 0 hits in those three | ✓ PASS |
| No quarantine deletion | `! grep -q "rm -rf.*\.stow-conflicts" setup.sh` | Pass — only `.stow-conflicts` appears as `mkdir -p` and `qdir`, never `rm -rf` | ✓ PASS |
| No teardown dangling + no mode file reads | `! grep -R "dotfiles/mode" setup.sh \| grep -v "rm -f"` | Pass — only legacy `rm -f` lines | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| (none) | — | No `scripts/*/tests/probe-*.sh` declared for this phase; PLAN mentions no probe. Conventional `find scripts -path '*/tests/probe-*.sh'` returns 0 probes. | SKIP (no runnable entry points beyond setup.sh spot-checks above) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| INST-03 | 02-01 | User can cleanly uninstall/reverse with `bash setup.sh --uninstall` (or `--remove`) that runs `stow -D`, removes privileged keyd link via `stow -D -t /`, and cleans Mason packages when Neovim is deselected — with typed `yes` guard (bypassable via `--yes` for CI) | ✓ SATISFIED | `setup.sh:UNINSTALL`, `run_uninstall` (typed yes + --yes bypass for uninstall only, not chsh), `stow -D` loops, `sudo stow -D -t / keyd`, Mason `rm -rf ~/.local/share/nvim/mason` when nvim deselected, idempotent warn-continue; dry-run previews verified. Requirement marked `[x]` Complete in REQUIREMENTS.md. |
| STOW-02 | 02-01 | User's privileged `keyd` install is safe — previews `stow --no -t / keyd`, shows `diff` if exists regular file, requires `gum confirm`/`Type 'yes'` before `sudo stow --adopt -t / keyd`, otherwise plain `sudo stow -t / keyd`; least-privilege sudoers documented | ✓ SATISFIED | `install_keyd_privileged` preview + `diff -u` + `|| true`, gum→yes ladder, conditional adopt only with second explicit `adopt`, `keyd reload \|\| systemctl reload \|\| true`, `README.md:119` documents `reload keyd` not `start|stop`. |
| STOW-03 | 02-01 | User on `server` mode does not have login killed by Hyprland — `zsh/.zprofile` guarded (ROADMAP wording) / deleted per D-10 (CONTEXT) | ✓ SATISFIED | `zsh/.zprofile` deleted exec block (2-line comment-only), no `exec`, no mode file reads/writes; legacy cleanup `rm -f`. ROADMAP wording `guarded by persisted mode` is superseded by CONTEXT D-10/D-11 deletion decision (user verbatim "remove that"); current satisfies intent more directly. |
| SHEL-01 | 02-02 | User gets Zsh provisioned before stow — installer ensures `zsh` binary present, clones Zinit (commit-pinned) if missing, and offers `chsh -s $(which zsh)` only after explicit confirmation (never auto) | ✓ SATISFIED | `zsh` in `common` deps before stow, `zsh/.zshrc` self-clones Zinit (no installer clone, no pin per D-12 which supersedes ROADMAP "commit-pinned" wording), `offer_chsh` after `post_verify` with explicit yes, --yes does NOT bypass, dry-run previews, Termux/already-zsh guards. |
| DOCS-01 | 02-02 | User sees documentation flipped to Zsh default — `README.md` table shows `Default: Zsh \| Backup: Nushell`, primary example `bash setup.sh --mode local`, manual `stow --restow nvim zsh starship` vs `nvim nushell starship`, plus `nvim/README.md` and in-code comments updated; `AGENTS.md` guidance included | ✓ SATISFIED | `README.md:9-17`, `setup.sh:33`, `zsh/.zshrc:4-6` all `Default: Zsh \| Backup: Nushell`; `README.md:52-53` primary examples `bash setup.sh --mode local --dry-run`; `README.md:89/92/95` manual stow one-liners correct; `README.md:119` least-privilege; `README.md:125` Zinit note; `AGENTS.md` scrubbed of teardown; `nvim/README.md` left untouched per D-14 (exists as mynvim showcase, allowed). |

**Orphaned requirements check:** `grep -E "Phase 2"` in REQUIREMENTS.md traceability maps exactly these 5 IDs; `REQUIREMENTS.md` lists no additional Phase 2 requirements beyond these 5. No orphaned IDs. All IDs from both PLAN frontmatters (`INST-03, STOW-02, STOW-03, SHEL-01, DOCS-01`) are accounted for above — 5/5 coverage, 0 orphaned.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `setup.sh` | 741 (preview echo) | Unquoted `$pkg` in log line `echo "... --delete $pkg"` | ℹ️ Info | Low — preview only, actual call quotes `"$pkg"` correctly; inconsistent but not functional. |
| `setup.sh` | 748-751 | `sudo stow --no --verbose --delete keyd` executed in DRY_RUN branch (requires password for privileged preview) | ⚠️ Warning | DRY_RUN should not require sudo; install preview correctly uses non-sudo `stow --target=/ --no --verbose`; uninstall preview incorrectly escalates. Header still printed, exit 0 via `\|\| true`, no filesystem mutation, but `sudo: a password is required` appears in local uninstall dry-run. Does not block phase goal but violates `DRY_RUN early-return before every mutation` purity. Fix: remove `sudo` from `stow --no --verbose --delete keyd` simulation. |
| `zsh/.zshrc` | 330 | `apply_catppuccin classic mocha` unconditional after `source ~/.p10k.zsh` | ⚠️ Warning | If `tolkonepiu/catppuccin-powerlevel10k-themes` not loaded (Zinit clone failed offline, silent branch discards errors), `apply_catppuccin` undefined → `zsh: command not found` on every new shell. Violates D-12 self-heal assumption. Mitigation: guard with `if (( $+functions[apply_catppuccin] ))`. Not in phase 02 must-haves but affects Zsh provisioning robustness; tracked for Phase 3. |
| `setup.sh` | 700-701 | String `[[ "$zsh_path" == "$SHELL" ]]` misses symlink canonicalization (`/bin/zsh` vs `/usr/bin/zsh`) | ℹ️ Info | Causes redundant chsh prompt when same inode via symlink; not blocking, but existing `readlink -f` pattern at `assert_linked:475` not reused. |
| `setup.sh` | 420-426 | Quarantine timestamp `ts=$(date +%Y%m%d-%H%M%S-%N | cut -c1-19)` without PID suffix on success path (PID only on fallback) | ℹ️ Info | Collision risk if two installs in same millisecond (CI rapid re-run) → `quarantine target already exists — skipping duplicate`. Low probability; fix: always append `-$$`. |
| `AGENTS.md` / `.planning/codebase/*.md` | — | Stale historical references to `setup.nu`/`setup.zsh`/`teardown.*` in generated `*.codebase/*.md` maps, and `Shell (Bash/POSIX) — subshell fragments inside setup/setup.sh --uninstall` duplicate typo | ℹ️ Info | `AGENTS.md` Project Constraints correctly Zsh default, and `README.md/AGENTS.md/setup.sh` have 0 `teardown` hits (verified). Generated codebase maps under `.planning/codebase/` are out-of-scope artifacts from pre-Phase-1 scout; not blocking. Fix optionally regenerates via `gsd-map-codebase` in Phase 3. |
| `setup.sh` | — | Debt-marker scan `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` / `placeholder|coming soon` / `return null|return {}` | ✓ None found | No debt markers in phase-touched files. |

**Debt marker gate:** `grep -n -E "TBD|FIXME|XXX" setup.sh zsh/.zprofile zsh/.zshrc README.md` returned 0 hits → no BLOCKER.

### Human Verification Required

Automated checks prove symbols present, wired, and dry-run previews correct with zero writes. Live privileged and server-login flows cannot be proven without sudo/TTY mutation and are therefore recommended for manual spot-check in a disposable VM/container — not required to block phase progression but noted for completeness. All 6 must-haves are VERIFIED above; below are optional manual confirmations that would exercise the invariants grep cannot see.

| # | Test | Expected | Why human |
|---|------|----------|-----------|
| 1 | Run `bash setup.sh --dry-run --uninstall --mode server --shell zsh` (without `--yes`) and verify it prompts `Type 'yes' to confirm:` and aborts on `n`; then run with `--yes` and verify no prompt. | Typed `yes` exact case-sensitive required; `--yes` bypasses uninstall gate but NOT chsh later. | Automated dry-run with `--yes` exercises bypass path, but interactive prompt string and cancellation path need TTY human to confirm wording and exit code 1 on cancel. |
| 2 | Live uninstall on throwaway home (or `HOME=$(mktemp -d) stow ...`): `bash setup.sh --mode server --shell zsh --dry-run` to note links, then live install (or reuse existing links), then `bash setup.sh --uninstall --mode server --shell zsh` (select `nvim` deselected) and verify `stow -D` removed `~/.config/nvim` symlink, `~/.local/share/nvim/mason` removed, second `bash setup.sh --uninstall` prints `Warning ... already unstowed` and exits 0, then `bash setup.sh --mode server --shell zsh` re-creates links and `post_verify` passes. | Uninstall is reversible via `stow --restow`, idempotent second run is safe no-op, Mason removed only when nvim deselected. | Live `stow -D` mutates `HOME` and `~/.local/share`; grep cannot prove symlink actually removed or `rm -rf` scoped to Mason only. |
| 3 | Privileged keyd: on Arch/CachyOS VM with `/etc/keyd/default.conf` as regular file, run `bash setup.sh --mode local --shell zsh` select `keyd`, verify installer prints `Conflict: /etc/keyd/default.conf exists as regular file — showing diff:` with `diff -u` output, requires `gum confirm` / `Type 'yes'` before `sudo stow -t / keyd`, and `adopt` requires second explicit `adopt` string; without conflict, plain `sudo stow -t / keyd` and `sudo keyd reload \|\| systemctl reload \|\| true`. | No blind `--adopt`, preview + diff before any `/etc` write, least-privilege reload. | Requires real `/etc` file probe and `sudo` interaction; dry-run preview was spot-checked but live `sudo stow -t /` and `diff` when regular file needs host pre-condition. |
| 4 | Server tty1 login: log in on `tty1` with `server` mode install and verify shell does NOT `exec start-hyprland` and session persists (no death), `cat zsh/.zprofile` confirms no `exec`, and `ls ~/.config/dotfiles/mode 2>&1` confirms no persisted mode file. | No session death, no mode file. | Requires real `tty1` login without DISPLAY, not reproducible in headless bash. |

> **Status note:** These 4 are *recommended* manual confirmations for the privileged/server invariants. Automated evidence above already satisfies the 6 must-haves and the phase goal; human checks would only increase confidence before production use on a real workstation. Per decision tree, since automated truths are VERIFIED and human items are optional (not `PRESENT_BEHAVIOR_UNVERIFIED` truths left unverified), the overall status remains `passed`. If a strict `human_needed` gate is desired for workstation rollout, these items can be run as UAT before merging.

### Gaps Summary

No must-have gaps. All 6 observable truths, 6 artifacts, and 6 key links verified as substantive and wired. Roadmap Success Criteria 1,2,5 satisfied verbatim; Criteria 3 and 4 satisfied via CONTEXT-approved alternative mechanisms (deletion vs guarded mode file per D-10/D-11, self-clone vs commit-pinned Zinit per D-12) which correctly achieve the user intent `privileged and server-mode writes never surprise` more directly than the stale roadmap wording. Non-blocking warnings from code review (CR-01 `apply_catppuccin` guard, CR-02 `sudo` in dry-run uninstall preview, WR-01 string SHELL compare) are noted as WARNING/INFO above but do not violate any must_have, prohibition, or requirement, and have fix suggestions for Phase 3 without blocking Phase 2 promotion.

Deferred items: Phase 3 (Polished Shell, Theme & Local Overrides) and Phase 4 (Editor Autonomy & Verified Health) address `SHEL-02/03/04, THEM-01, EDIT-04` and `EDIT-01/02/03, HLTH-01` respectively — none of the warnings above are deferred must-haves, so no deferred section required.

---
_Verified: 2026-09-12T02:30:00Z_
_Verifier: the agent (gsd-verifier)_
